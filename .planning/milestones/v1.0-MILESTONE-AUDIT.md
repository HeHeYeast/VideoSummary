---
milestone: v1.0
audited: 2026-05-02
status: passed
scores:
  requirements: 52/52
  phases: 6/6
  integration: 4/4
  flows: 4/4
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: doc-drift
    items:
      - "CLAUDE.md:7-17 top-level 可用工具 list still says '3 个核心命令 + 2 个辅助命令' and shows only 5 legacy commands. 9 new subcommands (ingest, doctor, extract_frames_batch, detect_scenes, detect_silence, diarize, list_frames, classify_frame, ocr_frame) are wired in cmds dict and individually documented in dedicated sections, but new users reading the top of CLAUDE.md will miss them. 5-line cosmetic update."
  - phase: 03-source-refactor
    items:
      - "WR-02: agent/sources/youtube.py:267-270 — VTT subtitle file picked by filesystem-glob iteration order. subtitle_origin classification still correct (driven by info_dict per SRC-08), only subtitle_path may point at wrong language file when zh-CN+zh+en VTTs all download. (Was fixed in Phase 5 commit f5347bb — see Phase 5 REVIEW-FIX. Re-flagged here as bounded edge case for future audit.)"
  - phase: 04-frame-fps-automation
    items:
      - "WR-01/02/04: agent/scheduler.py validate gaps — inverted segments / fps=0 / ffmpeg-not-on-PATH not caught at validate time; reach ffmpeg with confusing errors. WR-04 (ffmpeg missing) breaks resume invariant. Diagnostic-quality only; user-impact bounded."
  - phase: 05-adaptive-output
    items:
      - "Pyannote spike took degrade fast-path per user decision (no real benchmark on Windows + CPU). Infrastructure ships ready; future GPU machine can populate SPIKE.md with real wall_time / RAM_peak data."
  - phase: 06-multi-agent-parallelism
    items:
      - "Two-terminal real-world smoke test documented in CLAUDE.md but intentionally not CI-asserted (timed subprocess spawning is flaky in CI). 20 unit tests cover the lock primitive + integration sites; manual two-terminal test left to user."
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 0
  overall: skipped (workflow.nyquist_validation = false)
---

# Milestone v1.0 Audit Report

**Milestone:** v1.0 — videoSummary  
**Audited:** 2026-05-02  
**Status:** ✅ **PASSED**  
**Definition of Done:** B站/抖音/YouTube 视频 → 结构化 Markdown 教程，全流程 ¥0 成本

## Scoreboard

| Dimension | Score | Notes |
|-----------|-------|-------|
| **Requirements** | 52/52 | All PRE/RES/SRC/FPS/TEACH/PARA wired or correctly self-contained (doc-only) |
| **Phases** | 6/6 | All `status: passed` in own VERIFICATION.md |
| **Integration** | 4/4 | All cross-phase wires verified by gsd-integration-checker |
| **E2E Flows** | 4/4 | Tutorial / Podcast / YouTube fallback / Multi-terminal — all complete |

## Requirements Coverage (3-source cross-reference)

All 52 milestone REQ-IDs satisfied. Per `gsd-integration-checker` review:

- **Wiring confirmed:** every REQ-ID either has a Python integration site (with grep evidence) or is correctly doc-only (in CLAUDE.md, by design — TEACH-01..05/09/10/13, PARA-06, PRE-04, RES-08).
- **Schema locks intact:** `meta.json` v1, `<artifact>.params.json` sidecar, `state.jsonl` events, Schedule v1 — all writers/readers consistent.
- **Source registry order** (`agent/sources/__init__.py`): defensive `assert` enforces Douyin → YouTube → Bilibili → Local → Generic at import time.
- **D-29 backward-compat:** all 17 archived re-runs remain byte-identical (verified Phase 2/3/5 regression reports).

## End-to-End Flows (verified)

1. ✅ **Tutorial (replicate-guide / B站):** `download → transcribe → aggregate → extract_frames_batch → CLAUDE.md adaptive teaching` — schema chain verified at each boundary.
2. ✅ **Podcast (interview-distillation):** `download → transcribe --profile podcast → aggregate --profile podcast → (optional diarize) → CLAUDE.md interview skeleton` — `--profile` sidecar threading + degrade-path both work.
3. ✅ **YouTube fallback:** `ingest <url>` → 5-class error classification (`gfw_blocked / cookies_stale / po_token_required / yt_dlp_outdated / other`) → user falls back to `ingest <local-mp4>`. Error class strings byte-equal between code and CLAUDE.md docs.
4. ✅ **Multi-terminal:** Two `cmd_transcribe` on same slug → second fails fast with `LockContended: ... held by PID <A_PID> since <ISO>`. Different slug = independent locks, parallel works. Vendor `config.yaml` race closed via separate FileLock.

## Phase-by-Phase Status

| Phase | Plans | Verification | Notes |
|-------|-------|-------------|-------|
| 01: Preflight & Regression Baseline | 3/3 | ✅ passed | 17 archives frozen as regression target |
| 02: Resume Infrastructure & Cache | 3/3 | ✅ passed | Atomic writes + sidecar + state.jsonl + doctor |
| 03: Source Refactor + new sources | 3/3 | ✅ passed | YouTube + Local + Generic + Douyin vendor crawler |
| 04: Frame fps Automation | 2/2 | ✅ passed | schedule.json + extract_frames_batch + FPS-04 silence gate |
| 05: Adaptive Output + UI Demos + Podcasts | 3/3 | ✅ passed | 4-mode CLAUDE.md + --profile + diarize CLI (degrade path) |
| 06: Multi-Agent Parallelism | 2/2 | ✅ passed | FileLock + cookies cache + slug-prefix logs (NTH shipped) |

## Tech Debt (non-blocking)

5 items aggregated — all bounded, documented, and tracked. None block milestone completion.

1. **CLAUDE.md top-level tool list outdated** (cosmetic) — 5-line update to mention 9 new subcommands. Does not affect functionality (each new subcommand has its own dedicated CLAUDE.md section).
2. **Phase 3 WR-02 (VTT picker)** — fixed in Phase 5 commit `f5347bb`; re-flagged here as bounded edge case for future audit. `subtitle_origin` classification correct; only `subtitle_path` symlink may pick wrong language file in multi-VTT case.
3. **Phase 4 WR-01/02/04 (Schedule validation gaps)** — diagnostic-quality issues (inverted segments / fps=0 / missing ffmpeg). Bounded user-impact; logged for future hardening.
4. **Phase 5 pyannote spike degraded** — user fast-path decision per `/gsd-autonomous` AskUserQuestion. Infrastructure ships ready; real benchmark deferred to GPU machine.
5. **Phase 6 two-terminal real-world smoke not in CI** — 20 unit tests cover lock primitive + integration sites; manual two-terminal test documented in CLAUDE.md but intentionally not CI-asserted (timed subprocess spawning is flaky).

## What ships in v1.0

- ✅ B站 / 抖音 / YouTube / Local mp4 / Generic — 5 source classes, ¥0 path for all
- ✅ Adaptive teaching: 4-mode classification (replicate-guide / concept-explanation / extension-applications / interview-distillation) + 8 hand-authored skeletons (4 modes × 2)
- ✅ `--profile podcast` for VAD-tightened transcription + breath-shaped paragraph aggregation
- ✅ Whisper repetition guard (warn-only, never auto-deletes per D-24 红线)
- ✅ Resume / atomic writes / parameter-aware caching / loud regen logs
- ✅ Frame fps automation via Claude-written `schedule.json` + state.jsonl resume
- ✅ Two-terminal safety: cross-platform stdlib FileLock (msvcrt + fcntl), per-slug `.resume.lock`, vendor `config.yaml` lock, slug-prefixed logs, cookies-in-memory cache
- ✅ Opt-in pyannote diarization (CLI ready; degrade path = Claude infers speakers from content)
- ✅ Doctor command for sidecar + state.jsonl introspection
- ✅ 17-archive D-29 backward-compat preserved end-to-end

## Verdict

Milestone v1.0 ships its definition of done — both core value (¥0 video → Markdown教程) and stretch goal (two-terminal NTH). Tech debt is bounded, documented, and non-blocking.

**Recommendation:** Proceed to `/gsd-complete-milestone v1.0`.
