---
phase: 08-writing-rules-claude-md-extensions-glossary
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - CLAUDE.md  # extend with new H2 section "v1.1 自适应教学文档增强 (Phase 08)" + extend "格式锁定" with 5th invariant + cross-references in /summarize-video Phase 2/6/7/8
autonomous: true
requirements:
  - CORR-01b
  - CORR-01c
  - CORR-02
  - TEACH-A1
  - TEACH-A2
  - TEACH-B
user_setup: []

must_haves:
  truths:
    - "When `output/<slug>/.v11_features.json` is present and lists `l2_l3_correction`, the /summarize-video Phase 2 prompt instructs Claude to read `transcribe_lint_warnings.json` and write a '已自动修正的术语' table to `plan.md` top — with hard caps (max 10 corrections, L2 ≥ 2 evidence sources, L3 ≤ 5 frames within ±0.5s window)."
    - "When marker lists `inline_trace_tokens`, Phase 6 写作 prompt instructs Claude to follow every concrete claim / parameter / screenshot reference with `[seg_NNNN_NNNNNN.jpg @ HH:MM:SS]` or `[para_ID @ HH:MM:SS]` — and Phase 8 收尾 prompt runs the self-check pass that adds `[?]` to confidence < 80% claims + writes the `[?] count vs total claim count` footer."
    - "When marker lists `self_contained_header`, Phase 6 prompt instructs Claude to write the locked top-of-summary structure: 标题/UP/时长/链接 → '## 读这篇前你需要知道' (≤ 3 行) → '## 你不需要知道什么' (≤ 3 行) → optional TL;DR → 正文. Header hard cap ≤ 6 lines (excluding TL;DR). Anti-patronizing: no '简单来说' / '说白了' / '你可能不知道'."
    - "When marker lists `cross_slug_glossary`, Phase 6 prompt instructs Claude to (a) annotate every first-mention term inline as `术语 (English / 中文释义)` REGARDLESS of glossary state, AND (b) append the term to cross-slug glossary via `python -m agent.tools glossary append --slug <slug> --term '...' --definition '...'` AFTER inline annotation. Universal terms (Python / JSON / Claude) are FORBIDDEN to annotate (anti-patronizing)."
    - "When marker lists `tldr_speedrun` AND (`paragraphs.json[-1].end > 1200` OR plan.md `estimated_sections > 50`), Phase 7 prompt instructs Claude to write the '## 5 分钟速读版' block LAST (after body + glossary appends). 10-15 lines hard cap (max 20). Zero citations inside (use section anchors `详见 §三、消化阶段`). Sync check: TL;DR step count ≈ body H2 count for replicate-guide mode."
    - "5th format-spec invariant added to `### 格式锁定` section: trace tokens are REQUIRED on specific claims/parameters/code/UI references, FORBIDDEN in TL;DR/glossary inline annotations/'你需要知道什么' prelude/章节小结 transitions, OPTIONAL in narrative connectors. Density target: avg ≤ 1 citation per 3 sentences."
    - "All 6 prompt extensions explicitly gate on `is_v11_enabled(slug, '<feature>')` — old archives without marker silently skip the new rules and produce v1.0-shape summary.md (D-29 byte-equal)."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Extended with new H2 section after `## 视频类型变奏` (line ~1090) — 'v1.1 自适应教学文档增强 (Phase 08)' covering 6 sub-rules; extended `### 格式锁定` with 5th invariant; added cross-reference notes in /summarize-video Phase 2/6/7/8"
      contains: "v1.1 自适应教学文档增强 (Phase 08)"
      min_lines_added: 250
  key_links:
    - from: "CLAUDE.md /summarize-video Phase 2"
      to: "CLAUDE.md v1.1 自适应教学文档增强 → CORR-01b 上下文修复 / CORR-01c 多模态兜底"
      via: "cross-reference note '当 .v11_features.json 启用 l2_l3_correction 时，详见 § v1.1 自适应教学文档增强 → CORR-01b/c'"
      pattern: "l2_l3_correction"
    - from: "CLAUDE.md /summarize-video Phase 6"
      to: "CLAUDE.md v1.1 → CORR-02 + TEACH-A1 + TEACH-A2"
      via: "cross-reference note 'v1.1 marker 启用时按 § v1.1 自适应教学文档增强 写作'"
      pattern: "inline_trace_tokens|self_contained_header|cross_slug_glossary"
    - from: "CLAUDE.md /summarize-video Phase 7"
      to: "CLAUDE.md v1.1 → TEACH-B"
      via: "cross-reference note '触发 TL;DR 写作（marker 启用 tldr_speedrun + 视频 > 20min OR estimated_sections > 50）'"
      pattern: "tldr_speedrun"
    - from: "CLAUDE.md /summarize-video Phase 8"
      to: "CLAUDE.md v1.1 → CORR-02 self-check footer"
      via: "cross-reference note 'self_check_confidence 启用时跑自检 pass，加 [?] + 写 footer'"
      pattern: "self_check_confidence"
    - from: "CLAUDE.md TEACH-A3 inline annotation rule"
      to: "agent/tools.py glossary append CLI (Plan 08-01)"
      via: "literal CLI invocation in prompt: `python -m agent.tools glossary append --slug $SLUG --term 'X (eng)' --definition '...'`"
      pattern: "agent\\.tools glossary append"
---

<objective>
Extend CLAUDE.md with the 6 prompt extensions that make v1.1-marked summaries diverge in shape — L2/L3 ASR corrections (CORR-01b/c), inline trace tokens + self-check (CORR-02), zero-baseline header (TEACH-A2), inline term annotation (TEACH-A1), TL;DR speedrun (TEACH-B). Plus the 5th format-spec invariant and cross-references at /summarize-video Phase 2/6/7/8 entry points.

Purpose: The 6 reqs are pure prompt engineering — `agent/` code change is zero. CLAUDE.md is the single source of truth Claude reads at session start. All 6 rules MUST gate on `.v11_features.json` opt-in markers (D-29 backward-compat) — old archives stay byte-equal, new marked slugs get the new shape.

Output: CLAUDE.md grows by ~250 lines (1 new H2 section after `## 视频类型变奏` + 5th invariant in `### 格式锁定` + 4 cross-reference notes in `## /summarize-video 完整工作流`). No Python touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-writing-rules-claude-md-extensions-glossary/08-CONTEXT.md
@.planning/phases/08-writing-rules-claude-md-extensions-glossary/08-01-PLAN.md
@.planning/phases/07-warm-up-k5-emitters-d-29-foundation/07-03-SUMMARY.md
@CLAUDE.md
@agent/_v11.py
@agent/glossary_audit.py
@agent/transcribe_lint.py
</context>

<interfaces>
<!-- Locked CLAUDE.md insertion anchor lines (verified by Grep at planning time). Executor uses these as exact location markers — do NOT search the file again, the line numbers below are valid as of the plan write time. -->

CLAUDE.md current structure (verified line numbers):
- Line 203: `## 视频类型变奏` (4-mode classification + 8 skeletons + Podcast骨架)
- Line 269: `### 格式锁定（无论哪个 mode，4 项不变量）` (extend with 5th invariant)
- Line 956: `### UI 操作演示子规则` (sub-section of 视频类型变奏)
- Line 983: `### Podcast / interview 模式骨架`
- Line 1092: `## v1.1 opt-in marker + 4 K5 emitters (Phase 07)` (Phase 07 baseline — DO NOT modify)
- Line 1140: `---` (separator before /summarize-video)
- Line 1142: `## /summarize-video 完整工作流` (modify Phase 2/6/7/8 sub-sections with cross-refs)

**INSERTION TARGET for the new H2 section**: BETWEEN line 1140 (`---` separator) and line 1142 (`## /summarize-video 完整工作流`) — i.e., insert AFTER the Phase 07 section closes and BEFORE the workflow walkthrough begins. Per 08-CONTEXT.md specifics: "insert Phase 8 prompt extensions as a new H2 section right after the existing `## 视频类型变奏` section so the 4-mode classification + Phase 8 quality rules are read together" — but the Phase 07 marker section now sits between them (line 1092). Decision: insert AFTER the Phase 07 marker section (line 1140) so Phase 07 marker + Phase 08 rules are co-located in the v1.1 cluster.

**5th invariant insertion target in `### 格式锁定`** (line 269-278): Insert after the existing 4 numbered invariants (after line 276), before the "锁死语" line (line 278). The 5th item is numbered `5.` and references the new H2 section for full rules.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Insert literal "v1.1 自适应教学文档增强 (Phase 08)" H2 section into CLAUDE.md after line 1140 (after Phase 07 marker section, before /summarize-video workflow)</name>
  <read_first>
    - CLAUDE.md lines 1090-1145 (verify Phase 07 section structure + the `---` separator at line 1140)
    - .planning/phases/08-writing-rules-claude-md-extensions-glossary/08-CONTEXT.md (locked decisions D-01/02/03 + 6 reqs scope)
    - .planning/phases/08-writing-rules-claude-md-extensions-glossary/08-01-PLAN.md (the glossary CLI surface this prompt references)
    - agent/_v11.py (V11_FEATURES tuple — confirm Plan 08-01 added the 5 new flag names so the prompt's `is_v11_enabled(slug, '<flag>')` checks reference real flags)
    - agent/transcribe_lint.py (WARNINGS_FILENAME = "transcribe_lint_warnings.json" — the prompt references this exact filename when reading L1 warnings)
  </read_first>
  <files>CLAUDE.md</files>
  <action>
**IMPORTANT — leading-space convention**: In the `old_string` and `new_string` blocks below, lines that are exactly ` ---` (one leading space + 3 dashes) represent CLAUDE.md horizontal-rule lines that are actually `---` (no leading space) in CLAUDE.md. The single leading space was added in this plan file ONLY to prevent gsd-tools frontmatter parser ambiguity. **Strip the leading space from those lines before passing to the `Edit` tool**. All other lines copy byte-for-byte.

**Step 1 — Locate insertion point.** Read CLAUDE.md lines 1138-1145 to confirm the `---` separator is at line 1140 and `## /summarize-video 完整工作流` starts at line 1142. (Line numbers may shift slightly depending on the worktree base; verify by reading the file before editing.) Use the `Edit` tool with the exact old/new strings below — DO NOT rely on line numbers for the actual edit.

**Step 2 — Insert the new H2 section.** Use `Edit` tool with `old_string` matching the EXACT current text spanning the end of the Phase 07 section to the start of /summarize-video:

`old_string` (the boundary region — must match byte-for-byte; copy from your read):
```
`~/.videoSummary/.queue.lock` (Phase 07 MISC-02) 是 v1.1 第一个跨 terminal 锁域。复用 `agent/_lock.py:FileLock`，stale-PID 接管逻辑同 `.resume.lock`。Phase 08 TEACH-A3 会再加 `output/.glossary.lock`（cross-slug glossary append 串行化）。

 ---

## /summarize-video 完整工作流
```

`new_string` — REPLACE WITH (literal markdown, ~280 lines; embedded between the same two boundary markers):

```markdown
`~/.videoSummary/.queue.lock` (Phase 07 MISC-02) 是 v1.1 第一个跨 terminal 锁域。复用 `agent/_lock.py:FileLock`，stale-PID 接管逻辑同 `.resume.lock`。Phase 08 TEACH-A3 会再加 `output/.glossary.lock`（cross-slug glossary append 串行化）。

 ---

## v1.1 自适应教学文档增强 (Phase 08)

> 这一节是 **opt-in 增强规则**。**只有** `output/<slug>/.v11_features.json` marker 显式启用对应 feature 的 slug 才走以下规则。无 marker 的 slug（包括 17 archived）继续按 v1.0 形态写 summary.md（D-29 byte-equal preserved）。
>
> **检查方式**：写每条 summary 之前，先用 `python -c "from agent._v11 import is_v11_enabled; print(is_v11_enabled('output/<slug>', '<feature_name>'))"` 检查 marker 是否启用对应 feature。返回 `False` 则该 feature 不触发，按 v1.0 path 写。
>
> **6 条 feature 一栏速查**（V11_FEATURES allowlist 中的对应 flag 名）：
>
> | Feature flag | 触发的规则 | 在 /summarize-video 哪个 phase 生效 |
> |---|---|---|
> | `l2_l3_correction` | CORR-01b/c — 读 `transcribe_lint_warnings.json` 做 L2 上下文修复 + L3 多模态兜底 | Phase 2（写 plan.md 时） |
> | `inline_trace_tokens` | CORR-02 — 每个 claim 后加 `[seg_*.jpg @ HH:MM:SS]` 行内溯源 token | Phase 6（写正文时） |
> | `self_check_confidence` | CORR-02 self-check pass — confidence < 80% 加 `[?]`，summary 末尾汇总 | Phase 8（收尾时） |
> | `self_contained_header` | TEACH-A1 + TEACH-A2 — 顶部"你需要知道什么"+"你不需要知道什么"+ 首次术语 inline 注解 | Phase 6（写正文起始时） |
> | `cross_slug_glossary` | TEACH-A3 — 首次术语用 `glossary append` CLI 写到 `output/_glossary.md` | Phase 6（每写完一个新术语时） |
> | `tldr_speedrun` | TEACH-B — 长视频顶部"5 分钟速读版"块 | Phase 7（写完正文后 LAST） |

### CORR-01b — L2 上下文修复（在 Phase 2 写 plan.md 时执行）

**触发条件**：`is_v11_enabled(slug, "l2_l3_correction")` 返回 True AND `output/<slug>/transcribe_lint_warnings.json` 存在且 `warnings` 数组非空。

**步骤**：

1. Read `output/<slug>/transcribe_lint_warnings.json`（Phase 07 CORR-01a 产出；schema 见 `agent/transcribe_lint.py` 模块 docstring）
2. Read `output/<slug>/meta.json`（标题 + UP 主 + description）
3. 对每个 warning，按 evidence_source + 上下文交叉验证决定是否采纳：
   - **L2 evidence sources**（每个 source 算 1 条独立证据）：
     - meta.title 子串匹配 suggested_text
     - meta.uploader / channel name 子串匹配
     - meta.description 子串匹配
     - segs.json 同段或相邻段（±2 段）出现 suggested_text 的高频形式
     - 视频简介 / chapter 标题（如有）
   - **采纳门槛（HARD CAP，违反则跳过）**：≥ 2 条独立 evidence sources 同时支持 suggested_text，才采纳
4. **采纳上限**：max 10 auto-applied corrections per slug。超过 10 条候选时按 L1 confidence 降序取前 10。其余记入 plan.md "未采纳的候选" 段。
5. 把所有 **采纳的** corrections 写到 `output/<slug>/plan.md` 顶部新增 段（在 5 字段 YAML front-matter 之后、第一个章节标题之前）：

```markdown
## 已自动修正的术语 (CORR-01b L2)

| 错误形式 | 正确形式 | 出现段 | 证据来源 | L1 confidence |
|---|---|---|---|---|
| Lora | LoRA (Low-Rank Adaptation) | para_0003 / para_0017 | meta.title + segs.json (LoRA × 4) | 0.65 |
| 木下 | Mucha | para_0021 | meta.description + frequency_variance | 0.72 |

> 自动修正自 transcribe_lint_warnings.json 的 L1 候选；L2 上下文交叉验证 ≥ 2 sources 才采纳。
> 全文写作时按"正确形式"列引用；不再回头改 segs.json (D-29 invariant)。
```

6. **D-29 invariant**：**绝不**修改 `segs.json`。修正只发生在 plan.md（透明性表）+ summary.md 写作时（按 plan.md 的"正确形式"列引用）。`segs.json` 永远是 ASR 原始输出。

### CORR-01c — L3 多模态兜底（在 Phase 4 看帧时执行）

**触发条件**：`is_v11_enabled(slug, "l2_l3_correction")` 返回 True AND 某 warning 满足 `L1 confidence < 60% AND L2 evidence < 2 sources`（即 L2 不足以采纳）。

**步骤**：

1. 对每个触发 L3 的 warning：
   - 取 warning 的 `start` 时间戳（来自 transcribe_lint_warnings.json）
   - **时间窗 HARD CAP**：抽帧范围 `[start - 0.5s, start + 0.5s]`（共 1 秒窗口）
   - **帧数 HARD CAP**：max 5 frames per warning
2. 用 `python -m agent.tools extract_frames <video> --out output/<slug>/frames --fps 5 --start <start-0.5> --end <start+0.5>` 抽帧
3. Read 抽出的 ≤ 5 帧，看图片中是否出现 suspect_text 或 suggested_text 的视觉形态（标题板 / 工具 UI / 代码 fence 中的拼写）
4. 决策：
   - 帧中**清晰出现** suggested_text → 采纳，写入 plan.md "已自动修正的术语" 表（标 evidence_source = `multimodal_frame`）
   - 帧中**清晰出现** suspect_text 且与 suggested_text 不一致 → 拒绝采纳，标 `[?]` 在 summary 中（CORR-02 self-check 会汇总）
   - 帧中**都无清晰证据** → 拒绝采纳，标 `[?]`
5. **采纳上限**：L3 采纳计入 CORR-01b 的 10 条总额（不分开计算），避免 L3 绕过总上限

### CORR-02 — 行内溯源 token + 自检 pass

**触发条件 (token)**：`is_v11_enabled(slug, "inline_trace_tokens")` 返回 True
**触发条件 (self-check)**：`is_v11_enabled(slug, "self_check_confidence")` 返回 True

#### 行内溯源 token 格式（Phase 6 写正文时）

**Token 格式锁**（违反一项即 5th format-spec invariant 违规）：

- **图片引用 token**：`[seg_NNNN_NNNNNN.jpg @ HH:MM:SS]`（HH:MM:SS 严格 8 字符，per 1st format-spec invariant）
- **段落引用 token**：`[para_NNNN @ HH:MM:SS]`（para_ID 来自 paragraphs.json，4 位补零）
- **位置**：紧跟 claim 句末，在句号 / 问号 / 感叹号**之前**插入；多个 token 用 ` + ` 分隔
- **示例**：
  - 单图：`这一步的 fps 设为 0.4 [seg_0030_000015.jpg @ 00:01:23]。`
  - 单段：`UP 反对 ECS 的核心论点是抽象层次错位 [para_0042 @ 00:09:00]。`
  - 多源：`Karpathy 在 OpenAI 待了 18 个月 [para_0007 @ 00:00:25] + [seg_0010_000003.jpg @ 00:00:25]。`

#### 引用资格规则（哪些句子需要 token）

**REQUIRED token 的句子类型**：
- 具体 claim（"UP 在 IDE 里输入了 X"）
- 具体参数值（"fps 设为 0.4 / context_window=4096 / temperature=0.7"）
- 代码片段引用（"以下代码来自 main.py，逐行抄录"——代码 fence 上方需 token）
- UI 操作引用（"点击工具栏第 3 个图标 / 按 Cmd+K 打开命令面板"）
- 截图引用（任何 `![](frames/seg_*.jpg)` 之前需 token；图片本身的 caption alt 不算 claim）

**FORBIDDEN token 的位置**（强制不放 token）：
- TL;DR 块内（`## 5 分钟速读版` — 用 `详见 §三、消化阶段` 的章节锚点替代）
- Glossary 内的 inline term annotation（`术语 (English / 中文释义)` 后不加 token）
- 顶部 "你需要知道什么" / "你不需要知道什么" prelude 段
- 章节末尾的 "*章节小结*" / "*为什么这么做*" transition 段（这些是 Claude 的提炼，不是字面 claim）

**OPTIONAL token 的位置**：
- Narrative 连接句（"接下来 UP 切换到了..."—— 可加可不加，看是否有时间戳锚点价值）
- Paraphrase 概述（"这一节核心论点是 X"——如果是 Claude 提炼，无字面 claim，可不加）

**密度目标**：avg ≤ 1 citation per 3 sentences（Phase 09 `summary_lint` 会量化校验）

#### 自检 pass（Phase 8 收尾时）

**触发条件**：`is_v11_enabled(slug, "self_check_confidence")` 返回 True

**步骤**：
1. 写完正文后，重读全文（第 2 遍）
2. 对每个带 token 的 claim 句，自评 confidence：
   - **≥ 80%**（基于字幕原文 + 帧截图 + meta，3 source 中至少 2 个直接支持）→ 不动
   - **< 80%**（仅 1 source 支持 / 部分推断 / 帧不清晰）→ 在句末 token 之后 + 句号之前插入 `[?]` 标记
   - **示例**：`这一步的 fps 设为 0.4 [seg_0030_000015.jpg @ 00:01:23][?]。`
3. 在 summary.md 末尾追加 `## 写作自检 (CORR-02)` 段：

```markdown
## 写作自检 (CORR-02)

- 总 claim 数（带 token 的句子）：47
- 低置信度标记 `[?]` 数：3
- 低置信度比例：6.4% (3/47)
- 修正历史 plan.md "已自动修正的术语" 表 → 7 条 L2 + 1 条 L3 共 8 条采纳

> 低置信度 `[?]` 句子的具体行号：
> - L142: "UP 提到 LangChain 的某个版本 [?]" — 帧中只看到 "LangChain" 字样未确认版本号
> - L257: "Tokenizer 在 GPT-4 上 vocab_size=100k [?]" — 字幕里只说"差不多十万"
> - L389: "训练用了大约 8000 步 [?]" — 字幕里说"8 千步左右"

详细引用资格规则见 § v1.1 自适应教学文档增强 → CORR-02 → 引用资格规则。
```

### TEACH-A1 — 首次术语 inline 注解

**触发条件**：`is_v11_enabled(slug, "self_contained_header")` 返回 True

**规则**：
- 每个**新术语第一次出现**时加 inline 注解：`术语 (English / 中文释义)`
- 后续出现可省略
- **FORBIDDEN 注解的术语**（普世术语，注解会显得 patronizing）：
  - 编程语言名：`Python` / `JavaScript` / `Go` / `Rust` / `Ruby` / `C++`
  - 数据格式：`JSON` / `YAML` / `XML` / `CSV` / `Markdown`
  - 工具基础：`Claude` / `Git` / `Docker` / `npm` / `pip`
  - HTTP 基础：`URL` / `API` / `HTTP` / `HTTPS`
  - 通用名词：`AI` / `ML` / `LLM`（除非视频专门讨论 LLM 内部机制）
- **REQUIRED 注解的术语类型**：
  - 领域专属概念：`LoRA (Low-Rank Adaptation)` / `ECS (Entity-Component-System)` / `RAG (Retrieval-Augmented Generation)`
  - 工具内部术语：`MCP (Model Context Protocol)` / `Compound Engineering (复利工程)`
  - 缩写首次出现：`SDXL (Stable Diffusion XL)`

**示例**：
- ✓ `本节用 LoRA (Low-Rank Adaptation) 微调，相比全参微调显存降到 1/10。后续段落直接说 LoRA，不再注解。`
- ✗ `打开 Python (一种编程语言)` — 普世术语注解会 patronizing

### TEACH-A2 — 自包含零基础 header（顶部固定结构）

**触发条件**：`is_v11_enabled(slug, "self_contained_header")` 返回 True

**结构（写在 summary.md 顶部，紧接标题之后）**：

```markdown
# <视频标题>

> UP / 来源：@xxx · 时长 MM:SS · [原视频链接](https://...)

## 读这篇前你需要知道

- <≤ 3 行先决条件>。每行写"知道什么 + 为什么需要"。
- <第 2 条>
- <第 3 条>

## 你不需要知道什么

- <≤ 3 行豁免>。每行写"不需要先学 X，文中会注解"。
- <第 2 条>
- <第 3 条>

[optional: ## 5 分钟速读版（TEACH-B 触发时插入这里，详见下文）]

 ---

# 一、<第一章正文标题>

...
```

**Hard caps**:
- "你需要知道什么" 段 ≤ 3 行
- "你不需要知道什么" 段 ≤ 3 行
- Header 总计 ≤ 6 行（不含 TL;DR 块）
- TL;DR 块（如有）独立 10-15 行 hard cap，不计入 header

**Tone constraints (anti-patronizing)**:
- **FORBIDDEN 短语**（违反则要 self-edit 重写该句）：
  - `简单来说` / `说白了` / `一言以蔽之` / `说人话就是`
  - `你可能不知道` / `相信很多人不清楚` / `你是不是觉得`
  - `通俗讲` / `打个比方` 单独使用（"打个比方" 后接具体类比 OK；空打比方禁用）
- **REQUIRED 语气**：直接陈述事实 + 第二人称指令式（"你需要先理解 X" 不是 "你可能没听说过 X"）

### TEACH-A3 — 跨 slug glossary 累积（在写正文遇到首次术语时执行）

**触发条件**：`is_v11_enabled(slug, "cross_slug_glossary")` 返回 True

**inline-first invariant (CRITICAL)**：每个首次出现的术语**必须**先按 TEACH-A1 加 inline 注解，**然后**再调用 glossary append。Glossary 是 fallback / 跨文档累积参考，**不是**首选阅读路径。**禁止**用"glossary 里有"作为跳过 inline 注解的理由。

**步骤**（每写完一个新术语的 inline 注解后立即执行）：

1. 调用 glossary append CLI（Plan 08-01 提供）：

```bash
python -m agent.tools glossary append \
  --slug <current_slug> \
  --term "LoRA (Low-Rank Adaptation)" \
  --definition "参数高效的微调技术 — 冻结预训练权重，只训练插入的低秩矩阵。" \
  --context "本视频用 LoRA 减少显存占用"
```

2. CLI 返回 `{"action": "appended"}` 或 `{"action": "skipped", "reason": "duplicate_slug_link"}`。两者都 OK，继续写作。
3. CLI 是 idempotent + FileLock 串行化（多个 terminal 并行写不同 slug 时不会撕裂）。
4. **绝不**在 summary.md 中嵌入 glossary 的链接（如 `详见 [output/_glossary.md](../../_glossary.md)`）—— glossary 是聚合视图，summary 应自包含可读。

**audit (Phase 7 收尾时建议跑一次)**：

```bash
python -m agent.tools glossary audit --json
```

报告 `duplicate_terms` / `conflicting_definitions` — 如有冲突（同一 term 不同 slug 给了不同 definition），由 Claude 决定是否手动统一（first-seen-wins 是默认 schema，但 audit 提示有歧义）。

### TEACH-B — 长视频 5 分钟速读版

**触发条件**：`is_v11_enabled(slug, "tldr_speedrun")` 返回 True AND（`paragraphs.json` 末尾段的 `end > 1200`（视频 > 20 分钟）OR plan.md front-matter `estimated_sections > 50`）

**位置**：在 TEACH-A2 header 之后、正文之前的 `## 5 分钟速读版` 段。

**结构（10-15 行 hard cap，max 20）**：

```markdown
## 5 分钟速读版

**核心结论**：<1 句，整篇 summary 最重要的一个判断>

**工作流速查表**：
1. <动词短语，章节 1 的核心动作>（详见 §一、<章节 1 标题>）
2. <动词短语>（详见 §二、<章节 2 标题>）
3. <动词短语>（详见 §三、<章节 3 标题>）
[... 最多 5-7 步，超过则压缩到 5 步以内]

**必看时间戳**（3-5 个）：
- `[00:01:23]` <这个时间戳为什么必看>
- `[00:09:00]` <第 2 个>
- `[00:15:42]` <第 3 个>

**何时跳读**：如果你只关心 X，直接跳到 §四、<章节 4>。
```

**关键规则**：

1. **写在 LAST**：必须在写完正文 + glossary appends 后才生成 TL;DR。这是防 drift 的核心约束（per P-06）。先写 TL;DR 再写正文 → 正文偏离 TL;DR 承诺。
2. **零 citation 在内**：FORBIDDEN 在 TL;DR 块内放 `[seg_*.jpg @ HH:MM:SS]` 或 `[para_NNNN @ HH:MM:SS]` token。改用 markdown 章节锚点 `详见 §三、消化阶段`（per CORR-02 引用资格规则的 FORBIDDEN 列）。
3. **同步检查（写完后自检）**：
   - **replicate-guide mode**：TL;DR "工作流速查表" 步数 ≈ 正文一级 H2 章节数（差距 ≤ 20%）。差距 > 20% → 在 plan.md 末尾追加一行 `tldr_drift_warning: TL;DR steps=N, body H2=M, ratio=...`，**不**自动修复（Claude is decider，per K2）
   - **interview-distillation mode**：TL;DR "必看时间戳" 数 ≈ chapters.json `chapters` 数（差距 ≤ 30%）
   - 其他 mode：sync check 由 Claude 自定义判断
4. **行数硬上限**：10-15 行是目标，max 20 行。超 20 行 → 重写 TL;DR 压缩信息密度，不是放宽上限。

 ---

## /summarize-video 完整工作流
```

(Note: only the `## /summarize-video 完整工作流` line at the end is the original; the entire content above it from `## v1.1 自适应教学文档增强 (Phase 08)` through the trailing `---` is NEW.)

**Step 3 — Verify the insertion**:
- `grep -n "^## v1.1 自适应教学文档增强 (Phase 08)$" CLAUDE.md` returns exactly 1 hit
- `grep -c "is_v11_enabled" CLAUDE.md` returns ≥ 7 (one in the speed-check intro + one per of the 6 feature-gating rules)
- `grep -c "trace_tokens\|inline_trace_tokens" CLAUDE.md` returns ≥ 3 (Phase 07 marker section + Phase 08 quick-reference table + Phase 08 CORR-02 sub-section)
  </action>
  <verify>
    <automated>cd D:/gxy_code/videoSummary && python -c "
import re
text = open('CLAUDE.md', encoding='utf-8').read()
# 1. New H2 section exists exactly once
assert text.count('## v1.1 自适应教学文档增强 (Phase 08)') == 1, 'H2 section missing or duplicated'
# 2. All 6 feature flag names referenced
for flag in ('l2_l3_correction', 'inline_trace_tokens', 'self_check_confidence',
             'self_contained_header', 'cross_slug_glossary', 'tldr_speedrun'):
    assert flag in text, f'missing flag reference: {flag}'
# 3. Glossary append CLI invocation literal present
assert 'agent.tools glossary append' in text, 'glossary append CLI not referenced'
# 4. Phase 07 section still intact (D-29 — no overwrite)
assert '## v1.1 opt-in marker + 4 K5 emitters (Phase 07)' in text, 'Phase 07 section damaged'
# 5. /summarize-video workflow still intact
assert '## /summarize-video 完整工作流' in text, '/summarize-video section damaged'
# 6. New section appears AFTER Phase 07 marker section AND BEFORE /summarize-video
p07 = text.index('## v1.1 opt-in marker + 4 K5 emitters (Phase 07)')
p08 = text.index('## v1.1 自适应教学文档增强 (Phase 08)')
psv = text.index('## /summarize-video 完整工作流')
assert p07 < p08 < psv, f'ordering broken: P07={p07}, P08={p08}, /sv={psv}'
print('PASS — Task 1 insertion clean')
"</automated>
  </verify>
  <acceptance_criteria>
    1. `grep -n "^## v1.1 自适应教学文档增强 (Phase 08)$" CLAUDE.md` returns exactly 1 hit.
    2. All 6 feature flag literals appear in CLAUDE.md: `l2_l3_correction`, `inline_trace_tokens`, `self_check_confidence`, `self_contained_header`, `cross_slug_glossary`, `tldr_speedrun`.
    3. The literal `agent.tools glossary append` appears in the TEACH-A3 sub-section (CLI invocation example).
    4. The Phase 07 section `## v1.1 opt-in marker + 4 K5 emitters (Phase 07)` is unchanged (still exists with same heading).
    5. The `## /summarize-video 完整工作流` section is unchanged (still exists, with same Phase 1-8 sub-structure).
    6. New H2 section appears strictly between Phase 07 marker section and /summarize-video workflow (verified by char-offset comparison).
    7. CLAUDE.md is valid UTF-8, no encoding corruption (`python -c "open('CLAUDE.md', encoding='utf-8').read()" exits 0`).
  </acceptance_criteria>
  <done>
    - CLAUDE.md grew by ~280 lines covering CORR-01b/c, CORR-02 (token rules + self-check), TEACH-A1, TEACH-A2, TEACH-A3, TEACH-B
    - All 6 sub-rules gate explicitly on `is_v11_enabled(slug, "<flag>")` checks
    - Phase 07 marker section + /summarize-video workflow remain byte-equal
    - Insertion order verified: Phase 07 → NEW Phase 08 → /summarize-video
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend `### 格式锁定` section with 5th invariant (trace tokens) + add 4 cross-reference notes in `## /summarize-video 完整工作流` Phase 2/6/7/8 sub-sections</name>
  <read_first>
    - CLAUDE.md lines 269-280 (current `### 格式锁定` 4-invariant block — extend with 5th item)
    - CLAUDE.md lines 1163-1252 (current /summarize-video Phase 1-8 sub-sections — add cross-ref notes inline)
    - The newly inserted Task 1 content (verify grep finds the new H2 anchors so cross-refs link correctly)
  </read_first>
  <files>CLAUDE.md</files>
  <action>
**IMPORTANT — leading-space convention**: same as Task 1. The ` ---` lines (1 leading space + 3 dashes) inside `old_string` / `new_string` blocks represent CLAUDE.md `---` HRs (strip the leading space when matching).

**Step 1 — Add 5th invariant to `### 格式锁定` block.**

Use `Edit` tool. Find this exact block (CLAUDE.md ~lines 269-278):

`old_string`:
```
### 格式锁定（无论哪个 mode，4 项不变量）

**这是 format-spec lock。不论 primary 是哪个 mode，summary.md 必须满足以下 4 项；违反一项就是质量退化（P1.2 退化路径）：**

1. **时间戳格式**：`[HH:MM:SS]`，必须 8 字符（`[01:23:45]` ✓ / `[1:23]` ✗ / `[83:45]` ✗ / `12:34` ✗）
2. **代码 fence 必带显式语言**：```` ```gdscript ```` / ```` ```python ```` / ```` ```bash ```` / ```` ```json ```` / ```` ```yaml ````。**不能裸 fence**（```` ``` ```` 后接代码）。即便是 shell 输出也写 ```` ```text ```` 或 ```` ```console ````
3. **图片嵌入**：`![](frames/seg_xxxx_xxxxxx.jpg)` 相对路径。**不能** absolute 路径（`![](D:/.../frames/...)` ✗）；**不能** 空 alt 含截图（OK 但放在 frames/ 目录下，非占位 placeholder）
4. **第二人称指令式**："你 + 动词"（"你打开 settings.json" ✓ / "我们打开 settings.json" ✗ / "settings.json 被打开" ✗）

锁死语：**内容自适应；形式不变。** 这 4 项是 17 archived 已建立的"读起来是 videoSummary 出品"的视觉指纹。
```

`new_string` (renames "4 项" to "4+1 项"; adds 5th item between item 4 and the lock-phrase):
```
### 格式锁定（无论哪个 mode，4+1 项不变量）

**这是 format-spec lock。不论 primary 是哪个 mode，summary.md 必须满足以下 4 项；违反一项就是质量退化（P1.2 退化路径）。第 5 项是 v1.1 opt-in 增强（仅 marker 启用 `inline_trace_tokens` 的 slug 必须满足）：**

1. **时间戳格式**：`[HH:MM:SS]`，必须 8 字符（`[01:23:45]` ✓ / `[1:23]` ✗ / `[83:45]` ✗ / `12:34` ✗）
2. **代码 fence 必带显式语言**：```` ```gdscript ```` / ```` ```python ```` / ```` ```bash ```` / ```` ```json ```` / ```` ```yaml ````。**不能裸 fence**（```` ``` ```` 后接代码）。即便是 shell 输出也写 ```` ```text ```` 或 ```` ```console ````
3. **图片嵌入**：`![](frames/seg_xxxx_xxxxxx.jpg)` 相对路径。**不能** absolute 路径（`![](D:/.../frames/...)` ✗）；**不能** 空 alt 含截图（OK 但放在 frames/ 目录下，非占位 placeholder）
4. **第二人称指令式**："你 + 动词"（"你打开 settings.json" ✓ / "我们打开 settings.json" ✗ / "settings.json 被打开" ✗）
5. **行内溯源 token (v1.1 opt-in，仅 `is_v11_enabled(slug, "inline_trace_tokens")` 时强制)**：每个 claim / 参数 / 截图引用句末加 `[seg_NNNN_NNNNNN.jpg @ HH:MM:SS]` 或 `[para_NNNN @ HH:MM:SS]` token。**FORBIDDEN** 在 TL;DR / glossary inline 注解 / "你需要知道什么" prelude / 章节小结 transitions 中放 token。**REQUIRED** 在具体 claim / 参数 / 代码 / UI 引用句。**OPTIONAL** 在 narrative 连接句。密度目标 avg ≤ 1 citation per 3 sentences。完整规则见 § v1.1 自适应教学文档增强 (Phase 08) → CORR-02。

锁死语：**内容自适应；形式不变。** 前 4 项是 17 archived 已建立的"读起来是 videoSummary 出品"的视觉指纹。第 5 项是 v1.1 marker-gated 增强 — v1.0 archives 不强制（D-29 byte-equal preserved）。
```

**Step 2 — Add cross-reference note in /summarize-video Phase 2 sub-section.**

Find (CLAUDE.md ~line 1176, the existing 2.4 paragraph):

`old_string`:
```
**2.4** 输出模式判断 + 写 plan.md（详见 § 视频类型变奏 → 模式分类）。从 4 模式（`replicate-guide` / `concept-explanation` / `extension-applications` / `interview-distillation`）选 primary + optional secondary，落到 `output/<slug>/plan.md` 顶部 5 字段 YAML front-matter；模糊 fallback 到 `replicate-guide`；写到一半误判可以改 mode 字段 + `mode_switched_at` 标记。

### Phase 3: 智能抽帧（你决定参数）
```

`new_string` (adds 2.5 v1.1 hook):
```
**2.4** 输出模式判断 + 写 plan.md（详见 § 视频类型变奏 → 模式分类）。从 4 模式（`replicate-guide` / `concept-explanation` / `extension-applications` / `interview-distillation`）选 primary + optional secondary，落到 `output/<slug>/plan.md` 顶部 5 字段 YAML front-matter；模糊 fallback 到 `replicate-guide`；写到一半误判可以改 mode 字段 + `mode_switched_at` 标记。

**2.5** **v1.1 hook (opt-in)**：如果 `is_v11_enabled('output/<slug>', 'l2_l3_correction')` 返回 True 且 `output/<slug>/transcribe_lint_warnings.json` 存在 → 走 § v1.1 自适应教学文档增强 → **CORR-01b L2 上下文修复**，把采纳的 corrections 写到 plan.md 的"已自动修正的术语"段。详见 § v1.1 自适应教学文档增强 (Phase 08) → CORR-01b/c。L3 多模态兜底在 Phase 4 看帧时执行（CORR-01c）。

### Phase 3: 智能抽帧（你决定参数）
```

**Step 3 — Add cross-reference note in /summarize-video Phase 6 sub-section.**

Find (CLAUDE.md ~line 1240, the existing mode-tip blockquote at end of Phase 6):

`old_string`:
```
> **mode 提示**：`replicate-guide` 是默认风格（17 archived 主流，本 phase 上方 markdown 模板就是它）。`concept-explanation` 不放完整代码块，只放概念图和最小例证；`extension-applications` 按场景横向罗列；`interview-distillation` 用 blockquote 替代图片（`> [HH:MM:SS] 嘉宾名："核心引文"`）。具体形态见 § 视频类型变奏 → 4 模式 skeleton。

### Phase 7: 完整代码 + 输出
```

`new_string`:
```
> **mode 提示**：`replicate-guide` 是默认风格（17 archived 主流，本 phase 上方 markdown 模板就是它）。`concept-explanation` 不放完整代码块，只放概念图和最小例证；`extension-applications` 按场景横向罗列；`interview-distillation` 用 blockquote 替代图片（`> [HH:MM:SS] 嘉宾名："核心引文"`）。具体形态见 § 视频类型变奏 → 4 模式 skeleton。

> **v1.1 hook (opt-in)**：如果 marker 启用了 `inline_trace_tokens` / `self_contained_header` / `cross_slug_glossary` 中任一 → 写正文时叠加对应规则。具体：
> - `inline_trace_tokens` → 每个 claim 句末加 `[seg_*.jpg @ HH:MM:SS]` 或 `[para_NNNN @ HH:MM:SS]` token（CORR-02 引用资格规则）
> - `self_contained_header` → 顶部按 TEACH-A2 写 "标题/UP/时长/链接 → 你需要知道 / 你不需要知道 → 正文" 结构 + TEACH-A1 首次术语 inline 注解
> - `cross_slug_glossary` → 每个新术语 inline 注解后立即调用 `python -m agent.tools glossary append --slug <slug> --term "..." --definition "..."`
>
> 三条规则的完整定义在 § v1.1 自适应教学文档增强 (Phase 08)。

### Phase 7: 完整代码 + 输出
```

**Step 4 — Add cross-reference note in /summarize-video Phase 7 sub-section.**

Find (CLAUDE.md ~line 1242):

`old_string`:
```
### Phase 7: 完整代码 + 输出

- 文档末尾合并完整代码（分文件列出）
- Write 到 `output/BVxxx/summary.md`

### Phase 8: 收尾
```

`new_string`:
```
### Phase 7: 完整代码 + 输出

- 文档末尾合并完整代码（分文件列出）
- Write 到 `output/BVxxx/summary.md`

> **v1.1 hook (opt-in)**：如果 marker 启用了 `tldr_speedrun` AND（视频时长 > 20 min OR plan.md `estimated_sections > 50`）→ 在 summary.md 顶部 header 之后、正文之前插入 `## 5 分钟速读版` 块。**写在 LAST**（写完正文 + glossary appends 后才生成，防 drift）。10-15 行 hard cap，零 citation 内容（用 `详见 §三、消化阶段` 章节锚点替代）。完整模板 + sync check 见 § v1.1 自适应教学文档增强 (Phase 08) → TEACH-B。

### Phase 8: 收尾
```

**Step 5 — Add cross-reference note in /summarize-video Phase 8 sub-section.**

Find (CLAUDE.md ~line 1247):

`old_string`:
```
### Phase 8: 收尾

- 质量自检（时间戳真实？代码从截图抄？图片对应步骤？无废话？）
- 可选：`python -m agent.tools cleanup_frames <dir> --keep <用到的帧>` 清理未引用的帧

 ---

## 质量红线
```

`new_string`:
```
### Phase 8: 收尾

- 质量自检（时间戳真实？代码从截图抄？图片对应步骤？无废话？）
- 可选：`python -m agent.tools cleanup_frames <dir> --keep <用到的帧>` 清理未引用的帧

> **v1.1 hook (opt-in)**：如果 marker 启用了 `self_check_confidence` → 跑 CORR-02 自检 pass：重读全文，对每个带 token 的 claim 句自评 confidence，< 80% 在句末加 `[?]`，并在 summary.md 末尾追加 `## 写作自检 (CORR-02)` footer 段（总 claim 数 / `[?]` 数 / 比例 + 低置信度行号列表）。完整规则见 § v1.1 自适应教学文档增强 (Phase 08) → CORR-02 → 自检 pass。
>
> 如果 marker 启用了 `cross_slug_glossary` → 收尾时建议跑一次 `python -m agent.tools glossary audit --json` 检查 `output/_glossary.md` 是否有 `duplicate_terms` / `conflicting_definitions` 需要 Claude 决策（first-seen-wins 是默认 schema，audit 只报告不修改 — K5）。

 ---

## 质量红线
```
  </action>
  <verify>
    <automated>cd D:/gxy_code/videoSummary && python -c "
text = open('CLAUDE.md', encoding='utf-8').read()
# 1. 5th invariant added
assert '4+1 项不变量' in text, '5th invariant header not updated'
assert '行内溯源 token (v1.1 opt-in' in text, '5th invariant body missing'
# 2. 4 cross-references in /summarize-video
assert '**2.5** **v1.1 hook (opt-in)**' in text, 'Phase 2.5 cross-ref missing'
assert 'self_contained_header` → 顶部按 TEACH-A2' in text, 'Phase 6 v1.1 hook missing'
assert 'tldr_speedrun` AND（视频时长 > 20 min' in text, 'Phase 7 v1.1 hook missing'
assert 'self_check_confidence` → 跑 CORR-02 自检 pass' in text, 'Phase 8 v1.1 hook missing'
# 3. Original sections intact
assert '## /summarize-video 完整工作流' in text
assert '### Phase 8: 收尾' in text
assert '## 质量红线' in text
# 4. Phase 07 marker section preserved
assert '## v1.1 opt-in marker + 4 K5 emitters (Phase 07)' in text
# 5. New Phase 08 H2 section from Task 1 still present
assert '## v1.1 自适应教学文档增强 (Phase 08)' in text
print('PASS — Task 2 cross-references clean')
"</automated>
  </verify>
  <acceptance_criteria>
    1. `### 格式锁定` section header now reads "4+1 项不变量" (was "4 项不变量"); 5th invariant body added between item 4 and the 锁死语 line.
    2. 5th invariant explicitly references `inline_trace_tokens` flag and gates with `is_v11_enabled(slug, "inline_trace_tokens")`.
    3. /summarize-video Phase 2 has new sub-step "**2.5**" v1.1 hook referencing `l2_l3_correction` flag and CORR-01b/c.
    4. /summarize-video Phase 6 has new blockquote v1.1 hook referencing `inline_trace_tokens` / `self_contained_header` / `cross_slug_glossary` flags.
    5. /summarize-video Phase 7 has new blockquote v1.1 hook referencing `tldr_speedrun` flag with the > 20min OR > 50 sections trigger.
    6. /summarize-video Phase 8 has new blockquote v1.1 hook referencing `self_check_confidence` + glossary audit.
    7. Both new H2 sections (Phase 07 marker line 1092 + Phase 08 line ~1140) and `## /summarize-video 完整工作流` section all remain present (no accidental damage from edits).
  </acceptance_criteria>
  <done>
    - 5th invariant added to `### 格式锁定` with `inline_trace_tokens` gate
    - 4 cross-reference notes added at /summarize-video Phase 2/6/7/8 entry points
    - All cross-references point back to the new "## v1.1 自适应教学文档增强 (Phase 08)" H2 anchor
    - All edits preserve surrounding text byte-equal (only specific old_string→new_string replacements)
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify D-29 byte-equal regression + cross-reference linkage + V11_FEATURES coverage by 6 prompt extensions</name>
  <read_first>
    - CLAUDE.md (entire file post-Task 1+2 edits — verify all changes coexist)
    - agent/_v11.py (V11_FEATURES tuple — confirm Plan 08-01 added the 5 new flag names)
    - scripts/replay_v10_archives.py (D-29 strict gate)
  </read_first>
  <files>CLAUDE.md</files>
  <action>
**Step 1 — Run D-29 byte-equal regression**:

```bash
cd D:/gxy_code/videoSummary
python -m scripts.replay_v10_archives --output-dir D:/gxy_code/videoSummary/output 2>&1 | tail -5
```

**Expected**: `33 PASS / 0 FAIL / 30 SKIP` (same as Phase 07 baseline). The CLAUDE.md edits are pure documentation — they do NOT touch any Python code path that re-runs aggregate. Therefore the v1.0 archives' `paragraphs.json` regen MUST still hash-equal the committed baseline.

If FAIL: investigate whether any Python module's import side-effect changed between Plan 08-01 and Plan 08-02 (Plan 08-02 should have ZERO Python edits — only CLAUDE.md). If a Python module did get touched accidentally, revert and re-do the CLAUDE.md edits without source code changes.

**Step 2 — Verify all 6 V11_FEATURES flag names referenced by the new CLAUDE.md content actually exist in V11_FEATURES allowlist**:

```bash
python -c "
from agent._v11 import V11_FEATURES
prompts_reference = {
    'l2_l3_correction',      # CORR-01b/c
    'inline_trace_tokens',   # CORR-02 token
    'self_check_confidence', # CORR-02 self-check
    'self_contained_header', # TEACH-A1+A2
    'cross_slug_glossary',   # TEACH-A3
    'tldr_speedrun',         # TEACH-B
}
missing = prompts_reference - set(V11_FEATURES)
assert not missing, f'CLAUDE.md prompts reference flags not in V11_FEATURES: {missing}'
print('PASS — all 6 prompt-referenced flags exist in V11_FEATURES')
"
```

**Step 3 — Verify cross-reference linkage** (every "see § v1.1 自适应教学文档增强" reference points to a real heading):

```bash
python -c "
import re
text = open('CLAUDE.md', encoding='utf-8').read()
# Find all '§ v1.1 自适应教学文档增强 → <X>' references
refs = re.findall(r'§\s*v1\.1\s*自适应教学文档增强[^\n]*?→\s*([^\n。]+?)(?:[\n。])', text)
print(f'Found {len(refs)} v1.1 cross-references')
# Each reference should map to a sub-section that EXISTS in the v1.1 H2 block
v11_section_start = text.index('## v1.1 自适应教学文档增强 (Phase 08)')
v11_section_end_marker = '## /summarize-video 完整工作流'
v11_section = text[v11_section_start:text.index(v11_section_end_marker)]
# Check each ref's target heading appears as ### inside the v1.1 section
expected_subsections = ['CORR-01b', 'CORR-01c', 'CORR-02', 'TEACH-A1', 'TEACH-A2', 'TEACH-A3', 'TEACH-B']
for sub in expected_subsections:
    assert sub in v11_section, f'expected subsection {sub} missing from v1.1 H2 block'
print('PASS — all 7 expected subsections (CORR-01b/c, CORR-02, TEACH-A1/A2/A3, TEACH-B) present in v1.1 H2 block')
"
```

**Step 4 — Verify byte count growth**:

```bash
python -c "
size_after = len(open('CLAUDE.md', encoding='utf-8').read())
print(f'CLAUDE.md size after Phase 08 = {size_after} bytes')
# Sanity check: should be at least ~10KB larger than pre-Plan-08-02 baseline
# (the new H2 section + 5th invariant + 4 cross-refs ~= 10-15KB of zh-CN markdown)
assert size_after > 50000, f'CLAUDE.md suspiciously small ({size_after} bytes); did edits succeed?'
print('PASS — CLAUDE.md size sanity check')
"
```

**Step 5 — Run full Phase 08 test suite to ensure no regressions**:

```bash
python -m unittest tests.test_glossary tests.test_k5_emitters tests.test_v11_marker tests.test_glossary_audit tests.test_replay_v10 -v 2>&1 | tail -10
```

**Expected**: All tests pass. CLAUDE.md edits are documentation only — no test should depend on CLAUDE.md content.
  </action>
  <verify>
    <automated>cd D:/gxy_code/videoSummary && python -m scripts.replay_v10_archives --output-dir D:/gxy_code/videoSummary/output 2>&1 | grep -E "PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    1. `python -m scripts.replay_v10_archives --output-dir D:/gxy_code/videoSummary/output` reports `33 PASS / 0 FAIL` (D-29 invariant preserved across all v1.0 archives).
    2. All 6 V11_FEATURES flag names referenced by the new CLAUDE.md content exist in `agent/_v11.V11_FEATURES` (verified by Python import check).
    3. The 7 expected subsection markers (CORR-01b, CORR-01c, CORR-02, TEACH-A1, TEACH-A2, TEACH-A3, TEACH-B) all appear inside the new "## v1.1 自适应教学文档增强 (Phase 08)" H2 block.
    4. CLAUDE.md size grew by ~10-15KB (the new content); UTF-8 encoding intact.
    5. `python -m unittest tests.test_glossary tests.test_k5_emitters tests.test_v11_marker tests.test_glossary_audit tests.test_replay_v10` shows zero failures.
    6. No Python files were modified in Plan 08-02 (verified by `git diff --stat HEAD~2 HEAD -- 'agent/*.py' 'tests/*.py' 'scripts/*.py'` showing only the Plan 08-01 commits' files, NOT the Plan 08-02 commit).
  </acceptance_criteria>
  <done>
    - D-29 strict gate still PASS (33/0)
    - 6 prompt-referenced flags all exist in V11_FEATURES
    - 7 expected subsections all present in new H2 block
    - All Python tests still pass
    - Plan 08-02 commit has only CLAUDE.md in `git show --stat` output
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude reads CLAUDE.md at session start | The file IS the runtime contract; a typo / broken anchor in CLAUDE.md silently degrades Claude's behavior in the next /summarize-video invocation (no compile error). |
| User runs /summarize-video with `.v11_features.json` enabling features Claude doesn't know how to handle | If a marker enables `inline_trace_tokens` but CLAUDE.md doesn't reference that flag, Claude has no instructions and falls back to v1.0 behavior silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | CLAUDE.md cross-reference broken (e.g., wrong section heading) | mitigate | Task 3 Step 3 grep-verifies all 7 expected subsection markers exist in the v1.1 H2 block. Task 3 Step 2 verifies all 6 referenced flag names exist in V11_FEATURES. |
| T-08-08 | Information disclosure | CLAUDE.md grows past Claude's effective working-context length, the v1.1 section gets summarized away | accept | Current CLAUDE.md is ~50KB; +15KB Phase 08 puts it at ~65KB. Claude Code's effective context for project instructions is well above 100KB. Phase 09 may need to factor 4-mode skeletons into a separate file if growth continues. Tracked as deferred enhancement. |
| T-08-09 | Repudiation | Claude inadvertently applies v1.1 rules to v1.0 archives | mitigate | Every v1.1 rule explicitly gates on `is_v11_enabled(slug, "<flag>")`. The prompt INSTRUCTS Claude to check the marker before applying — no implicit fallback. D-29 replay gate (Task 3 Step 1) catches if the gate is bypassed. |
| T-08-10 | Tampering | A future Phase 09 edit to CLAUDE.md damages the Phase 08 anchor | accept | Phase 09 is a separate plan with its own verification step. Source-of-truth for Phase 08 anchors = `## v1.1 自适应教学文档增强 (Phase 08)` heading; Phase 09 plan should grep-verify Phase 08 anchor exists before adding its own content. |
</threat_model>

<verification>

## Plan-level verification (run AFTER all 3 tasks complete)

```bash
# 1. CLAUDE.md structural integrity
cd D:/gxy_code/videoSummary
python -c "
text = open('CLAUDE.md', encoding='utf-8').read()
# All Phase 08 anchors present
assert '## v1.1 自适应教学文档增强 (Phase 08)' in text
assert '4+1 项不变量' in text
# All 6 v1.1 hooks in /summarize-video
assert '**2.5** **v1.1 hook' in text
# All 6 prompt-referenced flags exist
for flag in ('l2_l3_correction', 'inline_trace_tokens', 'self_check_confidence',
             'self_contained_header', 'cross_slug_glossary', 'tldr_speedrun'):
    assert flag in text, flag
# Pre-existing sections intact (D-29 spirit for documentation)
assert '## v1.1 opt-in marker + 4 K5 emitters (Phase 07)' in text
assert '## /summarize-video 完整工作流' in text
assert '## 视频类型变奏' in text
print('CLAUDE.md structural PASS')
"

# 2. V11_FEATURES allowlist matches CLAUDE.md references (no orphan flags)
python -c "
from agent._v11 import V11_FEATURES
import re
text = open('CLAUDE.md', encoding='utf-8').read()
v08_section_start = text.index('## v1.1 自适应教学文档增强 (Phase 08)')
v08_section_end = text.index('## /summarize-video 完整工作流')
v08 = text[v08_section_start:v08_section_end]
# Extract all backtick'd flag-style identifiers from v08 section
flag_candidates = re.findall(r'\`(l2_l3_correction|inline_trace_tokens|self_check_confidence|self_contained_header|cross_slug_glossary|tldr_speedrun)\`', v08)
referenced = set(flag_candidates)
print(f'CLAUDE.md Phase 08 references {len(referenced)} unique flags: {referenced}')
expected = {'l2_l3_correction', 'inline_trace_tokens', 'self_check_confidence', 'self_contained_header', 'cross_slug_glossary', 'tldr_speedrun'}
assert referenced == expected, f'flag mismatch: missing {expected - referenced}, extra {referenced - expected}'
# All referenced flags must be in V11_FEATURES
for f in referenced:
    assert f in V11_FEATURES, f'flag {f} referenced in CLAUDE.md but missing from V11_FEATURES'
print('Flag-allowlist consistency PASS')
"

# 3. D-29 strict gate
python -m scripts.replay_v10_archives --output-dir D:/gxy_code/videoSummary/output 2>&1 | grep -E "PASS|FAIL"

# 4. All Phase 08 tests still pass (no regressions from CLAUDE.md edits)
python -m unittest tests.test_glossary tests.test_k5_emitters tests.test_v11_marker tests.test_glossary_audit tests.test_replay_v10 -v 2>&1 | tail -8

# 5. Confirm Plan 08-02 commit touched ONLY CLAUDE.md (no Python files modified)
git log --oneline -1 -- CLAUDE.md
git diff --stat HEAD~1 HEAD 2>&1 | grep -E "agent/|tests/|scripts/" || echo "PASS — Plan 08-02 commit touches no Python files"
```

All 5 checks must pass to mark plan complete.

</verification>

<success_criteria>

- CLAUDE.md has new H2 section "## v1.1 自适应教学文档增强 (Phase 08)" inserted between the Phase 07 marker section and the /summarize-video workflow
- The new H2 section contains 7 explicit sub-rules: CORR-01b (L2 上下文修复), CORR-01c (L3 多模态兜底), CORR-02 (行内溯源 token + 自检 pass), TEACH-A1 (首次术语 inline 注解), TEACH-A2 (自包含零基础 header), TEACH-A3 (跨 slug glossary), TEACH-B (5 分钟速读版)
- Every rule gates explicitly on `is_v11_enabled(slug, "<flag>")` with the correct flag name from V11_FEATURES
- `### 格式锁定` extended from "4 项" to "4+1 项" with the 5th invariant (trace tokens) added and gated on `inline_trace_tokens`
- /summarize-video Phase 2 / 6 / 7 / 8 sub-sections each have a v1.1 hook blockquote pointing to the new H2 anchor
- All 6 prompt-referenced flag names (`l2_l3_correction`, `inline_trace_tokens`, `self_check_confidence`, `self_contained_header`, `cross_slug_glossary`, `tldr_speedrun`) exist in `agent/_v11.V11_FEATURES` (verified by Plan 08-01 extension)
- D-29 strict regression gate still 33 PASS / 0 FAIL (CLAUDE.md edits are documentation only; no Python imports changed)
- All Phase 07/08 unittest suites still pass
- Plan 08-02 git commit shows only CLAUDE.md modified (no Python file touched)

</success_criteria>

<output>
After completion, create `.planning/phases/08-writing-rules-claude-md-extensions-glossary/08-02-SUMMARY.md` per the template, with `requirements-completed: [CORR-01b, CORR-01c, CORR-02, TEACH-A1, TEACH-A2, TEACH-B]`.

Note: TEACH-A3 was completed in Plan 08-01 (the glossary CLI). Plan 08-02 ADDS the prompt rule that INSTRUCTS Claude to call that CLI — TEACH-A3's prompt-rule half is covered in this plan's CLAUDE.md TEACH-A3 sub-section, but the requirement is closed by Plan 08-01's code shipping.
</output>
