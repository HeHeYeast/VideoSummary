---
phase: 11-per-slug-index-json-aggregator-phase76-hook
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [KB-02, KB-06]
tags: [v1.2, knowledge-base, CLAUDE.md, workflow, hook, K5, D-29-gate]

must_haves:
  truths:
    - "CLAUDE.md `/summarize-video 完整工作流` section gains a `### Phase 7.6: 知识库索引（v1.2 ship 后默认启用）` block inserted between the existing Phase 7.5 verifier block and Phase 8 cleanup block"
    - "Phase 7.6 hook content prescribes: (1) 3-condition trigger gate; (2) Read 5 specific files; (3) infer 8 schema fields with mode fallback + glossary fallback rules; (4) pipe JSON to `python -m agent.tools index write --slug <slug> --from-stdin`; (5) CLI auto-validates + atomic-writes per-slug index.json + rebuilds top-level aggregator; (6) error-handling path for schema fail + non-whitelist topic"
    - "Phase 7.6 hook explicitly instructs Claude to keywords-reuse `output/_glossary.md` H2 anchors as byte-equal canonical form (e.g., `LoRA (Low-Rank Adaptation)` not `LoRA` or `Lora`) when present, and to fall back gracefully when missing — D-06 KB-03 documented at the prompt level"
    - "Phase 7.6 hook explicitly instructs Claude that `chapters[].start` derivation cross-references `paragraphs.json` for true float-second precision (RESEARCH Pitfall 4)"
    - "Phase 7.6 hook explicitly instructs Claude that the 5 D-29 core artifacts (summary.md / segs.json / paragraphs.json / meta.json / plan.md) are READ-ONLY during index generation (D-07.4 Claude-side invariant; complement to K5 source-grep CLI-side coverage)"
    - "After Phase 11 plan-02 ships, `python scripts/replay_v10_archives.py` reports the same PASS/FAIL count as before plan-02 (no regression in 4-core-artifact byte-equal — D-29 invariant preserved; index.json is a NEW sidecar outside replay scope per D-03.6)"
    - "An end-to-end manual UAT smoke (pick 1 slug from `output/`, follow Phase 7.6 hook steps, produce valid index.json + aggregator entry) is achievable using only the documented hook + the Plan 11-01 CLI"
  artifacts:
    - path: "CLAUDE.md"
      provides: "+ ~50-70 line Phase 7.6 hook block inserted between line ~1771 (current blank line after Phase 7.5) and line ~1772 (current `### Phase 8: 收尾`)"
      contains: "### Phase 7.6: 知识库索引"
    - path: ".planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-02-SUMMARY.md"
      provides: "Phase 11 completion record + D-29 replay gate result + manual UAT note for KB-02 E2E"
  key_links:
    - from: "CLAUDE.md `/summarize-video 完整工作流`"
      to: "agent/index.py → cmd_index_write → write_per_slug_index → rebuild_aggregator"
      via: "Phase 7.6 hook prescribes `python -m agent.tools index write --slug <slug> --from-stdin <<EOF`"
      pattern: "python -m agent.tools index write"
    - from: "CLAUDE.md Phase 7.6 hook"
      to: "output/_topics.md (Approved Taxonomy whitelist) + output/_glossary.md (H2 anchor canonical reuse) + paragraphs.json (chapters[].start cross-ref)"
      via: "Hook step 1 prescribes Read 5 specific files; CLI internally calls agent.topics.read_topics + agent.index.glossary_h2_anchors"
      pattern: "Read 5 个文件"
    - from: "Phase 11 close gate"
      to: "scripts/replay_v10_archives.py"
      via: "Per D-07.1: must run before phase shippable; expected output `33 PASS / 0 FAIL` (or branch baseline)"
      pattern: "scripts/replay_v10_archives.py"
---

<objective>
Insert the `### Phase 7.6: 知识库索引（v1.2 ship 后默认启用）` block into `CLAUDE.md`'s `## /summarize-video 完整工作流` section between the existing Phase 7.5 verifier block (line ~1770) and Phase 8 cleanup block (line ~1772). The hook prescribes the 5-step Phase 7.6 workflow, including: 3-condition trigger gate; Read 5 files (`summary.md` / `meta.json` / `plan.md` / `_glossary.md` / `_topics.md`) with explicit fallback rules for missing `plan.md` (→ `mode = "replicate-guide"`) and missing `_glossary.md` (→ keywords candidate set empty); the 8-field schema inference rules including topic-whitelist + `pending: <name>` escape hatch + keywords reuse `_glossary.md` H2 anchors as byte-equal canonical form + `chapters[].start` cross-reference to `paragraphs.json`; the pipe-to-CLI invocation `python -m agent.tools index write --slug <slug> --from-stdin <<EOF ... EOF`; the CLI's automatic schema validation + atomic per-slug write + immediate top-level aggregator rebuild; and the error-handling paths for schema-fail and non-whitelist-topic cases. Then run `python scripts/replay_v10_archives.py` as the D-29 byte-equal close gate per D-07.1; baseline PASS count must hold.

Purpose: Phase 11 Plan 01 shipped the CLI; this plan documents how Claude consumes the CLI inside the `/summarize-video` workflow. Without the hook, new videos do not auto-generate index.json — defeating KB-02 (Phase 7.6 auto-write) and breaking the data-flow that Phase 12 KB-12 backfill assumes (same generator, same Claude-side prompt). The hook explicitly references the K5 boundary at the prompt level (5 D-29 core artifacts are READ-ONLY) which complements the source-grep test coverage shipped in Plan 11-01. The D-29 replay gate is the **last verification** Phase 11 closes against — index.json is a NEW sidecar (D-03.6) so byte-equal across 4 core files (summary.md / segs.json / paragraphs.json / meta.json) must hold across Phase 10 → Phase 11 with zero regression.

Output: a single CLAUDE.md edit (one Edit tool call inserting ~50-70 lines), zero new code, plus a D-29 replay run captured in 11-02-SUMMARY.md. KB-02 is satisfied at the documentation level (the workflow now exists and is reachable from `/summarize-video`); KB-02 E2E (Claude actually following the hook on a real video) is a deferred manual UAT — same gating pattern as v1.1 Phase 09 P-09 token budget gate (RESEARCH Q-F + A6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-CONTEXT.md
@.planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-RESEARCH.md
@.planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-01-SUMMARY.md
@.planning/phases/10-topic-taxonomy-governance-bootstrap-cli/10-01-SUMMARY.md
@.planning/phases/10-topic-taxonomy-governance-bootstrap-cli/10-02-SUMMARY.md
@CLAUDE.md
@agent/index.py
@scripts/replay_v10_archives.py

<interfaces>
<!-- The exact CLAUDE.md text anchors the executor edits between. Verified via Bash grep on this branch. -->

CLAUDE.md insertion anchor (verified by `grep -n "Phase 7.\|Phase 8: " CLAUDE.md`):

```
1770:> 完整规则 + verifier prompt 全文 + UNRESOLVED.md 模板见 § v1.1 校对自动化 (Phase 09)。
1771:[blank line]
1772:### Phase 8: 收尾
```

The Phase 7.6 block is inserted INTO the blank line 1771, becoming:

```
1770:> 完整规则 + verifier prompt 全文 + UNRESOLVED.md 模板见 § v1.1 校对自动化 (Phase 09)。
1771:[blank line]
1772:### Phase 7.6: 知识库索引（v1.2 ship 后默认启用）  ← NEW
...
N:[blank line preceding Phase 8 H3]
N+1:### Phase 8: 收尾  ← shifted from line 1772
```

CLI invocation pattern that the hook prescribes (verbatim — must match the CLI's actual contract from Plan 11-01):

```bash
python -m agent.tools index write --slug <slug> --from-stdin <<EOF
{"slug": "<slug>", "title": "...", "duration_s": ..., "mode": "...",
 "topics": [...], "keywords": [...], "tldr_oneliner": "...",
 "chapters": [{"title": "...", "start": ..., "excerpt": "..."}, ...]}
EOF
```

Stdout JSON shape that hook documents (matches D-05.5 byte-equal contract from Plan 11-01):

```json
{"action": "written", "slug": "...", "_index_path": "...",
 "_aggregator_path": "...", "_topics_pending_appended": [...]}
```

D-29 replay gate (per D-07.1 — Phase 11 close):

```bash
python scripts/replay_v10_archives.py
# Expected: 33 PASS / 0 FAIL  (or branch-baseline; main has 17 archives + 13 skip dirs = 33/30)
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Phase 7.6 hook block into CLAUDE.md `/summarize-video 完整工作流` section</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md lines 1740-1780 (verify Phase 7 / Phase 7.5 / Phase 8 boundaries are still at expected positions)
    - CLAUDE.md `## v1.1 自适应教学文档增强` H2 block (lines ~1145+ — mirror the byte-locked rule format style)
    - CLAUDE.md `## /summarize-video 完整工作流` section opening (find via grep — anchors how the workflow is structured)
    - .planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-CONTEXT.md `<specifics>` block (verbatim Phase 7.6 markdown source per D-02)
    - .planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-RESEARCH.md §Code Examples Example C (full block including the 5 expansion bullets — fallback rules + cross-ref instructions); §Open Questions Q-B (insertion point lock — line 1771); §Pitfalls 2, 3, 4, 7 (the 4 fallback / cross-ref / read-only rules the hook MUST encode)
    - .planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-01-SUMMARY.md (verify CLI surface matches the hook's pipe-to-CLI invocation byte-equal — adjust args if Plan 11-01 deviated)
  </read_first>
  <acceptance_criteria>
    - `grep -c "### Phase 7.6: 知识库索引" CLAUDE.md` outputs `1`
    - `grep -c "### Phase 8: 收尾" CLAUDE.md` outputs `1` (still present, just shifted down)
    - `grep -c "### Phase 7.5: 校对自动化" CLAUDE.md` outputs `1` (still present, unchanged)
    - `grep -c "python -m agent.tools index write --slug" CLAUDE.md` outputs `≥ 1` (CLI invocation in the hook)
    - `grep -c "_topics.md" CLAUDE.md` shows ≥ 1 reference inside Phase 7.6 block (Approved Taxonomy whitelist instruction)
    - `grep -c "_glossary.md" CLAUDE.md` shows ≥ 1 reference inside Phase 7.6 block (H2 anchor canonical reuse instruction)
    - `grep -c "paragraphs.json" CLAUDE.md` shows ≥ 1 reference inside Phase 7.6 block (chapters[].start cross-ref instruction)
    - `grep -c "pending: " CLAUDE.md` shows ≥ 1 reference inside Phase 7.6 block (topic escape-hatch instruction)
    - `grep -c "replicate-guide" CLAUDE.md` shows ≥ N references where N includes the new Phase 7.6 block (mode fallback rule)
    - `awk '/### Phase 7.5/,/### Phase 7.6/' CLAUDE.md | wc -l` is ≥ 25 (Phase 7.5 block intact)
    - `awk '/### Phase 7.6/,/### Phase 8/' CLAUDE.md | wc -l` is ≥ 30 (Phase 7.6 block has substance — at least 30 lines)
    - The 4 explicit READ-ONLY core artifact mentions inside the Phase 7.6 block (covering D-29 5 cores being read by Claude generator without mutation): `awk '/### Phase 7.6/,/### Phase 8/' CLAUDE.md | grep -cE "READ-ONLY|read-only|只读|不修改"` is ≥ 1 (D-07.4 prompt-level invariant present in the block)
    - `python -m agent.tools index --help` still works (CLI not regressed by an inadvertent edit elsewhere)
  </acceptance_criteria>
  <action>
    Read CLAUDE.md lines 1740-1780 to confirm current insertion-anchor structure. Per RESEARCH §Open Questions Q-B (verified by Bash grep on this branch):

    - Line 1770: `> 完整规则 + verifier prompt 全文 + UNRESOLVED.md 模板见 § v1.1 校对自动化 (Phase 09)。`
    - Line 1771: blank line
    - Line 1772: `### Phase 8: 收尾`

    **Insertion strategy:** use the `Edit` tool with `old_string` matching the line-1770 + line-1771 + line-1772 sequence and `new_string` injecting the Phase 7.6 block + a separating blank line in between. Concretely:

    `old_string` (3 lines including the blank):
    ```
    > 完整规则 + verifier prompt 全文 + UNRESOLVED.md 模板见 § v1.1 校对自动化 (Phase 09)。

    ### Phase 8: 收尾
    ```

    `new_string` (the same 3 lines but with Phase 7.6 block prepended after the blank, then another blank, then Phase 8):
    ```
    > 完整规则 + verifier prompt 全文 + UNRESOLVED.md 模板见 § v1.1 校对自动化 (Phase 09)。

    ### Phase 7.6: 知识库索引（v1.2 ship 后默认启用）

    > **v1.2 hook (默认)**：满足以下全部 3 条才走 Phase 7.6（Claude is decider）：
    > 1. `output/_topics.md` 存在（v1.2 Phase 10 ship 后默认存在）
    > 2. `output/<slug>/summary.md` 已写完（Phase 7 完成 + Phase 7.5 verifier 已通过）
    > 3. `output/<slug>/index.json` 不存在 OR 用户显式要求重新生成

    **Phase 7.6 步骤**（按顺序）：

    1. **Read 5 个文件**：`output/<slug>/summary.md` / `output/<slug>/meta.json` / `output/<slug>/plan.md` / `output/_glossary.md` / `output/_topics.md`。**这 5 个文件全部 READ-ONLY**——D-29 invariant 锁死 4 核心文件（summary.md / segs.json / paragraphs.json / meta.json）byte-equal 不破；不要用 Edit / Write 工具改动它们，哪怕你读时发现 typo 也不改（K5 边界 + Plan 11-01 K5 source-grep 测试覆盖 CLI 侧；本节是 prompt 侧的对应锁）。
       - **`plan.md` 缺失情况**（17 v1.0/v1.1 archives 没有 plan.md — verified via `ls output/<slug>/`）→ `mode = "replicate-guide"` per CLAUDE.md `### 模式分类 (Phase 2 末尾步骤) → Fallback 规则`
       - **`_glossary.md` 缺失情况**（v1.1 TEACH-A3 cross_slug_glossary 是 opt-in；当前 branch + 17 archives + 16 douyin/BV 全部没有）→ `keywords` 候选集为空，由 Claude 直接从 `summary.md` 自由提议（agent.index.glossary_h2_anchors 在文件缺失时 silently 返回 `[]`，不抛异常）

    2. **推断 8 字段** (`slug` / `title` / `duration_s` / `mode` / `topics[]` / `keywords[]` / `tldr_oneliner` / `chapters[]`)：
       - `slug` = 目录名（与 `--slug <slug>` arg 一致）
       - `title` = `meta.json["title"]`
       - `duration_s` = `meta.json["duration"]`（浮点秒；可能是 int 或 float — schema validator 都接受）
       - `mode` = `plan.md` front-matter 的 `mode` 字段；缺失时 fallback `"replicate-guide"`（4 modes 之一：`replicate-guide` / `concept-explanation` / `extension-applications` / `interview-distillation`）
       - `topics[]` **必须从 `output/_topics.md` 的 `## Approved Taxonomy` 段选取**（白名单约束）。当前 ground truth 24 nodes / 5 categories（Phase 10 plan-02 ship）。**新概念**（不在 Approved 白名单内）→ 用 `"pending: <new-name>"` 字面形态（前缀 6 字节 `pending: ` 后跟新 topic 名）。CLI 收到后自动 append 到 `## Pending` 段（调用 `agent.topics.append_pending`）；普通 string topic 不在 Approved 集合 AND 没有 `pending: ` 前缀 → CLI fail-fast。
       - `keywords[]` **优先复用 `output/_glossary.md` H2 anchors 的 byte-equal canonical 形式**（如 `LoRA (Low-Rank Adaptation)` 是 canonical；不要写 `LoRA` / `Lora` / `low-rank adaptation` 散落形态）。`agent.index.glossary_h2_anchors(Path("output/_glossary.md"))` 给你 candidate set；先看 summary 里命中的 H2 anchor，命中即用 canonical 形态。新概念才创造新 keyword，不强制 append 回 `_glossary.md`（_glossary.md 由 v1.1 TEACH-A3 维护，写时机不同）。
       - `tldr_oneliner` = 1 行视频核心，10-50 字。每 mode 自然形态参考（仅 prior，不是 template）：
         - `replicate-guide`: "用 X 做 Y 的 N 步流程"
         - `concept-explanation`: "X 不是 Y，而是 Z"
         - `extension-applications`: "X 在 N 个场景里的应用对比"
         - `interview-distillation`: "嘉宾的核心判断 + 反共识立场"
       - `chapters[]` = `[{title, start, excerpt}, ...]`，**每项无独立 keywords 字段** per D-02。
         - `chapters[i].title` = summary.md 里对应 H2 章节的标题文字（去掉 `[HH:MM]` timestamp 前缀如有）
         - **`chapters[i].start` = 浮点秒**（与 segs.json / paragraphs.json 一致单位）。**关键：cross-reference `paragraphs.json` 拿真实浮点 start**——summary.md 的 `[HH:MM]` 是秒级 round（精度损失），`paragraphs.json` 的 `paragraphs[i].start` 是浮点。不同 mode 的章节 H2 约定不一致（`interview-distillation` 多用 `## [HH:MM] topic`，`replicate-guide` 多用 `## 一、Chinese-numeral`，`extension-applications` 混合）—**不要单纯 regex `summary.md`**，而是把每个 H2 章节标题语义匹配到 paragraphs.json 中最接近的 paragraph，用其 `start` 浮点值。
         - `chapters[i].excerpt` = 1-2 行（≤ 200 字）章节核心摘要；从该章节正文提炼，不要 verbatim 抄第一段

    3. **Pipe JSON 给 CLI**：
       ```bash
       python -m agent.tools index write --slug <slug> --from-stdin <<EOF
       {"slug": "<slug>", "title": "...", "duration_s": ..., "mode": "...",
        "topics": [...], "keywords": [...], "tldr_oneliner": "...",
        "chapters": [{"title": "...", "start": ..., "excerpt": "..."}, ...]}
       EOF
       ```
       JSON 结构 = per-slug index.json 8 字段 verbatim（不需要 wrapping object）；CLI 会自动在 stdin 缺 `slug` 字段时从 `--slug` arg 注入；`slug` 字段值 mismatch `--slug` arg → CLI fail-fast。

    4. **CLI 自动**:
       - 验证 8 字段 schema (`agent.index.validate_per_slug_index`) — 缺字段 / 类型错 / mode 不在 4 之内 / topic 不在白名单且非 `pending: ` 前缀 → IndexValidationError + stderr 详细错误 + exit 1
       - 持锁 `output/.index.lock`（Phase 11 D-09.1，第 4 个跨 slug 锁域）
       - atomic 写 `output/<slug>/index.json`（tempfile + os.fsync + os.replace）
       - 立刻 rebuild 顶层 `output/.index.json`（atomic write — sorted lexicographic by slug；扁平 dict `{"<slug>": <per-slug>, ...}`，无 backlinks per D-07）
       - 输出 stdout JSON: `{"action": "written" | "skipped", "slug": "...", "_index_path": "...", "_aggregator_path": "...", "_topics_pending_appended": [<names>, ...]}`
       - Idempotent: 已存在 `output/<slug>/index.json` AND stdin JSON byte-equal → no-op + `action: "skipped"`；任何字段不一致 → 覆盖（视为新版本）

    5. **错误处理**:
       - schema 校验失败 → CLI exit 1 + stderr 详细错误（field name 显式）；Claude 修正 JSON 重试
       - topic 不在白名单 AND 非 `pending: <name>` 形态 → 同上 fail-fast；Claude 改写为 `pending: <name>` 后重试（CLI 会 append 到 _topics.md Pending 段）
       - lock contended (其他终端正在写 `output/.index.json`) → exit 1 + stderr `lock contended`；Claude 等待 / 重试 / 检查孤儿 PID
       - slug dir 不存在 → exit 1 + stderr `slug dir not found`；Claude 检查 `--slug` arg 是否拼对、`--output-dir` 是否对

    > **K5 边界提醒**：Phase 7.6 hook 的 5 个被 Read 的文件中，4 个核心文件（summary.md / segs.json [虽不在本 hook Read 列表但仍受保护] / paragraphs.json / meta.json）+ plan.md 永远 READ-ONLY。Plan 11-01 的 K5 source-grep 测试已经禁止 `agent/index.py` + `cmd_index_write` + `cmd_index_rebuild` 包含这 5 文件 literal；本 hook 是对应的 prompt-level invariant。Phase 11 close 前会跑 `python scripts/replay_v10_archives.py` 双重 verify 4 核心文件 byte-equal 不破（D-07.1 close gate）。

    ### Phase 8: 收尾
    ```

    **Verify edit:** after the Edit tool call, re-read CLAUDE.md lines 1769-1850 to confirm the block is correctly placed and well-formed. Run `grep -c "### Phase 7.6: 知识库索引" CLAUDE.md` → expect `1`.

    **Critical anchors during edit:**

    - The `old_string` must include the trailing newline of line 1770 + the blank line 1771 + the `### Phase 8: 收尾` line 1772 — exact 3-line match. If line numbers have shifted (e.g., if Plan 11-01 inadvertently modified CLAUDE.md), re-grep for the line content first and adjust the match.
    - Use exact byte-for-byte matching of CJK characters in the old/new strings — Windows zh-CN GBK code-page hazards do NOT apply here because we're operating on file content via Edit tool which is UTF-8 aware (verify by running `grep -c "校对自动化" CLAUDE.md` after edit — should still be present at original location).
    - The new Phase 7.6 block uses 4 levels of markdown structure: H3 (`###`), blockquote-list with 3 conditions, ordered list (1-5 with bullets), and **bold-prefixed** sub-rules. Mirror v1.1 Phase 08 H2 byte-locked rule format style. Total ~50-70 lines.
    - The block ends with a `> **K5 边界提醒**：...` blockquote that explicitly references Plan 11-01's K5 tests + D-07.1 replay gate — this is the prompt-level pair to the source-grep test (RESEARCH Pitfall 7 + Q-F double-coverage).

    **Per CLAUDE.md `## Windows zh-CN 终端设置`:** When using Edit tool on CJK content, ensure no encoding artifacts. The block contains Chinese characters; expected to flow through UTF-8 cleanly via the Edit tool.

    **Commit message:**
    ```
    docs(11-02): CLAUDE.md /summarize-video Phase 7.6 hook (KB-02)

    Insert ### Phase 7.6: 知识库索引 block between existing Phase 7.5 verifier
    block (line 1770) and Phase 8 cleanup block (line 1772) in
    ## /summarize-video 完整工作流 section. Block prescribes:

      - 3-condition trigger gate (_topics.md exists, summary.md done,
        index.json absent or user-forced)
      - Read 5 files with explicit fallback rules: plan.md missing → mode
        = "replicate-guide" (Pitfall 2 / A4 — 17 archives); _glossary.md
        missing → keywords candidate set vacuously empty (Pitfall 3 / A3)
      - 8-field schema inference rules including topic whitelist + pending:
        <name> escape hatch + keywords reuse _glossary.md H2 anchors
        byte-equal canonical + chapters[].start cross-ref to paragraphs.json
        (Pitfall 4)
      - Pipe-to-CLI: python -m agent.tools index write --slug <slug>
        --from-stdin <<EOF (matches Plan 11-01 D-05 contract byte-equal)
      - CLI auto-validation + atomic per-slug + immediate aggregator rebuild
        + idempotent skip semantics
      - Error handling for schema-fail / non-whitelist-topic / lock-contended
        / slug-dir-missing
      - K5 boundary reminder: prompt-level invariant pairs with Plan 11-01
        source-grep tests; D-29 replay gate verifies 4-core-artifact
        byte-equal

    Implements: KB-02 (Phase 7.6 auto-write workflow documented). Per D-02.1..4.
    ```
  </action>
  <verify>
    <automated>grep -c "### Phase 7.6: 知识库索引" CLAUDE.md</automated>
  </verify>
  <done>
    `CLAUDE.md` contains the new `### Phase 7.6: 知识库索引（v1.2 ship 后默认启用）` block in the `## /summarize-video 完整工作流` section, positioned between Phase 7.5 verifier (unchanged) and Phase 8 cleanup (unchanged, just shifted down). Block has all 5 numbered steps + 3-condition trigger gate + fallback rules for missing plan.md / _glossary.md + topic whitelist + chapters[].start cross-ref + error-handling paths + K5 boundary reminder. CLI invocation in the block matches Plan 11-01's D-05 contract byte-equal. Phase 8 still ships intact at its new line position. Single commit on CLAUDE.md only (no code change).
  </done>
</task>

<task type="auto">
  <name>Task 2: Run D-29 byte-equal replay gate (Phase 11 close gate per D-07.1) + capture verification result</name>
  <files>.planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-02-SUMMARY.md</files>
  <read_first>
    - scripts/replay_v10_archives.py (verify the script is unchanged + understand its expected output format — `Summary: N PASS / M FAIL` line)
    - .planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-RESEARCH.md §Open Questions Q-F (replay gate integration recommendation — pre-Phase-11 baseline + post close gate)
    - .planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-CONTEXT.md D-07 (full text — close-gate spec)
    - .planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-01-SUMMARY.md (just-shipped Plan 01 — confirms what was changed in Phase 11)
    - .planning/phases/10-topic-taxonomy-governance-bootstrap-cli/10-01-SUMMARY.md (Phase 10 ship documented `1 PASS / 0 FAIL` on worktree, baseline reference)
    - .planning/phases/10-topic-taxonomy-governance-bootstrap-cli/10-02-SUMMARY.md (Phase 10 plan-02 last-known replay number on this branch)
  </read_first>
  <acceptance_criteria>
    - `python scripts/replay_v10_archives.py` exits 0
    - Replay stdout contains a `Summary:` line of form `Summary: N PASS / 0 FAIL` (any positive N; the FAIL count must be `0`)
    - The N value matches or exceeds the last-recorded baseline (Phase 10 plan-02 SUMMARY's recorded number)
    - `.planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-02-SUMMARY.md` exists with sections: ## What Was Done, ## CLAUDE.md Edit, ## D-29 Replay Result, ## KB-02 Manual UAT (Deferred), ## Phase 11 Close Gate Status, ## Phase 12 Readiness Checklist
    - 11-02-SUMMARY.md records the exact replay output line (verbatim quote of the `Summary:` line)
    - 11-02-SUMMARY.md notes that KB-02 E2E (Claude actually following the hook on a real video) is a manual UAT deferred to user (mirrors v1.1 Phase 09 P-09 token-budget gate pattern per RESEARCH A6) — NOT a phase-blocking item
    - `python -m unittest discover tests 2>&1 | tail -3 | grep -c "^OK"` outputs `1` (full test suite still green after CLAUDE.md edit — should be a no-op since CLAUDE.md doesn't affect tests)
    - `python -m agent.tools index --help` still works (smoke check that nothing regressed)
    - Final commit message references the D-29 replay result + KB-02 / KB-06 + close-gate status
  </acceptance_criteria>
  <action>
    Run the D-29 close gate per CONTEXT D-07.1:

    ```bash
    python scripts/replay_v10_archives.py
    ```

    Capture the full stdout output. Expected last line is `Summary: N PASS / M FAIL` where M MUST be `0`. The N value depends on which branch / worktree:

    - Main branch typically has 17 archives + 13 skip dirs = `33 PASS / 0 FAIL` per CONTEXT.md
    - Phase 10 plan-02 ship (this branch) reported the actual number — see 10-02-SUMMARY.md
    - Worktree may have a smaller number — that's still acceptable as long as `0 FAIL`

    **If `M > 0` (any FAIL):** Phase 11 is NOT shippable per D-07.2. Investigate which slug + which artifact has byte diff:

    ```bash
    # If FAIL > 0, the script prints the failing diff. Capture full output.
    # The diff should NEVER exist — Phase 11 only ships:
    #   1. Plan 01: agent/index.py + agent/tools.py + tests/* (none touch 4 core files)
    #   2. Plan 02 Task 1: CLAUDE.md edit (does not touch 4 core files in any slug)
    # If a 4-core-artifact byte diff exists, the cause is one of:
    #   (a) Inadvertent Edit/Write tool call on a slug's summary.md / segs.json /
    #       paragraphs.json / meta.json during Plan 01 or Plan 02 Task 1
    #   (b) Filesystem mtime or encoding drift caused by something else in this
    #       branch (extremely unlikely on a clean checkout)
    # Action: revert the offending edit, re-run replay, confirm 0 FAIL, then
    # mark Phase 11 shippable.
    ```

    **If `M == 0` (clean):** Phase 11 close gate passes. Proceed to write 11-02-SUMMARY.md.

    Write `.planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-02-SUMMARY.md` with the following structure:

    ```markdown
    ---
    phase: 11-per-slug-index-json-aggregator-phase76-hook
    plan: 02
    subsystem: knowledge-base/index-workflow-hook
    tags: [v1.2, knowledge-base, CLAUDE.md, workflow, hook, D-29-gate]

    dependency-graph:
      requires:
        - 11-01-PLAN ship (agent/index.py + cmd_index_{write,rebuild} + 3 K5 tests)
        - Phase 10 ship (output/_topics.md present)
        - scripts/replay_v10_archives.py (v1.1 Phase 07 ship)
      provides:
        - CLAUDE.md /summarize-video Phase 7.6 hook (Claude prompt-level workflow rule)
        - D-29 replay close gate result (Phase 11 shippable status)
      affects:
        - CLAUDE.md (single edit; no code change in this plan)

    tech-stack:
      added: []  # zero new deps; pure documentation + verification
      patterns:
        - Markdown blockquote-list rule (mirror v1.1 Phase 08 byte-locked rule format)
        - Edit-tool single-file insertion (no rename, no rewrite)
        - scripts/replay_v10_archives.py as standalone verify gate

    key-files:
      modified:
        - CLAUDE.md (+~50-70 lines around line 1771)

    decisions:
      - "Phase 7.6 inserted between Phase 7.5 verifier and Phase 8 cleanup (Q-B insertion lock)"
      - "Mode fallback `replicate-guide` documented at prompt level for plan.md-missing case (Pitfall 2 / A4 — 17 archives)"
      - "Vacuously-empty _glossary.md fallback documented (Pitfall 3 / A3)"
      - "chapters[].start cross-ref to paragraphs.json mandated (Pitfall 4)"
      - "K5 prompt-level invariant pairs with Plan 11-01 source-grep tests (Pitfall 7 / Q-F double-coverage)"
      - "KB-02 E2E E2E (Claude actually following the hook on a real video) deferred to user manual UAT, mirror v1.1 P-09 pattern (RESEARCH A6)"
      - "D-29 replay close gate run before phase shippable (D-07.1)"

    metrics:
      duration_minutes: <fill in>
      completed: <ISO date>
    ---

    # Phase 11 Plan 02 — CLAUDE.md Phase 7.6 hook + D-29 close gate Summary

    Insert the v1.2 knowledge-base Phase 7.6 hook into the existing `/summarize-video 完整工作流` section in CLAUDE.md, completing KB-02 documentation, and run the D-29 byte-equal replay gate per D-07.1 to certify Phase 11 shippable. Zero code change in this plan; one Edit tool call + one script invocation.

    ## What Was Done

    ### CLAUDE.md Edit (Task 1)

    - Anchor verified: line 1770 = `> 完整规则 + verifier prompt 全文 + UNRESOLVED.md 模板见 § v1.1 校对自动化 (Phase 09)。`; line 1771 = blank; line 1772 = `### Phase 8: 收尾` (per RESEARCH Q-B).
    - Block inserted between line 1771 and 1772, ~<N> lines added.
    - Phase 7.5 (line 1747) and Phase 8 (now line ~1772 + N) both still present and unchanged in content.
    - Block contents: 3-condition trigger gate, Read 5 files with fallback rules (plan.md → replicate-guide; _glossary.md → empty candidates), 8-field schema inference rules (topics whitelist + pending escape hatch + keywords H2 anchor reuse + chapters[].start cross-ref to paragraphs.json), pipe-to-CLI invocation byte-equal to Plan 11-01 D-05 contract, error-handling paths, K5 boundary reminder.

    ## CLAUDE.md Edit

    Verified anchors:
    - `grep -c "### Phase 7.6: 知识库索引" CLAUDE.md` → `1`
    - `grep -c "### Phase 7.5: 校对自动化" CLAUDE.md` → `1` (intact)
    - `grep -c "### Phase 8: 收尾" CLAUDE.md` → `1` (intact, shifted down)
    - `grep -c "python -m agent.tools index write --slug" CLAUDE.md` → `≥ 1` (CLI invocation in hook)

    ## D-29 Replay Result

    ```
    $ python scripts/replay_v10_archives.py
    [paste full output here]
    ...
    Summary: <N> PASS / 0 FAIL
    ```

    Result: <N> PASS / 0 FAIL — D-29 invariant **PRESERVED**. Phase 11 cleared the close gate per D-07.1. The 4 core artifacts (summary.md / segs.json / paragraphs.json / meta.json) on every replay-scope slug remain byte-equal to their Phase 10 ship state. The new sidecars (`output/<slug>/index.json` per Plan 11-01 + ready-to-be-written when Claude follows Phase 7.6 / `output/.index.json` ditto) are NOT in replay scope per D-03.6.

    Baseline comparison: Phase 10 plan-02 ship recorded `<X> PASS / 0 FAIL` (see `.planning/phases/10-topic-taxonomy-governance-bootstrap-cli/10-02-SUMMARY.md`). Phase 11 ship: `<N> PASS / 0 FAIL`. The PASS count is unchanged or higher — no regression.

    ## KB-02 Manual UAT (Deferred)

    KB-02 success criterion ("`/summarize-video` Phase 7.6 hook auto-writes index.json + rebuilds aggregator") is satisfied at the **documentation level** by Plan 11-02 Task 1 (Phase 7.6 hook block in CLAUDE.md) plus at the **CLI level** by Plan 11-01 (the `index write` + `index rebuild` subcommands actually exist and work). The **end-to-end behavioral verification** — Claude in a real `/summarize-video` session reading the hook block, following all 5 steps, producing a valid index.json + aggregator entry — is a manual UAT that requires a real Claude session on a real new video.

    This is a **deferred manual UAT** per RESEARCH A6 + Q-F, mirroring the v1.1 Phase 09 P-09 token budget gate pattern. The phase is shippable without this UAT being run; the UAT is a "next time you process a new video" check rather than a phase-blocking item. Recommend running it once on the next video the user processes.

    ## Phase 11 Close Gate Status

    | Gate | Source | Result | Note |
    |---|---|---|---|
    | KB-01 8-field schema | Plan 11-01 SC | ✅ PASS | `validate_per_slug_index` shipped + 7 unit tests |
    | KB-02 Phase 7.6 hook (doc) | Plan 11-02 Task 1 | ✅ PASS | CLAUDE.md edited + 5-step block inserted |
    | KB-02 Phase 7.6 hook (E2E) | manual UAT | ⏸ DEFERRED | by-design; mirror v1.1 P-09 pattern |
    | KB-03 keywords reuse `_glossary.md` H2 | Plan 11-01 SC | ✅ PASS | `glossary_h2_anchors` helper shipped + Pitfall 3 vacuous-empty test |
    | KB-04 atomic aggregator rebuild | Plan 11-01 SC | ✅ PASS | `rebuild_aggregator` + lexicographic ordering + stale detection |
    | KB-05 manual `index rebuild` CLI | Plan 11-01 SC | ✅ PASS | `cmd_index_rebuild` shipped + 3 edge tests |
    | KB-06 D-29 byte-equal 33/0/30 | Plan 11-02 Task 2 | ✅ PASS | `<N> PASS / 0 FAIL` on this branch |
    | K5 boundary 3 new tests | Plan 11-01 | ✅ PASS | count 17 → 20 in tests/test_k5_emitters.py |

    Phase 11 is **SHIPPABLE**. All 6 KB-XX requirements satisfied; 1 manual UAT deferred (KB-02 E2E) by design.

    ## Phase 12 Readiness Checklist

    - [x] `agent.index.write_per_slug_index(slug_dir, index_data, *, force=False)` is importable + callable (Plan 11-01)
    - [x] `--force` flag in `python -m agent.tools index write` for backfill emergency (Plan 11-01 D-05.7)
    - [x] `_topics.md` Approved Taxonomy populated (Phase 10 plan-02)
    - [x] D-29 replay baseline recorded for Phase 12 close-gate comparison
    - [x] CLAUDE.md hook section is structurally complete (only `## v1.2 知识库自然语言推荐入口` H2 remains for Phase 12 KB-14 — by design, NOT this phase's job per RESEARCH Q-B)

    ## Decisions Made

    1. **Insertion point locked at line 1771 → 1772 boundary** — alternative (inserting H2 inside the "## v1.2 知识库索引层" segment) deferred to Phase 12 KB-14 per Q-B verdict, to avoid premature H2 with empty rule body.
    2. **K5 prompt-level invariant explicitly stated in the hook** — pairs with Plan 11-01 source-grep tests; double coverage of CLI-side + Claude-side D-29 invariant per Pitfall 7 + Q-F.
    3. **No script change to scripts/replay_v10_archives.py** — index.json + .index.json are NEW sidecars, not in 4-core scope (D-03.6 / D-07.3); script needs no modification.

    ## Deviations from Plan

    [Fill in any auto-fixes / drifts; none expected — this plan is purely documentation + verification.]

    ## Self-Check: PASSED

    - File `CLAUDE.md` modified ✓
    - Phase 7.6 H3 block inserted between Phase 7.5 (line 1747) and Phase 8 (now line ~1772 + N) ✓
    - `python -m agent.tools index --help` still lists 2 subcommands ✓
    - `python -m unittest discover tests` still green ✓
    - `python scripts/replay_v10_archives.py` → `<N> PASS / 0 FAIL` ✓
    - All 6 KB-XX requirements satisfied (5 fully + 1 manual UAT deferred by design) ✓
    - Phase 11 is shippable ✓
    ```

    Fill in `<N>` (replay PASS count), `<X>` (Phase 10 baseline PASS count from 10-02-SUMMARY.md), and the metric / date / minute fields with actual values from the run.

    **Commit message:**
    ```
    docs(11-02): D-29 close gate + 11-02-SUMMARY.md (KB-02 + KB-06)

    Run scripts/replay_v10_archives.py per D-07.1 close gate: <N> PASS / 0 FAIL
    on this branch (baseline: <X> PASS / 0 FAIL from Phase 10 plan-02 ship; no
    regression). 4-core artifacts (summary.md / segs.json / paragraphs.json /
    meta.json) byte-equal preserved across Phase 11 ship — index.json sidecars
    are NEW, NOT in replay scope per D-03.6.

    Capture phase result in 11-02-SUMMARY.md: KB-01..KB-06 status, Phase 12
    readiness checklist, deviations + decisions, self-check.

    Implements: KB-06 (D-29 close gate). Per D-07.1 / D-07.2 / D-07.3.
    ```
  </action>
  <verify>
    <automated>python scripts/replay_v10_archives.py 2>&1 | grep -E "Summary:.*0 FAIL" | wc -l</automated>
  </verify>
  <done>
    `python scripts/replay_v10_archives.py` exits 0 and reports `Summary: N PASS / 0 FAIL` (N depends on branch; the FAIL count is 0). `11-02-SUMMARY.md` exists with all sections (What Was Done, CLAUDE.md Edit, D-29 Replay Result, KB-02 Manual UAT Deferred, Phase 11 Close Gate Status, Phase 12 Readiness Checklist, Decisions, Deviations, Self-Check). Final commit references D-29 replay number + KB-02/KB-06 + close-gate status. Phase 11 is now shippable per D-07.1; user / next milestone can confidently move to Phase 12 backfill (KB-12) which reuses Plan 11-01's `agent.index.write_per_slug_index` as the canonical generator entry point.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 11-02 → CLAUDE.md prompt | Documentation edit, no executable code; trust = "Claude actually reads + follows the hook in future sessions" |
| Plan 11-02 → scripts/replay_v10_archives.py | Verification script, read-only on 4 core artifacts; trust = "script's compare logic is correct from v1.1 Phase 07 ship" |
| 17 v1.0/v1.1 archives → replay scope | Pre-existing trust — D-29 invariant is the contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering | inadvertent edit on a 4-core artifact during this plan's execution | mitigate | Plan 11-02 only edits CLAUDE.md (documentation) — has zero `Edit` calls on `output/<slug>/summary.md|segs.json|paragraphs.json|meta.json|plan.md`. The replay gate at Task 2 catches any inadvertent leak (M > 0 = phase NOT shippable per D-07.2). |
| T-11-12 | Tampering | future Claude sessions ignore the hook → no auto index.json written | accept | KB-02 E2E is deferred manual UAT (RESEARCH A6 / Q-F). Pattern mirror of v1.1 Phase 09 P-09. Single-user is in-the-loop; if hook fails to trigger, user sees missing index.json on next `/summarize-video` and reports it. Mitigation by manual reproduce + iteration, not by automated test (cannot test "Claude in a real session follows a prompt rule" via unit test — that's the deferred UAT scope). |
| T-11-13 | Information Disclosure | hook leaks env vars / secrets in error messages | accept | Plan 11-02 hook only references local file paths + slug names + 8-field schema names. No env vars or secrets surface. Plan 11-01's CLI handlers (verified by their own threat model in 11-01-PLAN) ensure error messages stay benign. |
| T-11-14 | DoS | replay script hangs on a large archive corpus | accept | Phase 10 plan-02 verified replay completes; script's complexity is O(N artifacts) per slug. 33 archives × 4 artifacts × byte-compare is < 5 sec. No mitigation needed. |
| T-11-15 | Spoofing | hook content claims `index write` exists but Plan 11-01 silently regressed | mitigate | Task 1 acceptance_criteria includes `python -m agent.tools index --help` smoke check. Task 2 also runs full unit test suite — any regression in agent/tools.py CLI registration surfaces immediately. |
| T-11-16 | Repudiation | who edited Phase 7.6 hook content | accept | git tracks CLAUDE.md commits; commit message references KB-02 + Phase 7.6 explicitly. Sufficient for single-user accountability. |
</threat_model>

<verification>

## Manual Verification Checklist

After both tasks complete:

```bash
# Task 1 verifications
grep -c "### Phase 7.6: 知识库索引" CLAUDE.md            # → 1
grep -c "### Phase 7.5: 校对自动化" CLAUDE.md            # → 1 (unchanged)
grep -c "### Phase 8: 收尾" CLAUDE.md                    # → 1 (unchanged, shifted down)
grep -c "python -m agent.tools index write --slug" CLAUDE.md  # → ≥ 1 (CLI invocation in hook)
grep -c "_topics.md" CLAUDE.md                            # → ≥ N (existing + new ref)
grep -c "_glossary.md" CLAUDE.md                          # → ≥ N (existing + new ref)
awk '/### Phase 7.6/,/### Phase 8/' CLAUDE.md | wc -l    # → ≥ 30 (block has substance)
awk '/### Phase 7.6/,/### Phase 8/' CLAUDE.md | grep -cE "READ-ONLY|read-only|只读|不修改"  # → ≥ 1

# Task 2 verifications
python scripts/replay_v10_archives.py 2>&1 | tail -10
# → must contain "Summary: N PASS / 0 FAIL" with N > 0 and 0 FAIL

# Plan 11-01 still green
python -m agent.tools index --help
python -m agent.tools index write --help
python -m agent.tools index rebuild --help
python -m unittest discover tests 2>&1 | tail -3   # → "OK"

# Plan 11-02 SUMMARY exists
ls .planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-02-SUMMARY.md
grep -c "## D-29 Replay Result" .planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-02-SUMMARY.md  # → 1
grep -c "## Phase 11 Close Gate Status" .planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-02-SUMMARY.md  # → 1
```

## D-29 Replay Gate Specifics

The `Summary:` line at the end of `python scripts/replay_v10_archives.py` is the only fail-stop signal:

- `Summary: N PASS / 0 FAIL` — Phase 11 SHIPPABLE.
- `Summary: N PASS / M FAIL` (M > 0) — Phase 11 NOT shippable per D-07.2; investigate diff and fix before close.

The actual N value depends on the branch:

- main with 17 archives + 13 skip dirs (per CONTEXT.md) → expected 33
- worktree with 1 archive (per Phase 10 ship) → expected 1
- this branch's actual baseline → see Phase 10 plan-02 SUMMARY for comparison

**No expected change in N** between Phase 10 ship and Phase 11 ship — Phase 11 is purely additive (new sidecars, new CLI, new docs); 4 core artifacts are read-only throughout.

## KB-02 Manual UAT (Reminder, Deferred)

The "Phase 7.6 hook auto-writes index.json + rebuilds aggregator on a real video" E2E is a deferred manual UAT — to be run once on the user's next `/summarize-video` invocation. **Phase 11 is shippable without it** (RESEARCH A6 + Q-F lock; mirrors v1.1 Phase 09 P-09 token budget gate).

Recommended UAT script (for user on next video):

1. Pick a video that doesn't have `output/<slug>/index.json` yet.
2. Run `/summarize-video <url>` (or resume from existing partial output).
3. After Phase 7 / 7.5, observe Claude reading the 5 files + composing 8-field JSON + piping to `python -m agent.tools index write --slug <slug> --from-stdin`.
4. Verify: `output/<slug>/index.json` exists, schema-valid; `output/.index.json` contains the slug as a key.
5. Idempotent re-run: re-trigger Phase 7.6 → CLI returns `action: "skipped"` (byte-equal stdin).
6. Pending escape hatch: if Claude proposed `pending: <name>` for some topic, verify `output/_topics.md` `## Pending` segment grew by an H3 entry with 3 sub-fields.

</verification>

<success_criteria>

## Plan 11-02 Completion

This plan is complete when ALL of the following are TRUE:

- [ ] `CLAUDE.md` has a new `### Phase 7.6: 知识库索引（v1.2 ship 后默认启用）` block in the `## /summarize-video 完整工作流` section, positioned between Phase 7.5 (unchanged) and Phase 8 (unchanged, shifted down)
- [ ] The Phase 7.6 block contains all 5 numbered steps + 3-condition trigger gate + fallback rules for missing plan.md (→ `replicate-guide`) and missing _glossary.md (→ vacuously empty candidates) + 8-field schema inference rules + topic whitelist + `pending: <name>` escape hatch + keywords reuse _glossary.md H2 anchors byte-equal canonical + chapters[].start cross-ref to paragraphs.json + pipe-to-CLI invocation byte-equal to Plan 11-01 D-05 contract + error-handling paths + K5 boundary reminder
- [ ] `grep -c "### Phase 7.6: 知识库索引" CLAUDE.md` → `1`
- [ ] `grep -c "### Phase 7.5: 校对自动化" CLAUDE.md` → `1` (Phase 7.5 intact)
- [ ] `grep -c "### Phase 8: 收尾" CLAUDE.md` → `1` (Phase 8 intact)
- [ ] `grep -c "python -m agent.tools index write --slug" CLAUDE.md` → `≥ 1`
- [ ] `python scripts/replay_v10_archives.py` exits 0 and reports `Summary: N PASS / 0 FAIL` (N depends on branch; FAIL count is `0`)
- [ ] N matches or exceeds the Phase 10 plan-02 baseline (no regression)
- [ ] `.planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-02-SUMMARY.md` exists with all required sections (## What Was Done, ## CLAUDE.md Edit, ## D-29 Replay Result, ## KB-02 Manual UAT Deferred, ## Phase 11 Close Gate Status, ## Phase 12 Readiness Checklist, ## Decisions Made, ## Self-Check)
- [ ] 11-02-SUMMARY.md verbatim quotes the actual `Summary:` line from the replay run (not a placeholder)
- [ ] 11-02-SUMMARY.md notes that KB-02 E2E is a deferred manual UAT (mirror v1.1 P-09 pattern)
- [ ] `python -m unittest discover tests` still green (no test regression from CLAUDE.md edit)
- [ ] `python -m agent.tools index --help` still lists `write` + `rebuild` (Plan 11-01 CLI not regressed)
- [ ] All 6 KB-XX requirements (KB-01..KB-06) satisfied at the documentation + CLI + unit-test level (KB-02 E2E deferred by design, NOT phase-blocking)
- [ ] Both tasks committed with messages referencing KB-XX + D-XX
- [ ] Zero new third-party deps introduced (verify `requirements.txt` unchanged)
- [ ] Phase 11 is shippable; ROADMAP.md `Phase 11` row can be marked `complete` after this plan ships

</success_criteria>

<output>
After completion, ensure `.planning/phases/11-per-slug-index-json-aggregator-phase76-hook/11-02-SUMMARY.md` is committed and reflects the actual replay numbers, actual line-count of the inserted block, and the actual completion timestamp. The orchestrator (`/gsd-execute-phase`) will use this SUMMARY to confirm phase close + offer next steps (Phase 12 plan).
</output>
