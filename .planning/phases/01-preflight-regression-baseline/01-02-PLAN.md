---
phase: 01-preflight-regression-baseline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - agent/io.py
  - docs/schema-versions.md
  - agent/tools.py
  - agent/prepare.py
  - src/pipeline.py
  - src/download.py
  - agent/douyin_downloader.py
autonomous: true
requirements: [PRE-03]
must_haves:
  truths:
    - "agent/io.py exists and exports load_meta, load_segs, load_paragraphs, plus SCHEMA_VERSION = 1"
    - "Loaders return v1 semantics for files lacking the schema_version field (D-04: dict gets obj.get('schema_version', 1); list is always v1)"
    - "All 9 enumerated load sites import from agent.io and call the helpers (no inline json.loads on the three artifacts)"
    - "No file under existing output/<slug>/ is modified (D-03 hard rule)"
    - "Existing 17 archived directories load successfully through the new helpers (verified by loading the 3 baselines committed in plan 01-01)"
    - "docs/schema-versions.md exists and grep-ably records the v1 field set for meta.json / segs.json / paragraphs.json (D-05 — must be a discoverable anchor)"
    - "agent/io.py contains zero new dependencies beyond stdlib json + pathlib"
  artifacts:
    - path: "agent/io.py"
      provides: "Three schema-version-tolerant loaders + SCHEMA_VERSION constant"
      contains: "def load_meta(, def load_segs(, def load_paragraphs(, SCHEMA_VERSION = 1"
      max_lines: 60
    - path: "docs/schema-versions.md"
      provides: "v1 field-set reference for the three artifacts; loader behavior contract; v1 frame-filename convention"
      contains: "schema_version: 1, video_path, aweme_id, para_id, seg_indices"
    - path: "agent/tools.py"
      provides: "Load sites L1 (line 77) and L2 (line 94) replaced with load_segs() calls"
    - path: "agent/prepare.py"
      provides: "Load sites L3 (line 77, meta), L4 (line 92, segs), L5 (line 114, paragraphs) replaced with helper calls"
    - path: "src/pipeline.py"
      provides: "Load sites L6 (line 34, meta) and L7 (line 46, segs) replaced with helper calls"
    - path: "src/download.py"
      provides: "Load site L8 (line 20, meta) replaced with load_meta() call"
    - path: "agent/douyin_downloader.py"
      provides: "Load site L9 (line 150, meta) replaced with load_meta() call"
  key_links:
    - from: "agent/tools.py (cmd_transcribe and cmd_aggregate)"
      to: "agent/io.load_segs"
      via: "import + replacement of inline json.loads"
      pattern: "from agent\\.io import.*load_segs"
    - from: "agent/prepare.py"
      to: "agent/io.load_meta / load_segs / load_paragraphs"
      via: "imports replacing 3 inline json.loads call sites"
      pattern: "from agent\\.io import"
    - from: "src/pipeline.py"
      to: "agent/io.load_meta / load_segs"
      via: "imports replacing 2 inline json.loads call sites"
      pattern: "from agent\\.io import"
    - from: "src/download.py"
      to: "agent/io.load_meta"
      via: "import + replacement at line 20"
      pattern: "from agent\\.io import"
    - from: "agent/douyin_downloader.py"
      to: "agent/io.load_meta"
      via: "import + replacement at line 150"
      pattern: "from agent\\.io import"
    - from: "docs/schema-versions.md"
      to: "agent/io.py / tests/regression/regression-check.md"
      via: "cross-link in 'Reference' section"
      pattern: "agent/io\\.py"
---

<objective>
Land schema-version tolerance as a single landing point so all future v2 work
has one file to change. Today this means three trivial loader functions
(`load_meta`, `load_segs`, `load_paragraphs`) in a new `agent/io.py` and a
`docs/schema-versions.md` retroactive field-set reference. Patch all 9
enumerated load sites (across 5 files) to use the helpers — no behavior
change today, but tomorrow's v2 migration is a single switch statement.

This plan delivers PRE-03 (`schema_version: 1` retroactively documented;
loaders default to v1 when the field is absent). Per D-03 hard rule, this
plan **does not modify any file under `output/<slug>/`** — the 17 archived
directories stay byte-identical. Per D-04, top-level-list artifacts
(`segs.json`, `paragraphs.json`) are NOT wrapped into a versioned dict (would
break PROJECT.md K3); they are unconditionally treated as v1 at the loader.

Purpose: Pitfall P7.4 in PITFALLS.md flags schema drift between runs as the
root cause of "why didn't my fix work?" debugging. Resolving it cheaply now
(one module, ~30 LOC) prevents every later phase from re-deciding "should
I patch the loader inline or centralize?". Per D-06, real v2 migration cost
belongs to whichever phase first wants v2 — this plan only lays the runway.

Output:
- New `agent/io.py` (≤ 60 lines, stdlib-only)
- New `docs/schema-versions.md` (v1 field reference)
- 9 single-line patches across 5 existing .py files (mechanical replacements)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-preflight-regression-baseline/01-CONTEXT.md
@.planning/phases/01-preflight-regression-baseline/01-RESEARCH.md
@.planning/phases/01-preflight-regression-baseline/01-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@agent/tools.py
@agent/prepare.py
@agent/douyin_downloader.py
@src/pipeline.py
@src/download.py
@agent/asr_v2.py

<interfaces>
<!-- Existing producers and the JSON shapes they emit. The loaders we ship
     must accept these shapes byte-for-byte (verified against committed
     baselines from plan 01-01). -->

From agent/asr_v2.py (paragraph producer):
```python
@dataclass
class Paragraph:
    para_id: str            # format "p%04d"
    start: float
    end: float
    text: str
    seg_indices: list[int] = field(default_factory=list)
```

From src/asr.py (segment producer):
```python
@dataclass
class Segment:
    start: float
    end: float
    text: str
```

meta.json producers (verified by RESEARCH §"JSON Shape Ground Truth"):
```python
# B站 path: src/download.py:77-87 emits dict with keys
#   video_path, subtitle_path, title, uploader, duration, description, url

# 抖音 path: agent/douyin_downloader.py:201-211 adds two more keys
#   aweme_id (str), source ("douyin")
```

Existing JSON read idiom (the pattern the new loaders wrap):
```python
# CONVENTIONS.md §"I/O & Path Conventions" — locked at 30+ sites
data = json.loads(path.read_text(encoding="utf-8"))
```

Lazy import pattern in agent/tools.py (used at lines 65, 92):
```python
sys.path.insert(0, str(Path(__file__).parent.parent))
from src.asr import ...
```
The new `from agent.io import ...` import follows this style.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create agent/io.py loaders + docs/schema-versions.md (the contract)</name>
  <files>agent/io.py, docs/schema-versions.md</files>
  <behavior>
    - load_meta(path) on a normal `output/<slug>/meta.json` (B站 path) returns a dict containing keys video_path / subtitle_path / title / uploader / duration / description / url.
    - load_meta(path) on a 抖音 meta.json returns a dict that ALSO contains aweme_id and source: "douyin".
    - load_meta(path) on a meta.json missing schema_version (i.e., all 17 existing archives) succeeds and returns the dict — `obj.get("schema_version", 1)` defaults to v1.
    - load_meta(path) on a JSON file containing a top-level non-dict (e.g., `[]`) raises ValueError with a message naming the path and the actual type — fail-fast per CONVENTIONS.md §"Error Handling".
    - load_segs(path) on a normal `output/<slug>/segs.json` returns a list of dicts each containing start / end / text.
    - load_segs(path) on a JSON file containing a top-level non-list (e.g., `{}`) raises ValueError naming path and actual type.
    - load_paragraphs(path) on a normal `output/<slug>/paragraphs.json` returns a list of dicts each containing para_id / start / end / text / seg_indices.
    - load_paragraphs(path) on a JSON file containing a top-level non-list raises ValueError.
    - Module exposes `SCHEMA_VERSION = 1` as an importable constant.
    - Module imports nothing beyond `json` (stdlib) and `pathlib.Path` (stdlib) plus `from __future__ import annotations`.
    - All three loaders accept either `str` or `pathlib.Path` for the path argument, coerced via `Path(path)` on entry (matches CONVENTIONS.md §"I/O & Path Conventions").
  </behavior>
  <read_first>
    .planning/phases/01-preflight-regression-baseline/01-CONTEXT.md (D-03..D-06 — loader-only tolerance, dict-vs-list rule, defer migration cost)
    .planning/phases/01-preflight-regression-baseline/01-RESEARCH.md (§"Pattern 1: Schema-Tolerant Loader Helper" — full skeleton; §"JSON Shape Ground Truth" — exact v1 field set; §"docs/schema-versions.md skeleton" — full doc template; §"Pitfall 1: Over-engineering the loader" — keep ≤ 50 lines, no classes; §"Pitfall 4: schema-versions.md location" — must be grep-able from `docs/`)
    .planning/PROJECT.md (K3 hard rule — no modification of existing output/<slug>/ files)
    .planning/codebase/CONVENTIONS.md (§"I/O & Path Conventions" — JSON read idiom; §"Type Hints" — `from __future__ import annotations`, `str | Path` pattern; §"Error Handling" — fail-fast with informative messages; §"Module Design" — no `__all__`, public surface = anything not `_`-prefixed)
    tests/regression/BV132wizyEEB/meta.json (committed by 01-01 — B站 variant with `\\` separators)
    tests/regression/BV1C9QCBdE1U/meta.json (committed by 01-01 — B站 variant with forward slashes)
    tests/regression/douyin_trae_ai/meta.json (committed by 01-01 — 抖音 variant with aweme_id + source)
    tests/regression/BV1C9QCBdE1U/segs.json (committed by 01-01 — list-shape ground truth)
    tests/regression/BV1C9QCBdE1U/paragraphs.json (committed by 01-01 — list-shape ground truth)
    agent/asr_v2.py (Paragraph dataclass — para_id format)
    src/asr.py (Segment dataclass — three required fields)
  </read_first>
  <action>
**Step A — Create `agent/io.py`** (verbatim from RESEARCH §"Pattern 1", with the exact docstring rationale lifted from research). The file MUST be ≤ 60 lines, stdlib-only:

```python
"""Schema-tolerant loaders for output/<slug>/ artifacts.

PRE-03 (Phase 1): centralizes the schema_version normalization so future
v2 migrations have a single landing point. Today all artifacts are v1;
this module exists to make tomorrow's v2 add a single switch statement
instead of a codebase-wide edit.

Behavior:
- Dict-shaped artifacts (meta.json): obj.get("schema_version", 1) — absence == v1
- List-shaped artifacts (segs.json, paragraphs.json): always treated as v1
  (top-level list cannot carry a schema_version field without breaking
  backward-compat per PROJECT.md K3 / D-04)

When v2 lands, add a migrate_to_current(obj) call inside each loader and
update the SCHEMA_VERSION constant. No call site changes.

Reference: docs/schema-versions.md
"""
from __future__ import annotations

import json
from pathlib import Path

SCHEMA_VERSION = 1  # current version for new artifacts; bump in v2 phase


def load_meta(path: str | Path) -> dict:
    """加载 meta.json. Dict 工件支持 schema_version 字段 (缺失则视为 v1)."""
    obj = json.loads(Path(path).read_text(encoding="utf-8"))
    if not isinstance(obj, dict):
        raise ValueError(
            f"meta.json must be a dict (v1); got {type(obj).__name__} at {path}"
        )
    # v1: schema_version absent or == 1 → no migration needed
    # future v2: insert obj = _migrate_meta(obj) here
    return obj


def load_segs(path: str | Path) -> list[dict]:
    """加载 segs.json. 顶层 list 类型, 一律视为 schema_version=1.

    历史决定 (D-04): 把 segs.json 改成 wrapped dict 即破坏老归档, 故顶层
    list 是 v1 的稳定契约; 未来 v2 如需结构升级, 由那个 phase 决定是否
    引入 wrapping (并写 migration script).
    """
    obj = json.loads(Path(path).read_text(encoding="utf-8"))
    if not isinstance(obj, list):
        raise ValueError(
            f"segs.json must be a list (v1); got {type(obj).__name__} at {path}"
        )
    return obj


def load_paragraphs(path: str | Path) -> list[dict]:
    """加载 paragraphs.json. 同 load_segs, 顶层 list 即 v1."""
    obj = json.loads(Path(path).read_text(encoding="utf-8"))
    if not isinstance(obj, list):
        raise ValueError(
            f"paragraphs.json must be a list (v1); got {type(obj).__name__} at {path}"
        )
    return obj
```

(RESEARCH skeleton omits the `isinstance(obj, dict)` check on `load_meta`; per RESEARCH §"Open Question 1 recommendation: keep the isinstance check — fail-fast is project convention", the action above adds it for symmetry with the list checks. Three loaders, three identical isinstance guards.)

**Step B — Create `docs/schema-versions.md`**. This is the new `docs/` directory at repo root (first time in project history per CONVENTIONS.md / STRUCTURE.md). Lift the entire skeleton verbatim from RESEARCH §"docs/schema-versions.md skeleton". The doc must contain (verifier checks for these literal strings):

- `schema_version: 1` (lock-version statement)
- `video_path` and Pitfall 5 footnote: "Path to video.mp4. Platform-dependent separator — may contain `/` or `\\`. Treat as opaque."
- `aweme_id` and `"source": "douyin"` notes for the 抖音 variant
- `para_id` (format `p%04d`)
- `seg_indices` (list[int], 0-indexed)
- v1 frame filename convention: `seg_<start_seconds:04d>_<frame_index:06d>.jpg`
- Cross-link to `agent/io.py` and `tests/regression/regression-check.md`

Keep the doc's "Loader Behavior (locked by Phase 1)" section explicit per D-04: "List artifacts: top-level list always treated as v1. Wrapping into `{schema_version: ..., items: [...]}` would break the 17 archived `output/<slug>/` directories and is forbidden by PROJECT.md K3."

Per Pitfall 5 in RESEARCH, also include the platform-separator note: "v1 `video_path` is platform-dependent; loaders treat it as opaque string. BV132wizyEEB carries `\\` separators (Windows); BV1C9QCBdE1U carries `/` (downloaded on a different day / shell). Both are valid v1."

Per A5 in RESEARCH §"Assumptions Log": explicitly document that this phase does NOT write `schema_version` into NEW artifacts — only adds tolerant reading. Phase 2 (RES-08) will decide the writing convention.

**Per D-06**, do not write a migration framework. The `# future v2: insert ...` comment is the entire forward-compat surface. No registry, no version dispatch table, no class hierarchy.
  </action>
  <verify>
    <automated>
# agent/io.py exists and is small (target ≤ 60 lines, hard cap 80)
test -s agent/io.py && echo "OK: agent/io.py exists"
LINES=$(wc -l < agent/io.py)
echo "agent/io.py is $LINES lines"
test "$LINES" -le 80 && echo "OK: agent/io.py within size cap"

# Required identifiers in agent/io.py
grep -q '^def load_meta(' agent/io.py && echo "OK: load_meta defined"
grep -q '^def load_segs(' agent/io.py && echo "OK: load_segs defined"
grep -q '^def load_paragraphs(' agent/io.py && echo "OK: load_paragraphs defined"
grep -q '^SCHEMA_VERSION = 1' agent/io.py && echo "OK: SCHEMA_VERSION constant present"
grep -q 'from __future__ import annotations' agent/io.py && echo "OK: future annotations import"

# Stdlib-only constraint: only json + pathlib + __future__ should be imported
NON_STDLIB=$(grep -E '^(import|from)' agent/io.py | grep -vE '^(from __future__|import json|from pathlib)')
test -z "$NON_STDLIB" && echo "OK: stdlib-only" || (echo "FAIL: extra imports: $NON_STDLIB"; false)

# Behavioral test (run via python directly — no pytest dependency, per ¥0 / no-CI philosophy):
python -c "
import sys
sys.path.insert(0, '.')
from agent.io import load_meta, load_segs, load_paragraphs, SCHEMA_VERSION

assert SCHEMA_VERSION == 1, f'expected SCHEMA_VERSION=1, got {SCHEMA_VERSION}'

# Load all 3 baselines (committed by plan 01-01) — no schema_version field present in any of them
m1 = load_meta('tests/regression/BV132wizyEEB/meta.json')
m2 = load_meta('tests/regression/BV1C9QCBdE1U/meta.json')
m3 = load_meta('tests/regression/douyin_trae_ai/meta.json')
assert isinstance(m1, dict) and 'video_path' in m1 and 'title' in m1
assert isinstance(m2, dict) and 'video_path' in m2 and 'duration' in m2
assert isinstance(m3, dict) and 'aweme_id' in m3 and m3.get('source') == 'douyin'
print('OK: 3 meta.json baselines load with v1 default')

s1 = load_segs('tests/regression/BV1C9QCBdE1U/segs.json')
assert isinstance(s1, list) and len(s1) > 0
assert 'start' in s1[0] and 'end' in s1[0] and 'text' in s1[0]
print('OK: segs.json baseline loads as list of dicts')

p1 = load_paragraphs('tests/regression/BV1C9QCBdE1U/paragraphs.json')
assert isinstance(p1, list) and len(p1) > 0
assert 'para_id' in p1[0] and 'seg_indices' in p1[0]
print('OK: paragraphs.json baseline loads as list of dicts')

# Fail-fast on wrong shape
import tempfile, json as j
tmp = tempfile.NamedTemporaryFile(mode='w', suffix='.json', delete=False, encoding='utf-8')
tmp.write(j.dumps([]))   # list where dict expected
tmp.close()
try:
    load_meta(tmp.name)
    print('FAIL: load_meta should have raised on list input')
    sys.exit(1)
except ValueError as e:
    assert 'must be a dict' in str(e)
    print('OK: load_meta rejects non-dict')

tmp2 = tempfile.NamedTemporaryFile(mode='w', suffix='.json', delete=False, encoding='utf-8')
tmp2.write(j.dumps({}))  # dict where list expected
tmp2.close()
try:
    load_segs(tmp2.name)
    print('FAIL: load_segs should have raised on dict input')
    sys.exit(1)
except ValueError as e:
    assert 'must be a list' in str(e)
    print('OK: load_segs rejects non-list')

# Path | str polymorphism
from pathlib import Path
m = load_meta(Path('tests/regression/BV132wizyEEB/meta.json'))
assert isinstance(m, dict)
print('OK: Path argument works')
print('ALL agent/io.py BEHAVIOR TESTS PASS')
"

# docs/schema-versions.md exists and contains required strings
test -s docs/schema-versions.md && echo "OK: docs/schema-versions.md exists"
grep -q 'schema_version: 1' docs/schema-versions.md && echo "OK: schema_version: 1 lock stated"
grep -q 'video_path' docs/schema-versions.md && echo "OK: video_path field documented"
grep -q 'aweme_id' docs/schema-versions.md && echo "OK: aweme_id (douyin variant) documented"
grep -q '"source": "douyin"' docs/schema-versions.md && echo "OK: source: douyin documented"
grep -q 'para_id' docs/schema-versions.md && echo "OK: para_id documented"
grep -q 'seg_indices' docs/schema-versions.md && echo "OK: seg_indices documented"
grep -qE 'seg_<.*start.*>.*<.*frame.*>\.jpg|seg_<start_seconds:04d>_<frame_index:06d>\.jpg' docs/schema-versions.md && echo "OK: frame filename grammar documented"
grep -q 'agent/io\.py' docs/schema-versions.md && echo "OK: links to agent/io.py"
grep -q 'tests/regression/regression-check\.md' docs/schema-versions.md && echo "OK: links to regression-check.md"
grep -qE 'PROJECT\.md.*K3|K3' docs/schema-versions.md && echo "OK: K3 backward-compat reference"
    </automated>
  </verify>
  <done>
- `agent/io.py` exists with exactly three public loader functions (`load_meta`, `load_segs`, `load_paragraphs`) and one public constant (`SCHEMA_VERSION = 1`).
- `agent/io.py` is ≤ 80 lines and imports only stdlib (`json` + `pathlib.Path` + `__future__`).
- All three loaders accept `str | Path`, return parsed JSON, and raise `ValueError` on shape mismatch.
- All 3 baseline files committed by plan 01-01 load successfully through the helpers (no `schema_version` field present, default to v1 — D-04).
- `docs/schema-versions.md` exists, documents the v1 field set for all three artifact types, calls out the `\\` vs `/` separator heterogeneity (Pitfall 5), the douyin-variant fields, the v1 frame filename convention, and the K3-backward-compat rationale.
- The doc cross-links to `agent/io.py` and `tests/regression/regression-check.md` (Pitfall 4: must be grep-discoverable).
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch all 9 load sites across 5 .py files to use agent.io helpers</name>
  <files>agent/tools.py, agent/prepare.py, src/pipeline.py, src/download.py, agent/douyin_downloader.py</files>
  <read_first>
    .planning/phases/01-preflight-regression-baseline/01-CONTEXT.md (D-03 hard rule — do not modify any output/<slug>/ file; D-04 — list shape stays as-is, no wrapping)
    .planning/phases/01-preflight-regression-baseline/01-RESEARCH.md (§"Loader-Tolerance Landing Point" — verbatim 9-row patch table with file:line / current code / replacement; §"Pattern 2: In-Place Loader Patch" — exact before/after; §"Where agent/io.py should be imported" — two acceptable styles)
    .planning/codebase/CONVENTIONS.md (§"Imports & Module Boundaries" — order: future > stdlib > 3rd-party > local; lazy-import pattern at handler scope used in agent/tools.py:65,92)
    .planning/codebase/STRUCTURE.md (`agent/` vs `src/` layering — both can import `agent.io`)
    agent/io.py (the new module being imported — verify exports match the imports we're about to add)
    agent/tools.py (255 lines, full file — patch sites L1=line 77, L2=line 94; lazy sys.path.insert pattern in cmd handlers)
    agent/prepare.py (218 lines, full file — patch sites L3=line 77, L4=line 92, L5=line 114; sys.path.insert at line 51)
    src/pipeline.py (121 lines, full file — patch sites L6=line 34, L7=line 46)
    src/download.py (88 lines, full file — patch site L8=line 20)
    agent/douyin_downloader.py (full file — patch site L9=line 150)
    tests/regression/regression-check.md (committed by plan 01-01 — the runbook this plan must not break; verifier re-runs the runbook concept by re-loading baselines)
  </read_first>
  <action>
Apply 9 mechanical patches. Each patch is a one-line replacement plus an import line at the appropriate scope. The 9 sites are EXACTLY (verified by RESEARCH grep — note CONTEXT.md said "6 sites" but research found 9; trust research):

| # | File | Line | OLD | NEW |
|---|------|------|-----|-----|
| L1 | `agent/tools.py` | 77 | `segs_data = json.loads(segs_file.read_text(encoding="utf-8"))` | `segs_data = load_segs(segs_file)` |
| L2 | `agent/tools.py` | 94 | `segs = json.loads(Path(args.segs_json).read_text(encoding="utf-8"))` | `segs = load_segs(args.segs_json)` |
| L3 | `agent/prepare.py` | 77 | `meta = json.loads((work_dir / "meta.json").read_text(encoding="utf-8"))` | `meta = load_meta(work_dir / "meta.json")` |
| L4 | `agent/prepare.py` | 92 | `segs_data = json.loads(segs_cache.read_text(encoding="utf-8"))` | `segs_data = load_segs(segs_cache)` |
| L5 | `agent/prepare.py` | 114 | `paras_data = json.loads(para_cache.read_text(encoding="utf-8"))` | `paras_data = load_paragraphs(para_cache)` |
| L6 | `src/pipeline.py` | 34 | `meta = json.loads((work_dir / "meta.json").read_text(encoding="utf-8"))` | `meta = load_meta(work_dir / "meta.json")` |
| L7 | `src/pipeline.py` | 46 | `data = json.loads(segs_cache.read_text(encoding="utf-8"))` | `data = load_segs(segs_cache)` |
| L8 | `src/download.py` | 20 | `meta = json.loads(meta_cache.read_text(encoding="utf-8"))` | `meta = load_meta(meta_cache)` |
| L9 | `agent/douyin_downloader.py` | 150 | `meta = json.loads(meta_cache.read_text(encoding="utf-8"))` | `meta = load_meta(meta_cache)` |

**Important:** RESEARCH §"Where agent/io.py should be imported" gives two acceptable import styles. Choose **per-file** to minimize delta:

- **agent/tools.py** — handler-scope lazy import. The two existing handlers (`cmd_transcribe` at line ~63, `cmd_aggregate` at line 89) already use the lazy `sys.path.insert(...)` + `from src.asr import ...` pattern at lines 64-65 and 91-92. Add `from agent.io import load_segs` immediately after the existing `from src.asr import ...` line in each handler. This keeps the convention.

- **agent/prepare.py** — handler-scope lazy import. After the existing `sys.path.insert(0, ...)` at line ~51 and the subsequent imports, add `from agent.io import load_meta, load_segs, load_paragraphs` once near the top of `main()` (covers all three patch sites L3 / L4 / L5).

- **src/pipeline.py** — top-of-file import. Add `from agent.io import load_meta, load_segs` to the local-imports block (after the existing `from .download import ...` / `from .asr import ...` lines). The `agent.io` module imports nothing from `src/`, so no circular risk. NOTE: If `src/` historically uses relative imports (`from .download`), this requires `agent` on `sys.path`. Verify that `python -m src.cli` ALREADY puts repo-root on `sys.path` — yes, per `src/cli.py:1-30` (Python module resolution from repo root). Use absolute import `from agent.io import ...` (matches the absolute-import style in agent/ → src/ direction documented in CONVENTIONS.md §"Imports & Module Boundaries: src/ uses relative, agent/ uses absolute"; cross-pkg src→agent is fine as absolute).

- **src/download.py** — top-of-file import. Add `from agent.io import load_meta` to the local imports.

- **agent/douyin_downloader.py** — top-of-file import. Add `from agent.io import load_meta` to the existing imports block.

**Per D-03 hard rule, do NOT touch any file under `output/<slug>/`** during this task. Verify with `git status` before commit (see verify block).

**Per D-04**, the segs.json / paragraphs.json shape stays as a top-level list — the new `load_segs` / `load_paragraphs` return that list unchanged. Do NOT add wrapping logic anywhere.

**Per CONVENTIONS.md §"I/O & Path Conventions"**, the existing JSON-write idiom (`path.write_text(json.dumps(...), encoding="utf-8")`) stays untouched — this plan only patches LOADS, not WRITES. (Per A5 in RESEARCH: writing `schema_version: 1` into new artifacts is a Phase 2 RES-08 decision, not Phase 1.)

**Per Pitfall 1 in RESEARCH**, do not over-engineer: do not add try/except around the new helper calls; the helpers raise ValueError on bad input which is the project's fail-fast convention.

After patching, do one read-pass of each modified file to verify:
1. The exact OLD line is gone (replaced).
2. The NEW line is present at approximately the original line number (line numbers may shift by 1-2 due to added imports).
3. The import statement is present once per file.
4. No `json.loads(... .read_text(...))` remains for any of the three artifact paths in the patched sites (other unrelated json.loads calls — e.g., `frame_store.json` loading in `agent/frame_store.py:88` — stay untouched per RESEARCH §"Out of scope for this loader").
  </action>
  <verify>
    <automated>
# Imports added (one per file at the appropriate scope)
grep -q 'from agent.io import' agent/tools.py && echo "OK: agent/tools.py imports agent.io"
grep -q 'from agent.io import' agent/prepare.py && echo "OK: agent/prepare.py imports agent.io"
grep -q 'from agent.io import' src/pipeline.py && echo "OK: src/pipeline.py imports agent.io"
grep -q 'from agent.io import' src/download.py && echo "OK: src/download.py imports agent.io"
grep -q 'from agent.io import' agent/douyin_downloader.py && echo "OK: agent/douyin_downloader.py imports agent.io"

# OLD inline patterns removed at the 9 enumerated sites:
# Specifically the patterns that loaded meta.json / segs.json / paragraphs.json by inline json.loads(...read_text...)

# In agent/tools.py, the two segs.json loads are gone:
! grep -E 'segs_data\s*=\s*json\.loads\(segs_file\.read_text' agent/tools.py && echo "OK: L1 patched"
! grep -E 'segs\s*=\s*json\.loads\(Path\(args\.segs_json\)\.read_text' agent/tools.py && echo "OK: L2 patched"

# In agent/prepare.py, the three loads are gone:
! grep -E 'meta\s*=\s*json\.loads\(\(work_dir\s*/\s*"meta\.json"\)\.read_text' agent/prepare.py && echo "OK: L3 patched"
! grep -E 'segs_data\s*=\s*json\.loads\(segs_cache\.read_text' agent/prepare.py && echo "OK: L4 patched"
! grep -E 'paras_data\s*=\s*json\.loads\(para_cache\.read_text' agent/prepare.py && echo "OK: L5 patched"

# In src/pipeline.py, the two loads are gone:
! grep -E 'meta\s*=\s*json\.loads\(\(work_dir\s*/\s*"meta\.json"\)\.read_text' src/pipeline.py && echo "OK: L6 patched"
! grep -E 'data\s*=\s*json\.loads\(segs_cache\.read_text' src/pipeline.py && echo "OK: L7 patched"

# In src/download.py and agent/douyin_downloader.py, the meta loads are gone:
! grep -E 'meta\s*=\s*json\.loads\(meta_cache\.read_text' src/download.py && echo "OK: L8 patched"
! grep -E 'meta\s*=\s*json\.loads\(meta_cache\.read_text' agent/douyin_downloader.py && echo "OK: L9 patched"

# NEW helper calls present at each replaced site (count must match 9 across the 5 files)
grep -E 'load_(meta|segs|paragraphs)\(' agent/tools.py | wc -l   # expect 2
grep -E 'load_(meta|segs|paragraphs)\(' agent/prepare.py | wc -l # expect 3
grep -E 'load_(meta|segs|paragraphs)\(' src/pipeline.py | wc -l  # expect 2
grep -E 'load_(meta|segs|paragraphs)\(' src/download.py | wc -l  # expect 1
grep -E 'load_(meta|segs|paragraphs)\(' agent/douyin_downloader.py | wc -l  # expect 1
TOTAL=$(grep -rE 'load_(meta|segs|paragraphs)\(' agent/tools.py agent/prepare.py src/pipeline.py src/download.py agent/douyin_downloader.py | wc -l)
echo "Total helper-call sites: $TOTAL (expect 9)"
test "$TOTAL" -eq 9 && echo "OK: 9 patch sites converted"

# Smoke test: import every patched module to confirm nothing broke at import time
python -c "
import sys; sys.path.insert(0, '.')
import agent.io
import agent.tools
import agent.prepare
import src.pipeline
import src.download
import agent.douyin_downloader
print('OK: all 5 patched modules import cleanly')
"

# End-to-end sanity: cmd_aggregate's loader path actually works on a real baseline
# (Re-loading the BV1C9QCBdE1U baseline through the patched code)
python -c "
import sys; sys.path.insert(0, '.')
from agent.io import load_segs, load_meta, load_paragraphs
m = load_meta('tests/regression/BV1C9QCBdE1U/meta.json')
s = load_segs('tests/regression/BV1C9QCBdE1U/segs.json')
p = load_paragraphs('tests/regression/BV1C9QCBdE1U/paragraphs.json')
print(f'OK: BV1C9QCBdE1U loads — {len(m)} meta keys, {len(s)} segs, {len(p)} paragraphs')
"

# D-03 hard rule: no file under output/<slug>/ was modified
# (assumes the working tree was clean for output/ before this plan started; verify nothing in output/ shows in `git status`)
git status --porcelain output/ | grep -v '^??' | wc -l   # expect 0 modified/staged files
test $(git status --porcelain output/ 2>/dev/null | grep -E '^(M|A|D|R)' | wc -l) -eq 0 && echo "OK: no output/ files modified"

# .gitignore unchanged
git diff --quiet -- .gitignore && echo "OK: .gitignore unchanged"

# Patch count by line-of-diff sanity (rough — should be small per file)
git diff --stat agent/tools.py agent/prepare.py src/pipeline.py src/download.py agent/douyin_downloader.py 2>/dev/null
    </automated>
  </verify>
  <done>
- All 5 modified .py files contain `from agent.io import ...` once at the appropriate scope (handler-scope for `agent/tools.py` and `agent/prepare.py`; top-of-file for the others).
- All 9 enumerated load sites have been replaced — the `json.loads(... .read_text(...))` inline pattern is gone for `meta.json` / `segs.json` / `paragraphs.json` reads at those exact sites; helper calls are present.
- Total helper-call count across the 5 patched files = 9 (verified by grep).
- All 5 modules import cleanly via `python -c "import ..."` (no runtime breakage from the patches).
- The 3 baselines committed by plan 01-01 round-trip through the new helpers and return non-empty data (sanity smoke test).
- No file under `output/<slug>/` was modified (D-03 hard rule).
- `.gitignore` is unchanged.
- Out-of-scope load sites (`agent/frame_store.py:88`, `src/pipeline.py:68,83`, `src/summarize.py:647`, `src/budget.py:81`) are NOT patched (per RESEARCH §"Out of scope for this loader").
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → agent/io.py loader | Loaders parse untrusted JSON files from `output/<slug>/`; bad input must produce a clean error, not a silent type confusion. |
| agent/io.py → 5 caller modules | Caller assumes loader returns the documented type; loader's `isinstance` checks enforce the contract. |
| existing `output/<slug>/` files | Read-only boundary — D-03 forbids any modification to legacy archives. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T (Tampering) | Malformed/corrupted artifact (e.g., truncated JSON, wrong shape from a partial write) (LOW severity) | mitigate | Each loader does `isinstance(obj, ...)` and raises ValueError naming path + actual type. Fail-fast per CONVENTIONS.md §"Error Handling"; matches existing project convention of bubbling exceptions to the CLI boundary. Verified by Task 1 behavior tests that pass `[]` to load_meta and `{}` to load_segs. |
| T-01-02-02 | T (Tampering) | A patch accidentally modifies a file under `output/<slug>/` (LOW severity but high-impact if missed — D-03 hard rule) | mitigate | Task 2's `<verify>` runs `git status --porcelain output/` and asserts zero modifications to `output/`. The patches by design only touch the 5 listed .py files. |
| T-01-02-03 | D (Denial of Service) / E (Elevation of Privilege) | New module imports something unsafe (LOW severity) | mitigate | Task 1 `<verify>` runs `grep -E '^(import\|from)' agent/io.py \| grep -vE '^(from __future__\|import json\|from pathlib)'` and asserts empty — stdlib-only. No 3rd-party code can hide here. |
| T-01-02-04 | I (Information Disclosure) | docs/schema-versions.md leaks something private (LOW severity) | accept | The doc only documents field names and types verified from already-public artifact shapes (no values, no titles, no URLs). The doc is essentially identical to RESEARCH §"docs/schema-versions.md skeleton" which is a template. |
| T-01-02-05 | T (Tampering) | A loader silently accepts a v2-shaped artifact and returns wrong-shaped data downstream (LOW severity, hypothetical for Phase 1 since no v2 exists) | accept | Per RESEARCH §"Open Question 4 recommendation: do NOT add speculative version-mismatch warnings in Phase 1". The Phase 2+ that introduces v2 owns this. |
</threat_model>

<verification>
After both tasks complete:

1. **agent/io.py contract:** `python -c "from agent.io import load_meta, load_segs, load_paragraphs, SCHEMA_VERSION; assert SCHEMA_VERSION == 1"` succeeds.

2. **Stdlib-only:** `grep -E '^(import|from)' agent/io.py` produces only `from __future__ import annotations`, `import json`, `from pathlib import Path`.

3. **All 9 patch sites converted:** `grep -rE 'load_(meta|segs|paragraphs)\(' agent/tools.py agent/prepare.py src/pipeline.py src/download.py agent/douyin_downloader.py | wc -l` returns 9.

4. **D-03 enforced:** `git status --porcelain output/` returns empty (no output/<slug>/ modifications).

5. **K3 enforced:** All 3 baselines committed by plan 01-01 still load successfully through the new code path. (The smoke test in Task 1 verify and Task 2 verify both load them.)

6. **docs/schema-versions.md is grep-discoverable:** `grep -r "schema_version" docs/` returns the doc.

7. **All 5 patched modules import without error:** import smoke test in Task 2 verify block.

8. **No `agent/io.py` over-engineering:** `wc -l agent/io.py` ≤ 80; no class definitions (`grep -E '^class ' agent/io.py` returns empty).
</verification>

<success_criteria>
**Maps to ROADMAP Phase 1 Success Criterion #2:**
> "Loaders for meta.json / segs.json / paragraphs.json accept files lacking schema_version and treat them as v1 (archived videos remain re-runnable unchanged)"

This plan delivers that criterion fully:
- New `agent/io.py` provides the three loaders with v1-default semantics.
- All 9 load sites for those three artifact types now go through the helpers (zero behavior change for v1 input — verified against the 3 plan-01-01 baselines).
- 17 archived `output/<slug>/` directories remain unchanged on disk; their JSONs round-trip through the new loaders cleanly.
- `docs/schema-versions.md` retroactively documents the v1 field set so v2 work has an explicit migration source.

**Plan-level pass:** All `<verify>` blocks return success; `<done>` criteria all true; `agent/io.py` ≤ 80 lines and stdlib-only.
</success_criteria>

<output>
After completion, create `.planning/phases/01-preflight-regression-baseline/01-02-SUMMARY.md` documenting:
- agent/io.py created (line count, stdlib-only confirmation)
- docs/schema-versions.md created (field-set summary)
- 9 patch sites converted (one bullet per site with file:old-line-number)
- Confirmation: 3 baselines from plan 01-01 round-trip through new helpers
- Confirmation: no output/<slug>/ files modified (D-03 enforced)
- Note: writing schema_version into NEW artifacts deferred to Phase 2 (RES-08) per A5
- Note: any future v2 migration begins by editing agent/io.py only
</output>
