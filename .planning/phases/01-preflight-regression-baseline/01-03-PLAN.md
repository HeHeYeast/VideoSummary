---
phase: 01-preflight-regression-baseline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/regression/encoding-audit.md
  - CLAUDE.md
autonomous: true
requirements: [PRE-04, PRE-05]
must_haves:
  truths:
    - "tests/regression/encoding-audit.md exists, dated, and lists the three exact grep commands plus their current outputs verbatim"
    - "Audit covers all 8 .py files in agent/ + all 9 .py files in src/ (D-14 + D-16)"
    - "Audit classifies all 4 bare open() calls (CONTEXT.md missed src/frames.py:53; research caught it; the doc must list all four)"
    - "Audit explicitly states: 0 text-I/O sites without explicit encoding=utf-8"
    - "CLAUDE.md contains a new '## Windows zh-CN 终端设置' section between the 抖音支持 section and the 环境变量 section, with literal commands chcp 65001 and PYTHONUTF8=1"
    - "The new CLAUDE.md section is phrased as '推荐 (recommended), not required' per D-18; the existing ensure_ascii=True fallback at agent/tools.py:59 is preserved"
    - "Zero code edits required by this plan — D-14 verified that 100% of text-I/O is already encoding-correct"
  artifacts:
    - path: "tests/regression/encoding-audit.md"
      provides: "PRE-04 audit-pass evidence: grep commands + outputs + classification of all 4 bare open() calls + scope statement"
      contains: "rg -n, agent/, src/, encoding=, open("
    - path: "CLAUDE.md"
      provides: "New 'Windows zh-CN 终端设置 (推荐)' section with chcp 65001 + PYTHONUTF8=1 + verification probe"
      contains: "chcp 65001, PYTHONUTF8=1, Windows zh-CN, 推荐"
  key_links:
    - from: "CLAUDE.md (new section)"
      to: "agent/tools.py:59 ensure_ascii=True fallback"
      via: "explicit reference '兜底保留不动' (preserved per D-18)"
      pattern: "ensure_ascii"
    - from: "tests/regression/encoding-audit.md"
      to: "tests/regression/regression-check.md"
      via: "The runbook's stub section '## Encoding Audit (PRE-04)' references this file"
      pattern: "encoding-audit\\.md"
    - from: "tests/regression/encoding-audit.md"
      to: "agent/douyin_downloader.py:194 / agent/embed.py:79 / agent/frames_v2.py:74 / src/frames.py:53"
      via: "verbatim citation of the four bare-open lines with classifications"
      pattern: "src/frames\\.py:53"
---

<objective>
Two non-code deliverables that finish the Phase 1 preflight: (a) an
encoding-audit-pass evidence file proving the codebase already meets PRE-04
(verified at research time — 100% compliance, no edits needed), and (b) a
≤30-line addition to CLAUDE.md documenting `chcp 65001` + `PYTHONUTF8=1` as
the recommended Windows zh-CN setup so subsequent phases inherit a clean
encoding baseline.

This plan delivers PRE-04 (encoding audit) and PRE-05 (Windows zh-CN docs).
Both are documentation-only — zero `.py` files are touched. The audit is
read-only evidence per D-15: "audit pass" means "we ran the grep, saved the
output, classified each finding." Per D-18, the CLAUDE.md addition is
phrased as 「推荐」not 「必需」 so the existing `ensure_ascii=True` fallback at
`agent/tools.py:59` keeps doing its job for users who don't set the codepage.

Purpose: PITFALLS §U3 (Windows zh-CN encoding/proxy/locale) compounds with
P3.1 (YouTube GFW) and P4.1 (Chinese filenames in subprocess args). Phase 3
will hit those head-on; this phase plants the documentation anchor so when
Phase 3 lands, the operator's environment is already prepared. Per D-15 the
audit-pass file also serves as an anchor for any future phase that touches
I/O — they re-run the three grep commands and append a fresh date stamp.

Output:
- `tests/regression/encoding-audit.md` (~60-100 lines, dated audit evidence)
- `CLAUDE.md` updated with one new section (~30 lines added)

This plan is in **Wave 1** (parallel with 01-01) because it touches zero `.py`
files and zero output/<slug>/ files; it only adds a new doc and edits CLAUDE.md.
The two file sets do not overlap with 01-01 (which only writes under
`tests/regression/<slug>/` and `tests/regression/regression-check.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-preflight-regression-baseline/01-CONTEXT.md
@.planning/phases/01-preflight-regression-baseline/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@agent/tools.py
@agent/douyin_downloader.py
@agent/embed.py
@agent/frames_v2.py
@src/frames.py

<interfaces>
<!-- Existing CLAUDE.md insertion-point context (verified 2026-04-30 by RESEARCH).
     Executor must Read CLAUDE.md to confirm line numbers haven't drifted before
     applying the Edit. -->

CLAUDE.md context around the insertion point (verified):
- Line 39: 注意：抖音 cookies 每几天失效，失败时重新导出。yt-dlp 的 douyin extractor 长期 broken（不支持 a_bogus），所以必须走 vendor crawler。
- Line 40: (blank)
- Line 41: ## 环境变量（.env）

Insert the new `## Windows zh-CN 终端设置（推荐）` heading + body BETWEEN
the blank line ending 抖音支持 (line 40) and the `## 环境变量（.env）` heading
(line 41). The Edit anchor for the insertion is the literal `## 环境变量（.env）`
heading — the new section is inserted IMMEDIATELY before it.

Existing fallback that must remain untouched (referenced by the new section):
```python
# agent/tools.py:58-59
# 避免打印含 emoji 的 title 炸 gbk 终端
print(json.dumps(meta, ensure_ascii=True, indent=2))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run encoding audit and write tests/regression/encoding-audit.md (PRE-04)</name>
  <files>tests/regression/encoding-audit.md</files>
  <read_first>
    .planning/phases/01-preflight-regression-baseline/01-CONTEXT.md (D-14 — agent/ + src/ scope; D-15 — audit-pass evidence file required even though work=0; D-16 — include v2 modules: frames_v2.py / pass1_classify.py / embed.py / frame_store.py / prepare.py)
    .planning/phases/01-preflight-regression-baseline/01-RESEARCH.md (§"Encoding Audit — Current State" — verified state including the FOURTH bare-open at src/frames.py:53 that CONTEXT.md missed; §"Encoding Audit Grep Commands" — three exact reproducible commands; §"Audit Pass Evidence Format" — full doc skeleton; §"Pitfall 3" — audit must be targeted not lazy; §"Pitfall 6" — distinguish encoding correctness from vendor-mutation hygiene)
    .planning/codebase/CONVENTIONS.md (§"I/O & Path Conventions" — encoding="utf-8" required, json.load(open(...)) forbidden)
    .planning/codebase/CONCERNS.md (§1.4 console-encoding workaround context, §8.2 Windows-specific assumptions)
    agent/douyin_downloader.py (line 194 — `with open(video_path, "wb") as f:` — binary write; correct)
    agent/embed.py (line 79 — `img = PILImage.open(p).convert("RGB")` — PIL handles encoding)
    agent/frames_v2.py (line 74 — `h = imagehash.phash(Image.open(f.path))` — PIL handles encoding)
    src/frames.py (line 53 — `h = imagehash.phash(Image.open(f.path))` — PIL handles encoding; this is the FOURTH bare-open that RESEARCH found and CONTEXT D-14 missed; documenting all four matters for audit completeness)
  </read_first>
  <action>
**Step A — Re-run the three grep commands and capture output verbatim.** Per RESEARCH §"Encoding Audit Grep Commands", run these three commands from the repo root and save their output exactly as printed:

Command 1 (every bare `open()` call):
```bash
rg -n '\bopen\s*\(' agent/ src/ --type py
```
Expected output (verified 2026-04-30 by research):
```
agent/douyin_downloader.py:194:        with open(video_path, "wb") as f:
agent/embed.py:79:                img = PILImage.open(p).convert("RGB")
agent/frames_v2.py:74:            h = imagehash.phash(Image.open(f.path))
src/frames.py:53:            h = imagehash.phash(Image.open(f.path))
```

Command 2 (text-I/O without explicit encoding — should be empty):
```bash
rg -n '(read_text|write_text)\s*\((?![^)]*encoding\s*=)' agent/ src/ --type py
```
Expected: empty output (zero matches).

Command 3 (forbidden idiom check):
```bash
rg -n 'json\.load\s*\(' agent/ src/ --type py
```
Expected: only docstring/comment matches if any (verify each non-empty match is non-functional).

If any output differs from the expected (e.g., a contributor added a new `read_text` without encoding since 2026-04-30), the audit must (a) record the actual output AND (b) flag the missing-encoding sites for fixing. **For Phase 1 with the current codebase, all three commands produce the verified expected outputs — no fix needed.** If a difference is found, halt the task and surface the diff to the user as a checkpoint condition; do not silently fix code in this PRE-04-evidence task (that would conflate audit with remediation; remediation if needed becomes a follow-up task).

**Step B — Write `tests/regression/encoding-audit.md`** using the structure from RESEARCH §"Audit Pass Evidence Format". The file MUST contain (verifier checks for these literal strings):

- A heading `# Encoding Audit (PRE-04)` (or `## Encoding Audit (PRE-04)` — verifier accepts either since the runbook's stub heading at the bottom of `regression-check.md` cross-references this file).
- An `**Audited:** YYYY-MM-DD` line with today's date.
- Scope statement: `**Scope:** agent/ + src/ (.py files only, vendor/ excluded)` and a one-line rationale ("Vendor encoding hygiene tracked in CONCERNS.md §3.1, out of D-14 scope.")
- A `**Result:** 100% compliant` line.
- The three grep commands inside a fenced bash block, identical to Step A.
- The four bare-open findings in a Markdown table with columns: Site / Mode / Verdict — listing ALL FOUR sites (`agent/douyin_downloader.py:194` binary write, `agent/embed.py:79` PIL, `agent/frames_v2.py:74` PIL, `src/frames.py:53` PIL — research found this fourth one that CONTEXT.md D-14 missed).
- A line stating `**Text I/O sites without encoding:** 0`.
- A line stating `**json.load(open(...)) calls:** 0` with a note "(the codebase exclusively uses `json.loads(path.read_text(encoding=\"utf-8\"))` per CONVENTIONS.md)".
- A "Re-running" subsection: tells the operator that this audit is read-only, anyone can re-run the three commands above, and future phase merges that touch `agent/` or `src/` should re-run the audit and append a fresh date stamp.
- An explicit cross-reference to `tests/regression/regression-check.md` so the runbook's stub heading "## Encoding Audit (PRE-04)" links here.

**Per Pitfall 3 in RESEARCH**: do NOT use a lazy `grep -r encoding utf-8 .` that catches strings inside Markdown — the three commands above are the targeted versions that catch actual `open()` / `read_text` / `write_text` / `json.load` calls.

**Per Pitfall 6 in RESEARCH**: do NOT conflate "is this encoding correct?" with "should this code exist?" — `agent/douyin_downloader.py:60` `_CONFIG.write_text(content, encoding="utf-8")` is correct encoding (it has `encoding="utf-8"`); the vendor-mutation concern is a CONCERNS §2.2 issue, out of PRE-04 scope. The audit doc explicitly notes this.

**D-16 inclusion check**: when classifying the bare opens, the audit must explicitly cover the v2 modules (`frames_v2.py`, `pass1_classify.py`, `embed.py`, `frame_store.py`, `prepare.py`) — three of the four bare-opens come from this v2 group. The doc states: "Per D-16, audit scope includes orphaned v2 modules even though they are not on the main ¥0 path."

Length target: 60-100 lines. Self-contained: a future contributor reading only this file knows what was audited, how, when, what was found, and how to re-run.
  </action>
  <verify>
    <automated>
# File exists and non-empty
test -s tests/regression/encoding-audit.md && echo "OK: encoding-audit.md exists"

# All four bare-open sites named (verifier checks the FOURTH that CONTEXT missed)
grep -q 'agent/douyin_downloader.py:194' tests/regression/encoding-audit.md && echo "OK: site 1 cited"
grep -q 'agent/embed.py:79' tests/regression/encoding-audit.md && echo "OK: site 2 cited"
grep -q 'agent/frames_v2.py:74' tests/regression/encoding-audit.md && echo "OK: site 3 cited"
grep -q 'src/frames.py:53' tests/regression/encoding-audit.md && echo "OK: site 4 (the one CONTEXT missed) cited"

# The three grep commands present (verbatim)
grep -q 'rg -n' tests/regression/encoding-audit.md && echo "OK: ripgrep commands present"
grep -q 'open\\\\s\*' tests/regression/encoding-audit.md && echo "OK: command 1 (open() pattern) present" || \
  grep -qE 'rg .+open' tests/regression/encoding-audit.md && echo "OK: command 1 form present"
grep -q 'read_text\|write_text' tests/regression/encoding-audit.md && echo "OK: command 2 (read_text/write_text) present"
grep -q 'json\.load' tests/regression/encoding-audit.md && echo "OK: command 3 (json.load) present"

# Scope and result statements
grep -qE 'agent/ \+ src/|agent/ and src/' tests/regression/encoding-audit.md && echo "OK: scope statement present"
grep -qE '100% compliant|100%-compliant|fully compliant' tests/regression/encoding-audit.md && echo "OK: result statement present"
grep -qiE 'vendor/.*excluded|excludes vendor|excluded.*vendor' tests/regression/encoding-audit.md && echo "OK: vendor exclusion noted"
grep -qE 'D-14|D-16' tests/regression/encoding-audit.md && echo "OK: scope decisions referenced"

# 0-finding statements
grep -q 'Text I/O sites without encoding' tests/regression/encoding-audit.md && \
  grep -qE 'without encoding:?\*?\*?\s*0|encoding=.*0|^.*0.*$' tests/regression/encoding-audit.md && \
  echo "OK: zero-finding text-I/O statement present"

# Date stamp present
grep -qE '\*\*Audited:\*\*\s*[0-9]{4}-[0-9]{2}-[0-9]{2}|Audited:?\s*[0-9]{4}' tests/regression/encoding-audit.md && \
  echo "OK: date stamp present"

# Cross-reference to regression-check.md (the runbook's stub section)
grep -q 'regression-check.md' tests/regression/encoding-audit.md && echo "OK: cross-link to runbook"

# Now actually re-run the three commands and confirm they still match research
# Command 1: should produce exactly 4 lines
COUNT1=$(grep -rcE '\bopen\s*\(' --include='*.py' agent/ src/ 2>/dev/null | awk -F: '{s+=$NF} END {print s+0}')
echo "Command 1 (bare open) live count: $COUNT1 (expected 4)"
# Note: grep -c counts per file then sum. Using rg if available is more direct.
test "$COUNT1" -ge 4 && echo "OK: at least 4 bare opens (matches research expectation)"

# Length sanity
LINES=$(wc -l < tests/regression/encoding-audit.md)
echo "encoding-audit.md length: $LINES lines (target 60-150)"
test "$LINES" -ge 30 && test "$LINES" -le 200 && echo "OK: reasonable length"
    </automated>
  </verify>
  <done>
- `tests/regression/encoding-audit.md` exists with a date stamp and 100%-compliant verdict.
- The three grep commands from RESEARCH §"Encoding Audit Grep Commands" are pasted verbatim into the doc.
- All FOUR bare-open sites are listed with their classification (the fourth, `src/frames.py:53`, is the one CONTEXT.md D-14 missed; research caught it; the audit-pass doc lists all four for completeness).
- Scope statement explicitly cites `agent/` + `src/` per D-14, includes v2 modules per D-16, excludes `vendor/` (with one-line rationale pointing at CONCERNS §3.1).
- Zero-finding statements: `Text I/O sites without encoding: 0`; `json.load(open(...)) calls: 0`.
- Cross-reference to `tests/regression/regression-check.md` so the runbook's stub heading links here.
- Re-running the three grep commands today still matches the research-verified expectation (4 bare opens, classified the same way).
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert "Windows zh-CN 终端设置" section into CLAUDE.md (PRE-05)</name>
  <files>CLAUDE.md</files>
  <read_first>
    .planning/phases/01-preflight-regression-baseline/01-CONTEXT.md (D-17 — insertion point near 「环境变量」section, two recommended commands; D-18 — phrased as 推荐 not 必需; ensure_ascii=True fallback at agent/tools.py:59 stays untouched)
    .planning/phases/01-preflight-regression-baseline/01-RESEARCH.md (§"CLAUDE.md Insertion — Exact Wording" — verbatim ≤30-line Markdown block; §"Verification of insertion point" — exact line numbers 39/40/41; §"Wording rationale per D-18")
    CLAUDE.md (full file — verify lines 39, 40, 41 still match RESEARCH's verified snapshot at the time of execution; if they have drifted, the Edit anchor `## 环境变量（.env）` is still uniquely identifiable and remains the correct insertion point)
    agent/tools.py (line 59: confirm `ensure_ascii=True` is still present — the new CLAUDE.md section references this line as the preserved fallback)
    .planning/research/PITFALLS.md (§U3 — Windows zh-CN encoding/proxy/locale rationale that this section codifies)
    .planning/codebase/CONCERNS.md (§1.4, §8.2 — context for why this section matters)
  </read_first>
  <action>
Apply a single Edit to `CLAUDE.md`. Insert the new section verbatim from RESEARCH §"CLAUDE.md Insertion — Exact Wording (≤ 30 lines)". The insertion uses the Edit tool with `## 环境变量（.env）` as the anchor — the new section is inserted IMMEDIATELY ABOVE that heading, with one blank line above and one blank line below the new section (so the document reads: "...抖音 extractor broken." [blank] [new section] [blank] "## 环境变量（.env）").

Use the Edit tool's `<old_string>` = `## 环境变量（.env）` (unique anchor; verified by RESEARCH that the heading appears once at line 41) and `<new_string>` = the verbatim Markdown below + an extra blank line + the original heading:

```markdown
## Windows zh-CN 终端设置（推荐）

中文 Windows 默认 GBK 终端会在打印含 emoji / 非 ASCII 的视频标题时炸出
`UnicodeEncodeError`。代码已在 `agent/tools.py:59` 留了 `ensure_ascii=True`
兜底，老路径不设置也能跑；但**推荐**把终端 + 解释器 都切成 UTF-8，让本仓库
所有路径行为一致：

1. **每个 terminal session 跑一次**（zh-CN cmd / PowerShell）：
   ```bash
   chcp 65001
   ```

2. **一次性设置 `PYTHONUTF8=1` 环境变量**（Windows 10+ 生效）：
   - PowerShell 永久：`[Environment]::SetEnvironmentVariable("PYTHONUTF8", "1", "User")`
   - 或在「系统属性 → 环境变量」面板加 `PYTHONUTF8=1`
   - 设好后重启 terminal 验证：`python -c "import sys; print(sys.flags.utf8_mode)"` 应输出 `1`

设了之后 `print(meta)`、文件 I/O、子进程 stderr 全程 UTF-8；老的 `ensure_ascii`
兜底保留不动，没设 codepage 的环境也能正常工作。

> **历史背景：** 项目里所有 `read_text` / `write_text` 都已显式 `encoding="utf-8"`
> （Phase 1 PRE-04 审计通过），这一节是给「希望子进程 stderr 也直接读到中文」
> 的开发者准备的、可选的一致化设置。

## 环境变量（.env）
```

**Per D-18**, the section is phrased as 「推荐」not 「必需」 — explicit in the heading `（推荐）` and the body's "推荐把终端 + 解释器 都切成 UTF-8" / "老路径不设置也能跑" / "兜底保留不动，没设 codepage 的环境也能正常工作".

**Per D-17**, the two commands required are present: `chcp 65001` (codepage) and `PYTHONUTF8=1` (env var).

**Per RESEARCH §"Wording rationale per D-18"**, the section also:
- Explicitly cites `agent/tools.py:59` (the existing fallback that's preserved).
- Foreshadows Phase 3's YouTube + ffmpeg-on-CJK paths via the closing 「希望子进程 stderr 也直接读到中文」 hint.
- Provides a one-line verification probe (`python -c "import sys; print(sys.flags.utf8_mode)"`) so the operator can confirm the setting took effect.

**Do NOT** modify `agent/tools.py:59` — the `ensure_ascii=True` fallback is preserved per D-18. This task only edits CLAUDE.md.

**Do NOT** modify any other section of CLAUDE.md — Phase 1 is documentation-additive only. The /summarize-video workflow (lines 47-148) and the 质量红线 (lines 151-157) stay untouched.
  </action>
  <verify>
    <automated>
# CLAUDE.md still exists and is non-empty
test -s CLAUDE.md && echo "OK: CLAUDE.md exists"

# All required literal strings present in CLAUDE.md
grep -q 'Windows zh-CN' CLAUDE.md && echo "OK: 'Windows zh-CN' phrase present"
grep -q '终端设置' CLAUDE.md && echo "OK: '终端设置' phrase present"
grep -qE '推荐' CLAUDE.md && echo "OK: 推荐 (recommended) phrasing present"
grep -q 'chcp 65001' CLAUDE.md && echo "OK: chcp 65001 command present"
grep -q 'PYTHONUTF8=1' CLAUDE.md && echo "OK: PYTHONUTF8=1 env var present"

# D-18 fallback preservation reference: must explicitly mention agent/tools.py:59 OR ensure_ascii
grep -qE 'agent/tools\.py:59|ensure_ascii' CLAUDE.md && echo "OK: existing fallback referenced"

# Verification probe present
grep -q 'sys.flags.utf8_mode' CLAUDE.md && echo "OK: UTF-8 mode verification probe present"

# Section ordering: new section appears AFTER the 抖音支持 setup recipe and BEFORE 「环境变量」 — strict ordering check
LINE_DOUYIN=$(grep -n '## 抖音支持' CLAUDE.md | head -1 | cut -d: -f1)
LINE_NEW=$(grep -n '## Windows zh-CN' CLAUDE.md | head -1 | cut -d: -f1)
LINE_ENV=$(grep -n '## 环境变量' CLAUDE.md | head -1 | cut -d: -f1)
echo "Section line numbers: 抖音支持=$LINE_DOUYIN, Windows zh-CN=$LINE_NEW, 环境变量=$LINE_ENV"
test "$LINE_DOUYIN" -lt "$LINE_NEW" && test "$LINE_NEW" -lt "$LINE_ENV" && \
  echo "OK: section order is 抖音支持 < Windows zh-CN < 环境变量"

# /summarize-video workflow section unchanged: confirm Phase 1-8 headings still present and untouched
grep -q '/summarize-video 完整工作流' CLAUDE.md && echo "OK: /summarize-video section heading preserved"
grep -q '### Phase 1: 获取原始数据' CLAUDE.md && echo "OK: Phase 1 heading preserved"
grep -q '### Phase 7: 完整代码 + 输出' CLAUDE.md && echo "OK: Phase 7 heading preserved"
grep -q '## 质量红线' CLAUDE.md && echo "OK: 质量红线 section preserved"

# agent/tools.py:59 was NOT modified (D-18 hard rule)
git diff --quiet -- agent/tools.py && echo "OK: agent/tools.py untouched"
sed -n '59p' agent/tools.py | grep -q 'ensure_ascii=True' && echo "OK: agent/tools.py:59 ensure_ascii=True still in place"

# No other code files were modified by this plan
git diff --quiet -- agent/ src/ && echo "OK: no .py files modified by this plan"

# Length sanity: the addition should be ~30 lines, not a runaway insertion
ADDED_LINES=$(git diff --numstat -- CLAUDE.md | awk '{print $1}')
echo "CLAUDE.md added lines: $ADDED_LINES"
test "$ADDED_LINES" -ge 15 && test "$ADDED_LINES" -le 50 && echo "OK: insertion size in expected range (15-50 added lines)"
    </automated>
  </verify>
  <done>
- CLAUDE.md contains a new `## Windows zh-CN 终端设置（推荐）` heading positioned strictly between the existing `## 抖音支持` setup section and the `## 环境变量（.env）` runtime config section (verified by line-number ordering grep).
- The new section contains both commands `chcp 65001` and `PYTHONUTF8=1`.
- The new section explicitly references `agent/tools.py:59` and/or `ensure_ascii` as the preserved fallback (D-18 compliance).
- The section provides a one-line verification probe (`python -c "import sys; print(sys.flags.utf8_mode)"`).
- `agent/tools.py` is NOT modified by this task (`git diff --quiet -- agent/tools.py` succeeds; line 59's `ensure_ascii=True` is still present).
- No `.py` files anywhere are modified by this plan (`git diff --quiet -- agent/ src/` succeeds).
- The /summarize-video workflow section (Phase 1-8) and 质量红线 are unchanged.
- Insertion size is 15-50 added lines (per `git diff --numstat`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public docs (CLAUDE.md, encoding-audit.md) | Both files become committed and visible. |
| user shell → Python interpreter | The `chcp 65001` / `PYTHONUTF8=1` recommendations affect how subprocess args are passed to ffmpeg / yt-dlp, but the recommendation itself is not executable code in the repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | T (Tampering) | An accidental edit to `agent/tools.py:59` would silently remove the `ensure_ascii=True` fallback (LOW severity but D-18 hard rule) | mitigate | Task 2 `<verify>` runs `git diff --quiet -- agent/tools.py` and `sed -n '59p' agent/tools.py \| grep -q 'ensure_ascii=True'` — both must pass before commit. |
| T-01-03-02 | I (Information Disclosure) | The audit doc names file paths and line numbers — could this leak anything sensitive? (LOW severity) | accept | Paths under `agent/` and `src/` are public source code; line numbers are public. The doc explicitly excludes `vendor/` (which contains 抖音 cookies after runtime patching) per D-14 — vendor is gitignored anyway. |
| T-01-03-03 | T (Tampering) | The audit's grep commands could be subtly miscopied, reporting false 100%-compliance (LOW severity) | mitigate | Task 1 `<verify>` re-runs at least the bare-open grep live and asserts the count is ≥ 4 (matching the four research-verified sites). The doc itself is also a textual artifact that future auditors re-run; it self-validates each invocation. |
| T-01-03-04 | I (Information Disclosure) | CLAUDE.md insertion accidentally drops a clause that would weaken the recommendation (LOW severity) | mitigate | Task 2 `<verify>` greps for the explicit `推荐` term, the `ensure_ascii` reference (preservation flag), and the ordering of section headings — any drift trips a verifier check. |
</threat_model>

<verification>
After both tasks complete:

1. `tests/regression/encoding-audit.md` exists, dated, lists all 4 bare-open sites (including the one CONTEXT.md missed: `src/frames.py:53`), states 100% compliance, and provides reproducible grep commands.

2. The three grep commands in the doc, when re-run live, still match the research-verified expectation (≥ 4 bare opens; zero text-I/O without encoding; zero functional `json.load(open(...))`).

3. CLAUDE.md contains a new `## Windows zh-CN 终端设置（推荐）` section positioned between `## 抖音支持` and `## 环境变量（.env）`, with literal `chcp 65001`, `PYTHONUTF8=1`, and a UTF-8-mode verification probe.

4. CLAUDE.md additions are 15-50 lines (per `git diff --numstat`) — ≤ 30-line target with some slack for blank lines.

5. `agent/tools.py:59` retains `ensure_ascii=True` (D-18 fallback preserved); `git diff --quiet -- agent/tools.py` succeeds.

6. No `.py` files anywhere are modified by this plan.

7. The /summarize-video workflow and 质量红线 sections of CLAUDE.md are untouched.

8. `tests/regression/encoding-audit.md` cross-references `tests/regression/regression-check.md` so the runbook's stub `## Encoding Audit (PRE-04)` section in plan 01-01's deliverable can link here without modification (the link is one-way: runbook → audit).
</verification>

<success_criteria>
**Maps to ROADMAP Phase 1 Success Criteria #3 and #4:**

> #3: "Every `open()` call in `agent/` and `src/` uses explicit `encoding=\"utf-8\"` (verified by audit)"

Delivered by Task 1: `tests/regression/encoding-audit.md` with the three reproducible grep commands and verified findings — all 4 bare opens are PIL/binary (no encoding needed), all 31+ text-I/O sites have explicit encoding, zero `json.load(open(...))` calls. Per D-15, the audit-pass evidence file IS the deliverable since the codebase already meets the criterion.

> #4: "CLAUDE.md documents `chcp 65001` + `PYTHONUTF8=1` as the recommended Windows zh-CN setup so subsequent phases inherit a clean encoding baseline"

Delivered by Task 2: a new ≤ 30-line `## Windows zh-CN 终端设置（推荐）` section in CLAUDE.md containing both commands, a verification probe, and an explicit reference to the preserved `ensure_ascii=True` fallback so legacy environments still work.

**Plan-level pass:** All `<verify>` blocks return success; `<done>` criteria all true; zero `.py` files modified.
</success_criteria>

<output>
After completion, create `.planning/phases/01-preflight-regression-baseline/01-03-SUMMARY.md` documenting:
- tests/regression/encoding-audit.md created (date, 4 bare-opens classified, 0 text-I/O issues found)
- Note: the audit explicitly includes the FOURTH bare-open at src/frames.py:53 that CONTEXT.md D-14 missed; research caught it; doc lists all four
- CLAUDE.md updated: new "Windows zh-CN 终端设置（推荐）" section between 抖音支持 and 环境变量
- Confirmation: agent/tools.py:59 ensure_ascii=True preserved (D-18)
- Confirmation: zero .py files modified by this plan
- Confirmation: zero output/<slug>/ files modified
- Note: this plan ran in Wave 1 parallel with 01-01 (no file-overlap)
</output>
