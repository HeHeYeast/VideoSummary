---
phase: 01-preflight-regression-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/regression/BV132wizyEEB/summary.md
  - tests/regression/BV132wizyEEB/meta.json
  - tests/regression/BV132wizyEEB/segs.json
  - tests/regression/BV132wizyEEB/paragraphs.json
  - tests/regression/BV1C9QCBdE1U/summary.md
  - tests/regression/BV1C9QCBdE1U/meta.json
  - tests/regression/BV1C9QCBdE1U/segs.json
  - tests/regression/BV1C9QCBdE1U/paragraphs.json
  - tests/regression/douyin_trae_ai/summary.md
  - tests/regression/douyin_trae_ai/meta.json
  - tests/regression/douyin_trae_ai/segs.json
  - tests/regression/douyin_trae_ai/paragraphs.json
  - tests/regression/regression-check.md
autonomous: true
requirements: [PRE-01, PRE-02]
must_haves:
  truths:
    - "tests/regression/ directory exists at repo root and is tracked by git (not gitignored)"
    - "Three baseline slugs each have summary.md + meta.json + segs.json + paragraphs.json frozen byte-for-byte from output/<slug>/"
    - "tests/regression/regression-check.md describes the three-step procedure (stage JSONs, --force re-run, manual eyeball-diff) so any future phase can verify regression without writing automation"
    - "Operator can copy-paste the runbook's commands without thinking about substitution"
    - "Pass criterion is Claude's 'no surprise drift' judgment, not byte-equality (per D-09)"
  artifacts:
    - path: "tests/regression/BV132wizyEEB/summary.md"
      provides: "Frozen code/AI workflow baseline"
      min_bytes: 4900
    - path: "tests/regression/BV132wizyEEB/meta.json"
      provides: "Frozen meta with Windows-style backslash separators (verifies loader handles platform variant)"
      min_bytes: 700
    - path: "tests/regression/BV132wizyEEB/segs.json"
      provides: "Frozen ASR output (B站 path)"
      min_bytes: 4100
    - path: "tests/regression/BV132wizyEEB/paragraphs.json"
      provides: "Frozen aggregated paragraphs"
      min_bytes: 2100
    - path: "tests/regression/BV1C9QCBdE1U/summary.md"
      provides: "Frozen Godot/code-dense baseline"
      min_bytes: 10000
    - path: "tests/regression/BV1C9QCBdE1U/meta.json"
      provides: "Frozen meta with forward-slash separators (verifies platform variant heterogeneity)"
      min_bytes: 300
    - path: "tests/regression/BV1C9QCBdE1U/segs.json"
      provides: "Frozen ASR output (B站 path, code-dense)"
      min_bytes: 23000
    - path: "tests/regression/BV1C9QCBdE1U/paragraphs.json"
      provides: "Frozen aggregated paragraphs"
      min_bytes: 16000
    - path: "tests/regression/douyin_trae_ai/summary.md"
      provides: "Frozen AI/UI demo baseline (douyin path)"
      min_bytes: 20000
    - path: "tests/regression/douyin_trae_ai/meta.json"
      provides: "Frozen meta with aweme_id + source: douyin (verifies douyin variant fields)"
      min_bytes: 400
    - path: "tests/regression/douyin_trae_ai/segs.json"
      provides: "Frozen ASR output (douyin path)"
      min_bytes: 12000
    - path: "tests/regression/douyin_trae_ai/paragraphs.json"
      provides: "Frozen aggregated paragraphs"
      min_bytes: 6500
    - path: "tests/regression/regression-check.md"
      provides: "Three-step manual runbook + eyeball-diff prompt template"
      contains: "cp -r tests/regression/, --force, Read tests/regression"
  key_links:
    - from: "tests/regression/regression-check.md"
      to: "tests/regression/<slug>/summary.md (3 slugs)"
      via: "step-3 manual diff prompt"
      pattern: "Read tests/regression/.+/summary\\.md"
    - from: "tests/regression/regression-check.md"
      to: "agent/tools.py --force flag"
      via: "step-2 stage re-run command"
      pattern: "transcribe.+--force"
    - from: ".gitignore"
      to: "tests/regression/"
      via: "verified non-overlap (tests/ not in gitignore)"
      pattern: "tests/"
---

<objective>
Lock the regression safety net for the milestone: commit byte-for-byte frozen
copies of the four self-describing artifacts (summary.md + meta.json + segs.json
+ paragraphs.json) for the three baseline videos, and ship a manual runbook
that any future phase merger can execute in three steps to verify "no surprise
drift" against those baselines.

This plan delivers PRE-01 (frozen baseline directory) and PRE-02 (runbook).
It is the gating artifact for every subsequent phase: per D-10, each later
phase's VERIFICATION.md must record the result of running this runbook on the
three baselines before merging.

Purpose: U1 / U2 in PITFALLS.md are explicitly meta-showstoppers — without
this snapshot, every later phase risks silently breaking the 17-video legacy
queue. This plan turns "we promise backward-compat" into "here is the
reproducible test."

Output:
- 12 baseline artifact files (3 slugs × 4 files each, ~99 KB total)
- 1 runbook with copy-pasteable commands and a verbatim Claude-prompt template
- A repo-tracked `tests/` directory (first time in project history)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-preflight-regression-baseline/01-CONTEXT.md
@.planning/phases/01-preflight-regression-baseline/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@.gitignore
@agent/tools.py

<interfaces>
<!-- The runbook references these existing CLI invocations verbatim. Executor
     should paste them as-is from agent/tools.py. -->

From agent/tools.py (verified line numbers):
```python
# Line 75-81: --force cache bypass pattern (used by runbook step 2)
if segs_file.exists() and not args.force:
    print(f"cached: {segs_file}")
    segs_data = json.loads(segs_file.read_text(encoding="utf-8"))
else:
    segs = transcribe(audio, model_size=args.whisper, language=None)
    segs_data = [asdict(s) for s in segs]
    segs_file.write_text(json.dumps(segs_data, ensure_ascii=False, indent=2), encoding="utf-8")

# Line 58-59: ensure_ascii=True terminal print (preserved per D-18, referenced in runbook context)
# 避免打印含 emoji 的 title 炸 gbk 终端
print(json.dumps(meta, ensure_ascii=True, indent=2))
```

From CLAUDE.md (existing CLI surface — runbook re-uses these unchanged):
```
python -m agent.tools download <url> --out <dir>
python -m agent.tools transcribe <video_path> --out <dir> [--whisper small] [--force]
python -m agent.tools aggregate <segs.json> --out <path> [--gap 1.5]
python -m agent.tools extract_frames <video_path> --out <dir> --fps N --start S --end E
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/regression/&lt;slug&gt;/ skeleton + commit 12 frozen baseline files</name>
  <files>
    tests/regression/BV132wizyEEB/summary.md,
    tests/regression/BV132wizyEEB/meta.json,
    tests/regression/BV132wizyEEB/segs.json,
    tests/regression/BV132wizyEEB/paragraphs.json,
    tests/regression/BV1C9QCBdE1U/summary.md,
    tests/regression/BV1C9QCBdE1U/meta.json,
    tests/regression/BV1C9QCBdE1U/segs.json,
    tests/regression/BV1C9QCBdE1U/paragraphs.json,
    tests/regression/douyin_trae_ai/summary.md,
    tests/regression/douyin_trae_ai/meta.json,
    tests/regression/douyin_trae_ai/segs.json,
    tests/regression/douyin_trae_ai/paragraphs.json
  </files>
  <read_first>
    .planning/phases/01-preflight-regression-baseline/01-CONTEXT.md (D-01..D-13 — slug list, 4-file scope, "do not commit frames/audio/video.mp4/video.info.json")
    .planning/phases/01-preflight-regression-baseline/01-RESEARCH.md (§"Baseline Snapshot Inventory" with byte-size table; §"Pitfall 5 BV132wizyEEB meta.json has Windows backslashes; treating that as a bug" — DO NOT normalize)
    .planning/PROJECT.md (K3 backward-compat hard rule — these files are read-only references for the milestone)
    .gitignore (full file — verify `tests/` is NOT a glob; `output/` IS gitignored, so source files are not in git but the copies under tests/regression/ will be)
    output/BV132wizyEEB/summary.md (source — copy as-is)
    output/BV132wizyEEB/meta.json (source — note: contains "video_path": "output\\BV132wizyEEB\\video.mp4" with double-backslash; DO NOT change)
    output/BV132wizyEEB/segs.json (source — copy as-is, ~4178 bytes)
    output/BV132wizyEEB/paragraphs.json (source — copy as-is, ~2179 bytes)
    output/BV1C9QCBdE1U/summary.md (source — copy as-is, ~10198 bytes)
    output/BV1C9QCBdE1U/meta.json (source — copy as-is, ~341 bytes; uses forward slashes; DO NOT normalize either way)
    output/BV1C9QCBdE1U/segs.json (source — copy as-is, ~23370 bytes)
    output/BV1C9QCBdE1U/paragraphs.json (source — copy as-is, ~16174 bytes)
    output/douyin_trae_ai/summary.md (source — copy as-is, ~20510 bytes)
    output/douyin_trae_ai/meta.json (source — copy as-is, ~487 bytes; contains "aweme_id" and "source": "douyin" — keep both)
    output/douyin_trae_ai/segs.json (source — copy as-is, ~12145 bytes)
    output/douyin_trae_ai/paragraphs.json (source — copy as-is, ~6532 bytes)
  </read_first>
  <action>
Create directory `tests/regression/` at repo root (first time in project history; verified by RESEARCH §"Architecture Patterns post-Phase 1 tree" and confirmed `.gitignore` does not contain a `tests/` glob — line-by-line: `.env / .claude/ / __pycache__/ / *.pyc / *.pyo / .venv/ / venv/ / .DS_Store / output/ / *cookies*.txt / vendor/`).

Then perform 12 byte-for-byte file copies. Use absolute paths from the repo root. Per D-11 / D-12, the destination slugs are exactly `BV132wizyEEB`, `BV1C9QCBdE1U`, `douyin_trae_ai` (NOT `godot_brave` — that slug only had cookies.txt per D-02 research finding).

Per D-03 (loader-only tolerance) and Pitfall 5 in RESEARCH (do NOT normalize backslash separators), copy bytes verbatim. Do NOT pretty-reformat JSON, do NOT change `\\` to `/` or vice versa, do NOT convert line endings.

Implementation: use `cp` from bash (preserves bytes on this Windows-with-bash setup) or equivalent. Concretely run these 12 copies:

```bash
mkdir -p tests/regression/BV132wizyEEB tests/regression/BV1C9QCBdE1U tests/regression/douyin_trae_ai

cp output/BV132wizyEEB/summary.md       tests/regression/BV132wizyEEB/summary.md
cp output/BV132wizyEEB/meta.json        tests/regression/BV132wizyEEB/meta.json
cp output/BV132wizyEEB/segs.json        tests/regression/BV132wizyEEB/segs.json
cp output/BV132wizyEEB/paragraphs.json  tests/regression/BV132wizyEEB/paragraphs.json

cp output/BV1C9QCBdE1U/summary.md       tests/regression/BV1C9QCBdE1U/summary.md
cp output/BV1C9QCBdE1U/meta.json        tests/regression/BV1C9QCBdE1U/meta.json
cp output/BV1C9QCBdE1U/segs.json        tests/regression/BV1C9QCBdE1U/segs.json
cp output/BV1C9QCBdE1U/paragraphs.json  tests/regression/BV1C9QCBdE1U/paragraphs.json

cp output/douyin_trae_ai/summary.md       tests/regression/douyin_trae_ai/summary.md
cp output/douyin_trae_ai/meta.json        tests/regression/douyin_trae_ai/meta.json
cp output/douyin_trae_ai/segs.json        tests/regression/douyin_trae_ai/segs.json
cp output/douyin_trae_ai/paragraphs.json  tests/regression/douyin_trae_ai/paragraphs.json
```

Per D-11, do NOT commit `frames/`, `audio.wav`, `video.mp4`, `video.info.json`, or any file other than the four listed per slug.

Per RESEARCH security note T1: before staging, verify these 12 files do NOT contain credentials. The four files are: summary.md (Markdown tutorial — by construction no secrets), meta.json (title/uploader/duration/url/description/video_path/aweme_id/source — no auth tokens), segs.json (ASR text only), paragraphs.json (aggregated text only). None of the producers (`src/download.py`, `agent/douyin_downloader.py`, `src/asr.py`, `agent/asr_v2.py`) write any cookie / API key / SESSDATA into these files. Sanity-grep for known secret patterns (see verify step below).
  </action>
  <verify>
    <automated>
# Confirm 12 files exist with sizes >= the documented thresholds (RESEARCH §"Baseline Snapshot Inventory")
test -s tests/regression/BV132wizyEEB/summary.md && \
test -s tests/regression/BV132wizyEEB/meta.json && \
test -s tests/regression/BV132wizyEEB/segs.json && \
test -s tests/regression/BV132wizyEEB/paragraphs.json && \
test -s tests/regression/BV1C9QCBdE1U/summary.md && \
test -s tests/regression/BV1C9QCBdE1U/meta.json && \
test -s tests/regression/BV1C9QCBdE1U/segs.json && \
test -s tests/regression/BV1C9QCBdE1U/paragraphs.json && \
test -s tests/regression/douyin_trae_ai/summary.md && \
test -s tests/regression/douyin_trae_ai/meta.json && \
test -s tests/regression/douyin_trae_ai/segs.json && \
test -s tests/regression/douyin_trae_ai/paragraphs.json && \
echo "OK: all 12 baseline files exist and are non-empty"

# Byte-equality with the source: cmp returns 0 only on byte-identical files
cmp output/BV132wizyEEB/summary.md       tests/regression/BV132wizyEEB/summary.md       && \
cmp output/BV132wizyEEB/meta.json        tests/regression/BV132wizyEEB/meta.json        && \
cmp output/BV132wizyEEB/segs.json        tests/regression/BV132wizyEEB/segs.json        && \
cmp output/BV132wizyEEB/paragraphs.json  tests/regression/BV132wizyEEB/paragraphs.json  && \
cmp output/BV1C9QCBdE1U/summary.md       tests/regression/BV1C9QCBdE1U/summary.md       && \
cmp output/BV1C9QCBdE1U/meta.json        tests/regression/BV1C9QCBdE1U/meta.json        && \
cmp output/BV1C9QCBdE1U/segs.json        tests/regression/BV1C9QCBdE1U/segs.json        && \
cmp output/BV1C9QCBdE1U/paragraphs.json  tests/regression/BV1C9QCBdE1U/paragraphs.json  && \
cmp output/douyin_trae_ai/summary.md     tests/regression/douyin_trae_ai/summary.md     && \
cmp output/douyin_trae_ai/meta.json      tests/regression/douyin_trae_ai/meta.json      && \
cmp output/douyin_trae_ai/segs.json      tests/regression/douyin_trae_ai/segs.json      && \
cmp output/douyin_trae_ai/paragraphs.json tests/regression/douyin_trae_ai/paragraphs.json && \
echo "OK: bytes match source"

# Variant fidelity: BV132wizyEEB meta.json must contain double-backslash (Windows variant per RESEARCH Pitfall 5)
grep -q 'output\\\\BV132wizyEEB\\\\video.mp4' tests/regression/BV132wizyEEB/meta.json && echo "OK: BV132wizyEEB \\\\ separator preserved"

# Variant fidelity: douyin_trae_ai meta.json must contain BOTH "aweme_id" AND "source": "douyin"
grep -q '"aweme_id"' tests/regression/douyin_trae_ai/meta.json && \
grep -q '"source": "douyin"' tests/regression/douyin_trae_ai/meta.json && \
echo "OK: douyin variant fields preserved"

# Confirm forbidden files were NOT committed (per D-11)
test ! -e tests/regression/BV132wizyEEB/video.mp4 && \
test ! -e tests/regression/BV132wizyEEB/audio.wav && \
test ! -e tests/regression/BV132wizyEEB/video.info.json && \
test ! -d tests/regression/BV132wizyEEB/frames && \
test ! -e tests/regression/BV1C9QCBdE1U/video.mp4 && \
test ! -e tests/regression/BV1C9QCBdE1U/audio.wav && \
test ! -e tests/regression/BV1C9QCBdE1U/video.info.json && \
test ! -d tests/regression/BV1C9QCBdE1U/frames && \
test ! -e tests/regression/douyin_trae_ai/video.mp4 && \
test ! -e tests/regression/douyin_trae_ai/audio.wav && \
test ! -d tests/regression/douyin_trae_ai/frames && \
echo "OK: no forbidden artifacts present"

# Security T1: no credentials leaked into committed JSON. Whitelist-grep for known secret token names.
# Expected: ZERO matches across all 9 JSON files (summary.md is plain prose; not in this scan).
! grep -E -i '(SESSDATA|VE_KEY_(CHEAP|QUALITY)|VE_BASE_URL|DOUYIN_COOKIES|cookie:|set-cookie|api[_-]?key|bearer\s)' \
    tests/regression/BV132wizyEEB/*.json \
    tests/regression/BV1C9QCBdE1U/*.json \
    tests/regression/douyin_trae_ai/*.json \
  && echo "OK: no credentials in baseline JSONs"

# .gitignore did not change in this task
git diff --quiet -- .gitignore && echo "OK: .gitignore unchanged"

# tests/ directory must be visible to git (i.e., NOT ignored)
git check-ignore tests/regression/BV132wizyEEB/summary.md; \
  test $? -eq 1 && echo "OK: tests/regression/ is tracked (check-ignore returned 1 = not ignored)"
    </automated>
  </verify>
  <done>
- All 12 baseline files exist at the exact paths listed in `<files>` and are byte-identical to their `output/<slug>/` sources (verified via `cmp`).
- `tests/regression/BV132wizyEEB/meta.json` retains its Windows-style `\\` separators (D-04 / Pitfall 5 — heterogeneity is intentional).
- `tests/regression/douyin_trae_ai/meta.json` retains both `aweme_id` and `source: "douyin"` fields (per RESEARCH §"JSON Shape Ground Truth — 抖音 path").
- No `frames/`, `audio.wav`, `video.mp4`, or `video.info.json` were committed under `tests/regression/`.
- No credentials present in any baseline JSON (security T1 cleared).
- `.gitignore` is unchanged; `git check-ignore` confirms `tests/regression/` is tracked normally.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write tests/regression/regression-check.md runbook (3-step procedure + manual-diff prompt)</name>
  <files>tests/regression/regression-check.md</files>
  <read_first>
    .planning/phases/01-preflight-regression-baseline/01-CONTEXT.md (D-07..D-10 — eyeball-diff method, three-step structure, "before each phase merge" cadence)
    .planning/phases/01-preflight-regression-baseline/01-RESEARCH.md (§"Regression Runbook Structure" gives the exact ≤150-line skeleton; §"Manual-Diff Prompt Template" gives the verbatim Claude prompt; §"Open Question 2 — list all 3 commands verbatim, recommendation: yes")
    CLAUDE.md (Phase 1.1 / 1.2 / 1.3 commands referenced verbatim in the runbook)
    agent/tools.py lines 75-81 (--force flag pattern that the runbook tells operators to use)
    output/BV132wizyEEB/meta.json (the "url" field provides the source URL recorded in the baseline table)
    output/BV1C9QCBdE1U/meta.json (source URL: https://www.bilibili.com/video/BV1C9QCBdE1U)
    output/douyin_trae_ai/meta.json (source URL: https://v.douyin.com/D4_5dfVmsIo/)
  </read_first>
  <action>
Write `tests/regression/regression-check.md`. Lift the runbook structure verbatim from RESEARCH §"Regression Runbook Structure" (research already wrote a complete operator-ready skeleton). Lift the diff-prompt verbatim from RESEARCH §"Manual-Diff Prompt Template".

The runbook MUST contain literal strings the verifier will check (see `<verify>` block below):
- `cp -r tests/regression/` (Step 1 stage command)
- `python -m agent.tools` (CLI invocation)
- `--force` (cache-bypass flag, per D-08 step 2)
- `Read tests/regression/` (Step 3 manual-diff instruction)
- `BV132wizyEEB`, `BV1C9QCBdE1U`, `douyin_trae_ai` (all three slugs in the baselines table)
- `PASS` and `FAIL` (the binary verdict labels in the prompt template)

The runbook is for a "Claude + human" pair (per D-07 / CLAUDE.md "Claude is sole decider"); use imperative second-person ("Copy this file", "Run this command", "Compare these two") not declarative documentation voice. Per D-09, the pass criterion is Claude's "no surprise drift" judgment — explicitly state this in the Pass/Fail section. Per D-10, explicitly tell the operator to record the verdict in the merging phase's `VERIFICATION.md`.

Final structure (sections in order, lifted from RESEARCH skeleton):

1. **Title + Purpose** — "Regression Baseline Runbook" / "Verify any milestone-2 phase change still reproduces the v1 regression baseline. Human + Claude operated, not CI. Run before merging any phase that touches `agent/` or `src/`."

2. **Baselines (do NOT modify)** — Three-row table: `BV132wizyEEB` (Code/AI workflow, https://www.bilibili.com/video/BV132wizyEEB) / `BV1C9QCBdE1U` (Godot tutorial, https://www.bilibili.com/video/BV1C9QCBdE1U) / `douyin_trae_ai` (AI/UI demo, https://v.douyin.com/D4_5dfVmsIo/). State D-13: snapshots are frozen for the milestone; only an intentional drift commits a new snapshot, with explanation in commit message.

3. **Procedure** — three numbered steps. Step 1: stage the four frozen JSONs into `output/<slug>/` via `cp -r tests/regression/<slug>/* output/<slug>/`; if `output/<slug>/video.mp4` is missing, recover via `python -m agent.tools download "<source-url>" --out output/<slug>`. Step 2: re-run only the stages the WIP phase touches; provide three commands verbatim (`transcribe ... --force`; `aggregate` with note that aggregate has no `--force`, delete-and-rerun caveat; `extract_frames` for the frame layer). Step 3: copy-paste the manual-diff prompt below.

4. **Manual-diff prompt template (copy-paste-ready)** — verbatim from RESEARCH §"Manual-Diff Prompt Template", five evaluation axes (STRUCTURE / TIMESTAMPS / CODE / FRAME REFS / RED-LINES), explicit `PASS — explainable diffs only:` / `FAIL — surprise drift:` output format.

5. **Pass/Fail criterion** — Restate D-09: PASS = explainable diffs / intentional improvements only; FAIL = structural drift not justified by phase goal, timestamps not in segs.json, fabricated content, broken frame filename grammar.

6. **Cadence (per D-10)** — "Run before each phase merge. Record verdict in `.planning/phases/<phase>/VERIFICATION.md`."

7. **(empty section heading reserved)** — `## Encoding Audit (PRE-04)` heading at the bottom; populated by Plan 01-03 (a separate `tests/regression/encoding-audit.md` file), so the runbook ends with a `> See [encoding-audit.md](encoding-audit.md) — populated by Plan 01-03 of this phase.` line. (This keeps Plan 01-01 self-contained without depending on 01-03's deliverable.)

Length target: ≤ 150 lines (operator-readable on one screen-pair).
  </action>
  <verify>
    <automated>
# File exists and non-empty
test -s tests/regression/regression-check.md && echo "OK: runbook exists"

# Required verbatim strings (per acceptance criteria)
grep -q 'cp -r tests/regression/' tests/regression/regression-check.md && echo "OK: cp -r tests/regression/ command present"
grep -q 'python -m agent.tools' tests/regression/regression-check.md && echo "OK: python -m agent.tools invocation present"
grep -q -- '--force' tests/regression/regression-check.md && echo "OK: --force flag mentioned"
grep -q 'Read tests/regression/' tests/regression/regression-check.md && echo "OK: Read tests/regression/ instruction present"

# All three baseline slugs named
grep -q 'BV132wizyEEB' tests/regression/regression-check.md && \
grep -q 'BV1C9QCBdE1U' tests/regression/regression-check.md && \
grep -q 'douyin_trae_ai' tests/regression/regression-check.md && \
echo "OK: all three baseline slugs referenced"

# Source URLs present (the operator pastes these into the download fallback step)
grep -q 'bilibili.com/video/BV132wizyEEB' tests/regression/regression-check.md && \
grep -q 'bilibili.com/video/BV1C9QCBdE1U' tests/regression/regression-check.md && \
grep -q 'v.douyin.com/' tests/regression/regression-check.md && \
echo "OK: source URLs present"

# PASS / FAIL verdict labels present (from the manual-diff prompt template)
grep -q 'PASS' tests/regression/regression-check.md && \
grep -q 'FAIL' tests/regression/regression-check.md && \
echo "OK: PASS/FAIL verdict labels present"

# Manual-diff axes named (so prompt remains structured)
grep -q 'STRUCTURE' tests/regression/regression-check.md && \
grep -q 'TIMESTAMPS' tests/regression/regression-check.md && \
grep -q 'CODE' tests/regression/regression-check.md && \
grep -q 'RED-LINES' tests/regression/regression-check.md && \
echo "OK: 5 evaluation axes named"

# D-10 cadence reference
grep -qE 'VERIFICATION\.md' tests/regression/regression-check.md && echo "OK: cadence (record in phase VERIFICATION.md) present"

# Length sanity
LINES=$(wc -l < tests/regression/regression-check.md)
echo "Runbook length: $LINES lines (target ≤ 150)"
test "$LINES" -le 200 && echo "OK: under 200-line cap"
    </automated>
  </verify>
  <done>
- `tests/regression/regression-check.md` exists and is operator-readable (≤ 200 lines).
- Contains all required verbatim strings: `cp -r tests/regression/`, `python -m agent.tools`, `--force`, `Read tests/regression/`.
- Names all three baseline slugs and embeds their source URLs.
- Manual-diff prompt template includes the five evaluation axes (STRUCTURE / TIMESTAMPS / CODE / FRAME REFS / RED-LINES) and the PASS/FAIL output format.
- D-09 pass criterion ("no surprise drift, explainable diffs OK") is explicitly stated.
- D-10 cadence ("record verdict in phase VERIFICATION.md") is explicitly stated.
- Runbook is in imperative second-person voice (per D-07: "this is an executable runbook, not documentation").
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public git history | Files committed in this plan become permanently visible to anyone with read access to the repo. |
| filesystem → git index | Whatever ends up in `tests/regression/` is staged; we must verify content before stage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | I (Information Disclosure) | The 12 baseline files committed by Task 1 (LOW severity) | mitigate | Task 1's `<verify>` block runs `! grep -E -i '(SESSDATA\|VE_KEY_(CHEAP\|QUALITY)\|VE_BASE_URL\|DOUYIN_COOKIES\|cookie:\|set-cookie\|api[_-]?key\|bearer\s)' tests/regression/*/*.json` and aborts if any match. The producers (`src/download.py`, `agent/douyin_downloader.py`, `src/asr.py`, `agent/asr_v2.py`) verified by RESEARCH never write cookies / SESSDATA / API keys into the four artifact types — these JSONs are pure transcript + metadata. summary.md is plain prose Markdown by construction. |
| T-01-01-02 | T (Tampering) | `.gitignore` regression accidentally exposing `output/` (LOW severity) | mitigate | Task 1's `<verify>` runs `git diff --quiet -- .gitignore` to confirm `.gitignore` was NOT modified by this plan. The plan does not touch `.gitignore` at all; verification catches any accidental edit. |
| T-01-01-03 | I (Information Disclosure) | Runbook commands paste source URLs into git history (LOW severity) | accept | Source URLs are public Bilibili / Douyin URLs already documented in `meta.json` files being committed; no incremental disclosure. |
| T-01-01-04 | T (Tampering) | Future contributor "fixes" `BV132wizyEEB/meta.json` backslashes during commit (LOW severity) | mitigate | Task 1 `<verify>` runs `cmp` on each file vs. its source to enforce byte-identity, AND a targeted grep `grep -q 'output\\\\BV132wizyEEB\\\\video.mp4'` confirms backslashes are preserved (Pitfall 5 in RESEARCH). |
</threat_model>

<verification>
After both tasks complete:

1. `tests/regression/` exists at repo root with three slug subdirectories (`BV132wizyEEB/`, `BV1C9QCBdE1U/`, `douyin_trae_ai/`).
2. Each slug subdirectory contains exactly four files: `summary.md`, `meta.json`, `segs.json`, `paragraphs.json`. No frames/, no audio.wav, no video.mp4, no video.info.json.
3. Every committed file is byte-identical to its `output/<slug>/` source (`cmp` returns 0).
4. Variant fidelity: `BV132wizyEEB/meta.json` retains `\\`; `BV1C9QCBdE1U/meta.json` retains `/`; `douyin_trae_ai/meta.json` retains `aweme_id` + `"source": "douyin"`.
5. `tests/regression/regression-check.md` exists, is ≤ 200 lines, contains all required verbatim strings (`cp -r tests/regression/`, `python -m agent.tools`, `--force`, `Read tests/regression/`), names all three slugs, embeds source URLs, includes PASS/FAIL prompt template with 5 axes, and references D-10 phase-VERIFICATION cadence.
6. No credentials in any baseline JSON (security grep clean).
7. `.gitignore` unchanged; `git check-ignore` confirms `tests/regression/` is tracked.
</verification>

<success_criteria>
**Maps to ROADMAP Phase 1 Success Criterion #1:**
> "tests/regression/ contains committed summary.md baselines for BV132wizyEEB, godot_brave [→ BV1C9QCBdE1U per D-02], and douyin_trae_ai plus a regression-check.md runbook describing how to re-run and manual-diff"

This plan delivers that criterion fully:
- 3 frozen baselines (with godot_brave correctly substituted by BV1C9QCBdE1U per D-02 — research verified godot_brave directory has no usable artifacts)
- runbook with re-run + manual-diff procedure

**Plan-level pass:** All `<verify>` blocks in both tasks return success; `<done>` criteria all true.
</success_criteria>

<output>
After completion, create `.planning/phases/01-preflight-regression-baseline/01-01-SUMMARY.md` documenting:
- 12 baseline files committed (paths + byte sizes)
- regression-check.md location and key sections
- Confirmation that `.gitignore` was untouched
- Confirmation that no credentials leaked
- Note that 01-02 will add `agent/io.py` loader-tolerance and 01-03 will add `tests/regression/encoding-audit.md` (referenced by the runbook's final stub section)
</output>
