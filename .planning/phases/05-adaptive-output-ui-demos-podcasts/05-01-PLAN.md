---
phase: 05-adaptive-output-ui-demos-podcasts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [TEACH-01, TEACH-02, TEACH-03, TEACH-04, TEACH-05]
must_haves:
  truths:
    - "CLAUDE.md contains a new top-level section `## 视频类型变奏` placed BEFORE the existing `## /summarize-video 完整工作流` section (per D-18); the existing 8-phase workflow main trunk is untouched"
    - "CLAUDE.md contains a Phase 2 classification step that emits a `mode` tag in `plan.md` front-matter — one of `replicate-guide` / `concept-explanation` / `extension-applications` / `interview-distillation` (TEACH-01 / D-01) — with optional `secondary_mode` and explicit `replicate-guide` fallback rule for ambiguous classification (D-03)"
    - "CLAUDE.md commits to a format-spec lock that ALWAYS holds regardless of selected mode: timestamp `[HH:MM:SS]`, code fence with explicit language, image embed `![](frames/seg_xxxx_xxxxxx.jpg)`, second-person imperative voice (TEACH-02 / P1.2 — content adaptive, form not)"
    - "CLAUDE.md ships exactly 8 hand-authored exemplar `summary.md` skeletons (4 modes × 2 each per D-05/D-07), each 50-120 lines minimal markdown showing mode-specific section structure with 1-2 example paragraphs (TEACH-03 / P1.3)"
    - "CLAUDE.md documents `output/<slug>/plan.md` written by Claude in Phase 2 of `/summarize-video`: free-form Markdown + 5-field YAML front-matter (`mode`, `secondary_mode`, `classification_evidence`, `fps_strategy_summary`, `estimated_sections`) per D-09; mandatory in Phase 2 but missing-file is warning-not-fatal so 17 archive re-runs still work (D-10)"
    - "CLAUDE.md documents `output/<slug>/depth_plan.md` as an OPTIONAL independent file for token-expensive videos (>30min or >50 sections), Claude self-judges (D-11); user can force-enable by `touch output/<slug>/.need_depth_plan` (no human-pause-confirm required, K2 — Claude is decider)"
    - "CLAUDE.md documents that mode-switch DURING writing is allowed: edit `plan.md` mode field + add `mode_switched_at: HH:MM:SS reason: ...` and rewrite the affected sections (D-04 / P1.5 fallback)"
    - "Existing `/summarize-video` 8-phase workflow trunk is byte-equal to current except (a) Phase 2 末尾 (CLAUDE.md current line ~145) inserts mode-classification + plan.md write step, and (b) Phase 4-7 reference mode-specific behavior (per D-21 — no parallel 8-phase forks)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Mode classification step in Phase 2 + format-spec lock + 8 exemplar skeletons + plan.md/depth_plan.md schema docs + mode-switch rule"
      contains: "## 视频类型变奏"
  key_links:
    - from: "CLAUDE.md Phase 2 classification step"
      to: "output/<slug>/plan.md YAML front-matter"
      via: "Claude writes 5 mandatory fields (mode, secondary_mode, classification_evidence, fps_strategy_summary, estimated_sections) after reading paragraphs.json"
      pattern: "mode: replicate-guide|mode: concept-explanation|mode: extension-applications|mode: interview-distillation"
    - from: "CLAUDE.md `## 视频类型变奏` section"
      to: "8 exemplar skeleton blocks (4 modes × 2)"
      via: "each mode has 2 minimal markdown skeletons sourced from archived corpus reshape (D-05/D-06)"
      pattern: "### Mode: replicate-guide|### Mode: concept-explanation|### Mode: extension-applications|### Mode: interview-distillation"
    - from: "CLAUDE.md format-spec lock"
      to: "ALL 4 modes' output documents"
      via: "regardless of mode, all summaries use [HH:MM:SS] / fenced code with lang / ![](frames/...) / second-person imperative"
      pattern: "格式锁定|format-spec lock|content adaptive.*form not"
---

<objective>
把 CLAUDE.md 升级成"自适应教学文档"的 prompt-engineering 决策中心：(1) 在现有 Phase 2 末尾嵌入视频分类步骤，让 Claude 写出 mode 标签到 `plan.md`；(2) 锁住 format spec（4 项不变量）让"内容自适应、形式不变"；(3) 嵌入 8 份手工 skeleton（4 模式×2）作为 exemplar prior，避免 PITFALLS P1.3 "Every video looks the same" 退化；(4) 文档化 plan.md / depth_plan.md / mode-switch 规则。

Purpose: TEACH-01..TEACH-05 五条 requirements 的 100% 兑现。**零 LOC Python**——本 plan 完全是 CLAUDE.md prompt engineering，下游 plan 02/03 才碰 Python。

Output: CLAUDE.md 顶部新增 `## 视频类型变奏` 章节（D-18 — 放在 `## /summarize-video 完整工作流` 之前），主干 8 阶段 workflow 几乎不动（仅 Phase 2 末尾插入 1 段 mode-classification + plan.md write 指令；Phase 4-7 在适当位置加 mode-specific 引用）。

Why prompt-only：ARCHITECTURE 已锁"adaptiveness is prompt engineering, not Python"（SUMMARY.md L77）。把模式选择放到 Python 会越过 K5 "Claude is decider" 红线（PROJECT.md Constraints L67）。CLAUDE.md exemplar 是 P1.3 showstopper 的唯一防线——没有 8 份 skeleton 锚定，Claude 会向 17 archived 偏置的 replicate-guide 模式塌缩。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-adaptive-output-ui-demos-podcasts/05-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

<interfaces>
<!-- 关键的"目标 prompt 形态" — executor 必须按这套契约写 CLAUDE.md，不要发散。 -->
<!-- 这些是 D-XX 决策的 byte-equal 落地点，不是建议。 -->

### CLAUDE.md 顶部插入位置（D-18）

新章节 `## 视频类型变奏` 必须放在以下两个 anchor 之间：

```
## 环境变量（.env）        <-- 现有 line ~110
- `VE_KEY_CHEAP` ...
- `DOUYIN_COOKIES_FILE` ...

---                          <-- 现有分隔符 line ~114

## 视频类型变奏              <-- 新增章节插入点
（本 plan 写作内容）
...

---                          <-- 新增分隔符（与下方对齐）

## /summarize-video 完整工作流    <-- 现有 line ~116（保留，主干不动）
```

### plan.md YAML front-matter 5 字段（D-09 字面锁定）

```yaml
# (yaml ---) 
mode: replicate-guide                    # 字符串：4 模式之一
secondary_mode: null                     # 字符串或 null
classification_evidence: |               # 多行字符串，4 模式各占比的判断证据
  70% 代码演示，30% 概念引入 ("ECS 是什么")，UP 全程 hands-on coding
fps_strategy_summary: 代码段 fps 0.4 / UI 段 fps 0.2 / 闲聊跳过   # 字符串
estimated_sections: 6                    # 整数
# (yaml ---) 
# Phase 2 判断笔记
（free-form Markdown，无 schema）
```

写错任何字段不让工具崩——free-form Markdown 不强 schema 校验（REQUIREMENTS 字面要求 "no schema enforcement"）。

### 4 模式标签字面锁定（D-01）

不可改名、不可省略下划线、不可加新模式标签：
- `replicate-guide` — 复刻指南（默认 fallback per D-03，与 17 archived 风格一致）
- `concept-explanation` — 原理讲解
- `extension-applications` — 延展应用
- `interview-distillation` — 访谈萃取

### 4 项 format-spec 不变量（TEACH-02 / P1.2 / D-21）

无论选哪个 mode，输出 summary.md 必须满足：

1. 时间戳格式：`[HH:MM:SS]`（不能写 `[H:MM]` 或 `12:34`）
2. 代码块带显式语言：```` ```gdscript ```` / ```` ```python ```` / ```` ```bash ```` 等（不能裸 fence）
3. 图片嵌入：`![](frames/seg_xxxx_xxxxxx.jpg)` 相对路径（不能 absolute / 不能空 alt）
4. 第二人称指令式："你"+动词（不能用"我们"或被动语态）

### 4 模式 → 候选源视频（D-06，plan execution 时由 executor 在 output/ 现有归档中挑最贴合的）

| Mode | 候选源 | 备注 |
|------|--------|------|
| replicate-guide | output/BV132wizyEEB（代码教程）+ output/godot_brave（Godot 操作） | 17 archived 主流，必有匹配 |
| concept-explanation | 从 douyin_trae_ai 类（AI 概念）+ 1 份代码教程"原理切片" reshape | 现有 corpus 弱；可以从代码教程提取"原理段"做轻 reshape |
| extension-applications | output/douyin_ai_kb / douyin_claude_code_hooks（应用整合） | 应用整合型现有归档充足 |
| interview-distillation | output/douyin_karpathy_llm_wiki（已归档，访谈结构最贴合） | 直接用，无需 reshape |

executor 自由选择最贴合的归档；若 reshape 工作量 > 200 行，停下来 surface "skeleton 过长，建议人审"（D-08 上限）。
</interfaces>

</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → CLAUDE.md content | user 修改 CLAUDE.md 是 trusted (本仓库私有) — 不是攻击面 |
| CLAUDE.md → Claude executor | Claude 读取 CLAUDE.md 作为 prompt prior — exemplar skeleton 内容是注入路径 |
| skeleton 引用的归档 frames/code | output/* 全部 user-generated，不是 untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | CLAUDE.md exemplar skeleton 内嵌 prompt-injection 字符串 | accept | exemplar 由 executor 从已归档 user-owned summary.md reshape；无外部输入路径，无注入风险 |
| T-05-01-02 | Information Disclosure | exemplar 中包含真实视频 BVid / 抖音 aweme_id | accept | 归档已 gitignore 在 output/，但 CLAUDE.md 引用 BV132wizyEEB / douyin_karpathy_llm_wiki 这种 ID 已经在 ROADMAP / PITFALLS 里公开过，不是新泄露 |
| T-05-01-03 | Repudiation | mode 选择无 audit trail，Claude 写错没法追责 | mitigate | plan.md 强制要求 `classification_evidence` 字段（D-09），任何 mode 决策都有理由可查；mode-switch 强制 `mode_switched_at` 标记（D-04） |
| T-05-01-04 | Denial-of-Service | CLAUDE.md skeleton 累计 > 1000 行导致 context 爆炸 | mitigate | D-08 硬上限：8 份 skeleton 累计 800-1000 行，单份 50-120 行；超限 surface "建议人审"，executor 必须停下 |
| T-05-01-05 | Elevation-of-Privilege | mode 选择跳过 plan.md 写入 → "silent decision"，越过 K5 | mitigate | D-10 强制 plan.md 写入是 Phase 2 末尾必须步骤；缺失打 warning（不强 fail 是为 K3 17 archive 兼容） |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 在 CLAUDE.md 插入 `## 视频类型变奏` 章节骨架 + 4 模式分类提示词 + format-spec lock + plan.md/depth_plan.md/mode-switch 文档</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md（必读全文，理解 Phase 1-8 工作流当前形态以及插入位置 anchor — 章节顺序必须严格按 D-18 不动主干）
    - .planning/phases/05-adaptive-output-ui-demos-podcasts/05-CONTEXT.md（必读 D-01..D-04 / D-09..D-12 / D-18 / D-21 — 这是本 task 的字面合同）
    - .planning/research/PITFALLS.md §P1.2 + §P1.3 + §P1.5（理解 format-spec 锁定的必要性 + exemplar 的 showstopper 地位）
    - .planning/codebase/CONVENTIONS.md §"Comments & Docstrings"（中文注释/章节标题约定，与现有 CLAUDE.md 风格一致）
    - .planning/REQUIREMENTS.md §TEACH-01, TEACH-02, TEACH-04, TEACH-05（4 条 req 字面契约）
    - output/BV132wizyEEB/summary.md（如存在，参考 17 archived 的"复刻指南"主流形态——format-spec lock 的 4 项不变量本来就是它的形态）
  </read_first>
  <action>
1. 在 CLAUDE.md 现有 line ~114 `---` 分隔符之后、`## /summarize-video 完整工作流` 之前，插入新章节 `## 视频类型变奏`（D-18 字面锁定位置；不允许放在文件末尾，不允许放在 `## 抖音支持` / `## YouTube 支持` 之间）。

2. 章节内部按以下子标题顺序写（顺序由 executor 自由裁量，但 5 个子节都必须出现，per D-18 "subsection ordering by readability"）：
   - **`### 模式分类（Phase 2 末尾步骤）`** — 写明 Claude 在 Phase 2 通读 paragraphs.json 后必须输出 4 行模式判断 + primary + secondary 结论。引文锁死：4 模式标签必须 byte-equal `replicate-guide` / `concept-explanation` / `extension-applications` / `interview-distillation`（D-01）。fallback 规则：模糊 → `replicate-guide`（D-03，与 17 archived 一致）。允许 mode-switch in-progress：写到一半发现误判 → 改 plan.md mode 字段 + 加 `mode_switched_at: HH:MM:SS reason: ...`（D-04）。
   - **`### plan.md 必写（Phase 2 末尾产物）`** — 文档化 5 字段 YAML front-matter（D-09 字面锁）：`mode` / `secondary_mode` / `classification_evidence` / `fps_strategy_summary` / `estimated_sections`。free-form Markdown 正文不强 schema。**强调**：mandatory in Phase 2，缺失打 warning 不强 fail（D-10 — K3 17 archive 兼容）。
   - **`### depth_plan.md 可选（仅 token-expensive 视频）`** — 触发条件：> 30min OR > 50 章节预估。Claude 自判（D-11，K2 — Claude is decider）；user 可手动 `touch output/<slug>/.need_depth_plan` 强制启用。**不**走 user-pause-confirm 流程。
   - **`### 格式锁定（无论哪个 mode，4 项不变量）`** — 字面引用 4 项（TEACH-02 / D-21）：(1) `[HH:MM:SS]` 时间戳；(2) 代码 fence 必带显式语言（```` ```gdscript ````）；(3) 图片 `![](frames/seg_xxxx_xxxxxx.jpg)`；(4) 第二人称指令式（"你" + 动词）。锁死语 "**内容自适应；形式不变**"（P1.2）。
   - **`### 4 模式 skeleton（exemplar prior）`** — 留 placeholder，task 2 写入 8 份具体 skeleton。

3. 在现有 `## /summarize-video 完整工作流` → `### Phase 2: 理解内容` 末尾（现行 line ~148 `- 决定分段抽帧策略（下一步用）` 之后），加一行 `**2.4** 输出模式判断 + 写 plan.md（详见 § 视频类型变奏 → 模式分类）`。**不**重写 Phase 2 整段——只追加 2.4 子步（D-18 "do not rewrite trunk"）。

4. 在现有 `### Phase 4: 看帧（多模态，核心步骤）` / `### Phase 5: 规划大纲` / `### Phase 6: 逐节写作` 各自末尾加一行 mode-aware 引用：
   - Phase 4：`> **mode 提示**：podcast / interview-distillation 时帧用量极少（每章节 1-2 帧，详见 § 视频类型变奏）；UI demo 模式有 4 子规则（不在本 plan 范围 — 见 plan 03 后续追加）。`
   - Phase 5：`> **mode 提示**：concept-explanation 大纲按"问题 → 反直觉答案 → 最小例证"流；interview-distillation 走 chapters.json + speaker turns（plan 03 引入）。其他 mode 按本 phase 主流程。`
   - Phase 6：`> **mode 提示**：复刻指南是默认风格（17 archived 主流）；其他 mode 见 § 视频类型变奏 skeleton 部分。`
   不要改 Phase 1 / 3 / 7 / 8（D-21 — 不分叉 /summarize-video 8 阶段）。

5. **CRITICAL Backward-compat（K3 + D-10）**：
   - 现有 5 条核心命令 `download / transcribe / aggregate / extract_frames / cleanup_frames` 文档不动
   - `/summarize-video` Phase 1 / 3 / 7 / 8 整段不动（只 Phase 2 加 2.4 子步、Phase 4/5/6 加单行 mode 提示）
   - 现有 `## 抖音支持` / `## YouTube 支持` / `## Windows zh-CN 终端设置` / `## 决策支持工具（Phase 4，可选）` 整段不动
   - 已归档 17 条视频不需要 plan.md（17 archive 没有该文件 → 老 re-run 路径打 warning 不 fail）

6. **不在本 task 写**：8 份 skeleton（task 2 负责）；UI demo 4 子规则（plan 03 D-20）；podcast 1-2 帧/章节 + chapters.json schema（plan 03 D-19）；pyannote setup 章节（plan 03 D-15）；VTT priority 文档（plan 03 D-30..D-32）。

7. 写完后 `git diff CLAUDE.md` 自查：(a) `## 视频类型变奏` 在 line < 现有 `## /summarize-video` 行号；(b) Phase 2 增加 1 行 2.4，Phase 4/5/6 各增加 1 行 mode 提示，其余 8 阶段文本无修改；(c) 5 条核心命令表无修改；(d) 抖音/YouTube/Windows 三节无修改。
  </action>
  <verify>
    <automated>
# 1. 视频类型变奏章节存在且在 /summarize-video 之前
grep -n "^## 视频类型变奏" CLAUDE.md && grep -n "^## /summarize-video" CLAUDE.md
# 期望: 行号 1 < 行号 2

# 2. 4 模式标签字面 byte-equal 都出现
grep -c "replicate-guide" CLAUDE.md  # >= 4 (规范 + skeleton placeholder + Phase 6 提示 + fallback 说明)
grep -c "concept-explanation" CLAUDE.md   # >= 2
grep -c "extension-applications" CLAUDE.md  # >= 2
grep -c "interview-distillation" CLAUDE.md  # >= 2

# 3. plan.md / depth_plan.md / mode_switched_at / classification_evidence 文档化
grep -F "classification_evidence" CLAUDE.md
grep -F "mode_switched_at" CLAUDE.md
grep -F "depth_plan.md" CLAUDE.md
grep -F ".need_depth_plan" CLAUDE.md

# 4. 4 项 format-spec 不变量都引用
grep -F "[HH:MM:SS]" CLAUDE.md
grep -F "![](frames/seg" CLAUDE.md
grep -F "第二人称" CLAUDE.md
grep -E "代码.{0,5}fence|代码块.{0,5}语言|```gdscript" CLAUDE.md

# 5. Phase 2 加了 2.4 子步
grep -E "^\*\*2\.4\*\*.*plan\.md" CLAUDE.md

# 6. 5 条核心命令表无修改 (line 11-17 字符级 diff 必须为 0)
git diff CLAUDE.md -- | grep -E "^[-+].*python -m agent\.tools (download|transcribe|extract_frames|aggregate|cleanup_frames)" | wc -l
# 期望: 0 (5 条命令本身字面字节不变)
    </automated>
  </verify>
  <done>
    - CLAUDE.md 比 task 开始前增长 100-300 行（章节骨架 + Phase 2/4/5/6 mode 提示），不超过 + 400 行（skeleton 留给 task 2）
    - 4 模式标签全部 byte-equal 出现 ≥ 2 次
    - plan.md 5 字段 / depth_plan.md / mode_switch 文档化齐全
    - 4 项 format-spec 不变量全部锁定
    - 5 条核心命令文档零修改、抖音/YouTube/Windows 三节零修改、Phase 1/3/7/8 零修改
  </done>
</task>

<task type="auto">
  <name>Task 2: 在 `## 视频类型变奏 → ### 4 模式 skeleton` 嵌入 8 份手工 minimal skeleton（4 mode × 2 each, 累计 800-1000 行内）</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md（task 1 完成后的版本，定位 `### 4 模式 skeleton` placeholder 位置）
    - .planning/phases/05-adaptive-output-ui-demos-podcasts/05-CONTEXT.md（D-05..D-08 字面合同：路径 b+c 混合、4×2=8 份、50-120 行/份、累计 800-1000 行硬上限、reshape 来源）
    - .planning/research/PITFALLS.md §P1.3（"Every video looks the same" — exemplar 是 P1.3 showstopper 的唯一防线，executor 必须严肃对待）
    - output/BV132wizyEEB/summary.md（replicate-guide 候选源 #1 — 直接 reshape 现有内容）
    - output/godot_brave/summary.md 或类似 Godot 教程归档（replicate-guide 候选源 #2）
    - output/douyin_karpathy_llm_wiki/summary.md（interview-distillation 候选源 — 已归档，访谈结构最贴合 — D-06）
    - output/douyin_ai_kb/summary.md OR output/douyin_claude_code_hooks/summary.md（extension-applications 候选源）
    - output/douyin_trae_ai/summary.md（concept-explanation 候选源 之一；如归档 corpus 不够 concept-explanation，executor 在已知代码教程归档里挑一段"原理切片" reshape——D-06 已授权这种轻 reshape）
    - .planning/research/SUMMARY.md §"Phase 5" §"Hand-authored exemplar skeletons in CLAUDE.md (P1.3) — 2-3 per dimension. Without these, T1 is at risk."
  </read_first>
  <action>
1. 定位 task 1 留下的 `### 4 模式 skeleton（exemplar prior）` placeholder。在其下嵌入 8 份 skeleton，**严格按以下顺序与 layout**：

```
### 4 模式 skeleton（exemplar prior）

> **使用方式**：Claude 在 Phase 2 决定 mode 后，写作时参照对应 mode 的 2 份 skeleton 做章节切分 + 段落形态。**不**复制粘贴；exemplar 是 prior，不是模板。

#### Mode: replicate-guide（复刻指南，17 archived 主流）

##### Skeleton 1: `<取自 BV132wizyEEB-derived 的代码教程 1 份>`
（50-120 行 minimal markdown）

##### Skeleton 2: `<取自 godot_brave 类 Godot 操作教程 1 份>`
（50-120 行 minimal markdown）

#### Mode: concept-explanation（原理讲解）

##### Skeleton 1: `<取自 douyin_trae_ai 类 AI 概念视频 1 份；只展示"先抛核心问题 → 给出反直觉答案 → 用 1 个最小例证 → 引申应用边界"流>`

##### Skeleton 2: `<从一份代码教程的原理段切片 reshape；展示"如果只讲原理会怎么写" — 不放完整代码块，只放概念图和 1 段最小例证>`

#### Mode: extension-applications（延展应用）

##### Skeleton 1: `<取自 douyin_ai_kb / douyin_claude_code_hooks 一份；展示"3-5 个应用整合 → 各自适用边界 → 横向对比"流>`

##### Skeleton 2: `<取自第二份应用整合归档；展示同一工具/概念的不同应用场景串讲>`

#### Mode: interview-distillation（访谈萃取）

##### Skeleton 1: `<取自 douyin_karpathy_llm_wiki；展示"speaker turns + key claims + timestamp-navigable quotes" 形态；无 frames/ image，用 blockquote 替代图片>`

##### Skeleton 2: `<reshape 同一访谈的不同切片，或从另一访谈类归档挑一份；展示当 chapters.json 存在时如何按章节组织而非按 speaker turn>`
```

2. 每份 skeleton 必须满足：
   - **50-120 行 minimal markdown**（D-07）—— 超过 120 行 stop，截断到 mode-specific 章节结构 + 1-2 个示例段落
   - **保持 4 项 format-spec 不变量**（与本 plan task 1 的 lock 一致）—— `[HH:MM:SS]` / 代码 fence 带语言 / `![](frames/seg_xxxx_xxxxxx.jpg)` / 第二人称指令式
   - **mode-specific 章节流要鲜明**：
     - replicate-guide：`步骤 N：动作 → 截图 → 代码 → 为什么这么做` 节奏（17 archived 主流）
     - concept-explanation：`核心问题 → 反直觉答案 → 最小例证 → 应用边界`（不放完整代码块）
     - extension-applications：`场景 1 / 场景 2 / 场景 3 横向罗列 + 各自边界 + 总评`
     - interview-distillation：`> [HH:MM:SS] 嘉宾名："核心引文"\n\n（你对引文的提炼 / 与其他段的呼应）` 替代图片嵌入（D-21 podcast 模式 1-2 帧/章节由 plan 03 D-19 落地，本 plan skeleton 仍可纯文本）
   - **来源标注**：每份 skeleton 第 1 行用 HTML 注释标注来源 `<!-- 来源: output/<slug>/summary.md (reshape) -->`，方便未来维护时回溯

3. **累计行数硬上限**（D-08）：8 份 skeleton + 章节框架累计 ≤ 1000 行；超过 stop 工作并 surface："skeleton 累计 N 行（> 1000）— 建议 user 人审"，等用户裁决。

4. **执行优先级**（如果时间紧）：
   - Tier 1 必须完成：interview-distillation Skeleton 1（直接用 douyin_karpathy_llm_wiki，最贴合，工作量最低）+ replicate-guide Skeleton 1（取自 BV132wizyEEB / godot_brave 任一即可，17 archived 主流）
   - Tier 2 高优：concept-explanation Skeleton 1（D-06 标记 corpus 弱 — executor 可从代码教程挑"原理段"轻 reshape）+ extension-applications Skeleton 1
   - Tier 3 凑齐 8 份：剩下 4 份各 mode 的第 2 份；若工作量真的爆了 D-08 1000 行上限，可以保留 4 模式 × 1 = 4 份 skeleton（每 mode 至少 1 份是 P1.3 防线最低标准）+ 在章节末尾标注 `> 注：每模式第 2 份 skeleton 留作 milestone v2 补充（per D-08 行数上限决策）`

5. **绝对禁止**：
   - 在 skeleton 里用真人姓名、敏感商业信息（PROJECT.md OOS — 单用户私有归档不该被 CLAUDE.md 公开化）—— 引用 douyin_karpathy_llm_wiki 这种已经在 ROADMAP 公开过的归档名 OK，但具体引文里的人名 / 公司名应保留原样不脱敏（user 自己的归档，K3 不动现有内容）
   - 编造没有的代码 / 截图引用 — 不注水不编造红线（PROJECT.md L11）。skeleton 引用的 `frames/seg_xxxx_xxxxxx.jpg` 路径必须是 placeholder 形态（写 `frames/seg_0030_000015.jpg` 但不要谎称该文件存在），或者干脆从源归档 reshape 已存在的引用。
   - 把完整的 skeleton 压缩成"省略号"（这 defeat 了 exemplar 的目的）

6. 写完后 line count check：`wc -l CLAUDE.md` 当前 - task 1 完成后的 wc -l = skeleton 区段净增量；应在 600-1000 之间（章节框架已在 task 1 写完，本 task 净增主要是 skeleton 内容）。
  </action>
  <verify>
    <automated>
# 1. 8 份 skeleton 章节标题都存在
grep -c "^##### Skeleton" CLAUDE.md  # >= 8

# 2. 4 个 mode 子标题都存在
grep -E "^#### Mode: replicate-guide" CLAUDE.md
grep -E "^#### Mode: concept-explanation" CLAUDE.md
grep -E "^#### Mode: extension-applications" CLAUDE.md
grep -E "^#### Mode: interview-distillation" CLAUDE.md

# 3. 每份 skeleton 都标注了来源
grep -c "<!-- 来源: output/" CLAUDE.md  # >= 4 (P1.3 fallback 最低标准 4 份；理想 8 份)

# 4. 累计行数硬上限 (D-08)
test $(wc -l < CLAUDE.md) -le 1500  # task 1 后基线 ~400 + skeleton ~1000 = 1400 上限留余量

# 5. format-spec 不变量在 skeleton 里也保持
grep -c "\[HH:MM:SS\]\|\[[0-9][0-9]:[0-9][0-9]:[0-9][0-9]\]" CLAUDE.md  # >= 8 (每 skeleton 至少 1 个真实或 placeholder 时间戳)
grep -c "!\[\](frames/seg_" CLAUDE.md  # >= 4 (replicate-guide / extension-applications 类必有 frame 引用)

# 6. interview-distillation 用 blockquote 替代图片 (D-21 / TEACH-10 兑现)
grep -E "^> \[[0-9][0-9]:[0-9][0-9]:[0-9][0-9]\]" CLAUDE.md  # >= 1 (interview skeleton 至少 1 处)
    </automated>
  </verify>
  <done>
    - 8 份 skeleton 全部嵌入（最低 fallback: 4 模式 × 1 份 = 4 份，需在 CLAUDE.md 显式 surface 该决策给 user）
    - 4 模式章节标题 byte-equal 出现
    - 累计行数 ≤ 1000 行（D-08 硬上限）；CLAUDE.md 总行数 ≤ 1500
    - format-spec 4 项不变量在每份 skeleton 里都保持（grep 验证）
    - interview-distillation 用 blockquote 替代图片（验证 P6.4 的"podcast 不嵌帧"原则）
    - 每份 skeleton 第 1 行有 `<!-- 来源: output/<slug>/summary.md (reshape) -->` 标注
  </done>
</task>

</tasks>

<verification>
- [ ] CLAUDE.md `## 视频类型变奏` 章节存在且位置正确（D-18 — 在 `/summarize-video 完整工作流` 之前）
- [ ] 4 模式标签字面 byte-equal: `replicate-guide` / `concept-explanation` / `extension-applications` / `interview-distillation`（D-01 / TEACH-01）
- [ ] 4 模式 skeleton 至少 1 份/模式（P1.3 防线最低标准；理想 2 份/模式 = 8 份 per D-05/D-07）
- [ ] 4 项 format-spec 不变量在文档 + skeleton 中都被锁定（TEACH-02 / D-21 / P1.2）
- [ ] plan.md 5 字段 YAML front-matter 文档化（D-09 / TEACH-04）
- [ ] depth_plan.md 可选 + Claude 自判 + `.need_depth_plan` 强制启用（D-11 / TEACH-05 / K2）
- [ ] mode-switch in-progress 规则文档化（D-04 / P1.5）
- [ ] `/summarize-video` 8 阶段主干 byte-equal 不变，仅 Phase 2 加 2.4 子步、Phase 4/5/6 加单行 mode 提示（D-21 — 不分叉）
- [ ] 5 条核心命令表零字符修改（K3 backward-compat）
- [ ] 抖音 / YouTube / Windows zh-CN / 决策支持工具 4 节零修改
- [ ] 17 archive 没 plan.md → re-run 不报 fatal（D-10 — warning 不 fail）
</verification>

<success_criteria>
1. (TEACH-01 / D-01..D-04) Claude 看完 paragraphs.json 后能从 4 模式（replicate-guide / concept-explanation / extension-applications / interview-distillation）选出 mode + secondary_mode 写入 plan.md，模糊时 fallback `replicate-guide`，写作中途允许 mode-switch。
2. (TEACH-02 / D-21 / P1.2) 无论选哪个 mode，输出 summary.md 必须满足 4 项 format-spec 不变量。
3. (TEACH-03 / D-05..D-08 / P1.3) CLAUDE.md 嵌入至少 4 份 skeleton（理想 8 份），覆盖 4 模式各 ≥ 1 份；exemplar 来自已归档 corpus reshape（¥0），累计 ≤ 1000 行。
4. (TEACH-04 / D-09..D-10) `output/<slug>/plan.md` 5 字段 YAML front-matter + free-form Markdown body 在 Phase 2 末尾必写；缺失打 warning 不强 fail（K3）。
5. (TEACH-05 / D-11..D-12) `output/<slug>/depth_plan.md` 可选独立文件，Claude 自判 token-expensive 视频时启用；user 可 `touch .need_depth_plan` 强制启用，不走 user-pause-confirm（K2）。
</success_criteria>

<output>
After completion, create `.planning/phases/05-adaptive-output-ui-demos-podcasts/05-01-SUMMARY.md` covering:
- TEACH-01..TEACH-05 五条 requirements 兑现状态（每条 PASS/PARTIAL + 证据）
- D-01..D-12 / D-18 / D-21 决策落地点列表
- skeleton 数量 / 累计行数 / 选用的归档源 list
- CLAUDE.md 行数变化（before / after / delta）
- backward-compat 验证（grep 5 条核心命令零修改 + 8 阶段主干差异行数）
- 已知风险或后续 plan 需要承接的 hook（mode 提示 placeholder → plan 03 完成 podcast/UI demo skeleton）
</output>
